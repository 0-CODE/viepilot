# GraphQL schema — ViePilot framework

**Not applicable.** The ViePilot framework repo does not expose a GraphQL API.

Use this file in **consumer projects** that generate schema docs from code or `.viepilot/schemas/`.

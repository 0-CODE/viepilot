# Brainstorm Session - 2026-04-02

## Session Info
- **Date**: 2026-04-02
- **Participants**: User, Claude (claude-sonnet-4-6)
- **Status**: Completed
- **Reference codebase**: `~/DEV_PROJECTS/for-learn-claude-cli-main`
  > Source code Claude Code (Anthropic CLI) — leaked snapshot 2026-03-31.
  > Dùng làm đối tượng nghiên cứu và tham khảo cho ViePilot v2.
  > Báo cáo phân tích đầy đủ: `~/DEV_PROJECTS/for-learn-claude-cli-main/reports/`

---

## Product Horizon

### MVP v2 (ship first)

**Core Architecture:**
- Typed state machine cho vp-auto execution loop `(MVP)`
- Layered silent recovery (3 tầng: L1 lint, L2 test fix, L3 scope reduce) `(MVP)`
- Tách TRACKER.md thành index ≤30 dòng + logs/ directory `(MVP)`
- Continuous state writing — HANDOFF.json ghi sau mỗi sub-task `(MVP)`
- Sub-agent scope contract (fork boilerplate) `(MVP)`
- Task metadata declarations (`type`, `complexity`, `write_scope`, `recovery_budget`) `(MVP)`
- Static/dynamic context boundary trong AI-GUIDE.md `(MVP)`
- Parallel context loading — batch read instructions `(MVP)`

**Gaps (promoted to MVP):**
- Gap A: crystallize auto-populate `write_scope`, `type`, `recovery_budget` trong TASK.md `(MVP)`
- Gap B: `recovery_overrides` block mechanism per task (L1/L2/L3 boolean) `(MVP)`
- Gap C: vp-request UI direction route detection `(MVP)`
- Gap D: Non-blocking HANDOFF.log recovery events `(MVP)`
- Gap G: Compliance domain detection → auto-block L3 cho auth/payment/data/crypto paths `(MVP)`

**Workflow improvements:**
- vp-resume 3-tier context restore (<30min / 30min–4h / >4h) `(MVP)`
- vp-request NLP intake — description-first, 2-band confidence threshold `(MVP)`
- vp-request horizon-aware routing `(MVP)`
- crystallize generate v2 artifacts (execution_state block, HANDOFF.json v2 schema) `(MVP)`
- vp-evolve generate v2 TASK.md template `(MVP)`
- HANDOFF.log append-only event log + phase-boundary rotation `(MVP)`
- 3-tier validation pipeline: contract → write_scope lock → git gate `(MVP)`

### Non-goals for MVP v2
- Thay đổi skill file format (SKILL.md vẫn giữ nguyên)
- Breaking changes với ViePilot v1 projects
- Rewrite vp-tools CLI

### Post-MVP
- Fork state updates — background bookkeeping sau task PASS `(Post-MVP)`
- Parallel task execution dựa trên dependency graph `(Post-MVP)`
- Conditional skill activation theo `paths:` frontmatter `(Post-MVP)`
- Gap G extended: description keyword scan + vp-auto pre-flight warning `(Post-MVP)`
- Gap G extended: consensus/contract compliance domains `(Post-MVP)`
- vp-request compliance domain detection → auto-suggest recovery_overrides `(Post-MVP)`

### Future / Exploratory
- Multi-agent coordinator cho complex phases `(Future)`
- ML-based auto-approval cho low-risk tasks `(Future)`
- Token budget awareness trong vp-auto `(Future)`
- Team memory sync cho multi-developer projects `(Future)`
- Gap E: `/vp-status --all` cross-project aggregation `(Future)`
- Gap F: Career documentation export từ `logs/decisions.md` `(Future)`
- AutoDream consolidation cho project knowledge → STACKS.md `(Future)`

### Deferred Capabilities
- Parallel task execution — cần dependency graph infrastructure trước
- AutoDream — cần layered context architecture trước

---

## Topics Discussed

### Topic 1: Tổng quan Claude Code codebase

**Context**: Nghiên cứu source code Claude Code (Anthropic) từ `~/DEV_PROJECTS/for-learn-claude-cli-main` để rút ra bài học cho ViePilot v2.

**Thống kê codebase tham khảo**:
- ~1,900 files, ~512,000 dòng TypeScript
- Runtime: Bun, UI: React + Ink
- 40+ tools, 80+ slash commands, 140+ React components

**Báo cáo chi tiết tại**:
```
~/DEV_PROJECTS/for-learn-claude-cli-main/reports/
├── REPORT-query-engine.md          ← Kiến trúc trung tâm
├── REPORT-memory-system.md         ← 5-layer memory
├── REPORT-multi-agent-coordinator.md ← AI điều phối AI
├── REPORT-permission-system.md     ← Defense in depth
├── REPORT-skills-plugin-system.md  ← Extensibility
├── REPORT-tool-system.md
├── REPORT-context-building.md
├── REPORT-service-layer.md
├── REPORT-session-storage.md
├── REPORT-bridge-ide-integration.md
└── REPORT-lessons-learned.md
```

**Decisions**:
- [x] Dùng Claude Code làm benchmark kiến trúc cho ViePilot v2
- [x] Ưu tiên học từ: Query Engine, Memory System, Multi-Agent, Permission System, Skills System

---

### Topic 2: Query Engine — Bài học cho vp-auto

**Context**: `~/DEV_PROJECTS/for-learn-claude-cli-main/reports/REPORT-query-engine.md`

**Insight cốt lõi**:

1. **Async Generator Pattern** — `submitMessage()` yield events realtime; tools execute overlap với streaming
2. **Withheld Messages + Layered Recovery**:
   ```
   Error → L1: collapse_drain_retry
         → L2: reactive_compact_retry
         → L3: max_output_tokens_escalate
         → L4: multi-turn recovery (max 3)
         → CHỈ surface cho user khi tất cả fail
   ```
3. **7 typed state transitions**: `collapse_drain_retry` | `reactive_compact_retry` | `max_output_tokens_escalate` | `max_output_tokens_recovery` | `stop_hook_blocking` | `token_budget_continuation` | `completed`
4. **Token budget + diminishing returns**: delta < 500 tokens + continuationCount ≥ 3 → inject nudge
5. **Stop Hooks** — fire-and-forget sau mỗi turn: extractMemories, autoDream, shell scripts

**Áp dụng cho ViePilot v2**:
- vp-auto cần typed state transitions thay vì prose workflow
- 3-layer silent recovery trước control point
- Task complexity estimate để detect "đang bị stuck"

**Decisions**:
- [x] Thay thế prose-based workflow bằng typed state machine trong PHASE-STATE.md
- [x] Implement 3-layer silent recovery: L1 lint → L2 test fix → L3 scope reduce → control_point

---

### Topic 3: Memory System — Bài học cho context management

**Context**: `~/DEV_PROJECTS/for-learn-claude-cli-main/reports/REPORT-memory-system.md`

**Kiến trúc 5 tầng của Claude Code**:
```
Layer 1: System Prompt Injection  — MEMORY.md index, LUÔN inject (≤200 dòng)
Layer 2: Query-time Recall        — Sonnet chọn ≤5 relevant files theo query
Layer 3: Extract Memories         — AI fork tự quyết định gì đáng nhớ sau mỗi turn
Layer 4: AutoDream                — Nightly consolidation (24h + 5 sessions gate)
Layer 5: Team Memory Sync         — Secret scan → PUT server → pull on session start
```

**Áp dụng cho ViePilot v2**:
```
.viepilot/
├── TRACKER.md           ← Index ≤30 dòng (current state only)
├── logs/
│   ├── decisions.md     ← Load on-demand (khi cần rationale)
│   ├── blockers.md      ← Load on-demand (khi debug)
│   └── version-history.md
└── phases/
    └── N/PHASE-STATE.md ← Runtime state machine cho phase đang chạy
```

**Decisions**:
- [x] TRACKER.md refactor thành index ≤30 dòng
- [x] Tách Decision Log, Blocker History, Version History ra file riêng
- [x] AI-GUIDE.md thêm static/dynamic boundary; enforce: chỉ load detail khi cần

---

### Topic 4: Multi-Agent Coordinator — Bài học cho parallel execution

**Context**: `~/DEV_PROJECTS/for-learn-claude-cli-main/reports/REPORT-multi-agent-coordinator.md`

**3 execution models của Claude Code**:
```
SYNC:     AgentTool → block → return result
ASYNC:    AgentTool → return agentId ngay → <task-notification> XML khi xong
TEAMMATE: spawnTeammate → mailbox-based messaging
```

**Fork Pattern + Prompt Cache Sharing**:
- Tất cả fork children có prefix **byte-identical** với parent
- Chỉ khác final directive → maximize cache hits, ~70% cost reduction

**Fork Boilerplate cho vp-auto**:
```
SCOPE CONTRACT - Task {X}
├── Execute: {specific objective only}
├── Write scope: {specific files/dirs}
├── Do NOT: modify out-of-scope files, ask questions
├── Output: YAML report starting with "SCOPE: Task {X}"
└── End with: "RESULT: PASS|FAIL|BLOCKED — {reason}"
```

**Decisions**:
- [x] Background fork cho state updates sau task PASS (Post-MVP)
- [x] Scope contract bắt buộc cho mỗi task execution
- [ ] Parallel execution infrastructure (Post-MVP)

---

### Topic 5: Permission System — Bài học cho safety và recovery

**Context**: `~/DEV_PROJECTS/for-learn-claude-cli-main/reports/REPORT-permission-system.md`

**Defense in depth**:
```
Permission System (layer 1): Tool có được execute không?
Sandbox System   (layer 2): Khi execute, OS cho phép làm gì?
```

**Circuit Breaker**: 3 consecutive denials → fallback to manual.

**Áp dụng cho ViePilot v2**:
- Task `write_scope` declaration bắt buộc → vp-auto verify AI không write ngoài scope
- Recovery circuit breaker: max attempts per layer (budget-aware)
- 3-tier validation pipeline: contract → write_scope lock → git gate

**Decisions**:
- [x] Task metadata `write_scope` declaration bắt buộc
- [x] vp-auto verify write scope sau mỗi sub-task commit
- [x] Circuit breaker: max attempts per layer theo recovery_budget

---

### Topic 6: Skills System — Bài học cho ViePilot skill quality

**Context**: `~/DEV_PROJECTS/for-learn-claude-cli-main/reports/REPORT-skills-plugin-system.md`

**Conditional Activation** (paths: frontmatter):
```yaml
paths: ["src/**/*.ts"]  # Skill chỉ visible khi làm việc với matching files
```

**Áp dụng cho ViePilot v2**:
```yaml
# vp-auto/SKILL.md
paths: [".viepilot/TRACKER.md"]

# vp-resume/SKILL.md
paths: [".viepilot/HANDOFF.json"]

# vp-debug/SKILL.md
paths: [".viepilot/phases/*/PHASE-STATE.md"]
```

**Decisions**:
- [x] Thêm `paths:` conditional activation vào tất cả vp-* skills (Post-MVP)

---

### Topic 7: Vấn đề kiến trúc sâu — 3 cải tiến cốt lõi

**Vấn đề 1: Prose workflow vs Typed State Machine**

```yaml
execution_state:
  current: task_2_3
  status: executing
  available_transitions:
    on_pass: → update_state → task_2_4
    on_fail_l1: → auto_fix_lint (max 1) → re_verify
    on_fail_l2: → auto_fix_tests (max 2) → re_verify
    on_fail_l3: → control_point
```

**Vấn đề 2: TRACKER.md blob vs Index + Topic Files**

TRACKER.md ≤30 dòng (current state pointer only), chi tiết trong `logs/`.

**Vấn đề 3: 1-tầng recovery vs 3-tầng silent recovery**

```
L1 — Lint/Format (silent, 1 attempt) → re-verify
L2 — Targeted test fix (silent, 2 attempts) → re-verify
L3 — Scope reduction (silent, 1 attempt) → PARTIAL
--- control_point (user thấy lần đầu) ---
```

**Decisions**:
- [x] Refactor PHASE-STATE.md template để include execution_state với typed transitions
- [x] Refactor TRACKER.md template thành index ≤30 dòng
- [x] Implement 3-layer silent recovery trong vp-auto workflow
- [x] Continuous HANDOFF.json writing sau mỗi sub-task

---

### Topic 8: Tool System & Session Storage — Bài học từ reports còn lại

**Context**: `REPORT-tool-system.md` + `REPORT-session-storage.md`

**Tool System insights**:
- Conservative defaults: tool phải opt-in capabilities (readOnly, cache, etc.)
- 3-tier validation: parse → validate → execute (strict order, no skip)
- Concurrency batching: multiple tool calls trong 1 turn execute concurrently
- Deferred tools: schema chỉ fetch khi cần → context window efficiency

**Session Storage insights**:
- Append-only JSONL: không overwrite, chỉ append
- Batched write queue: 100ms window → group writes → single disk op
- Lite read optimization: read first 64KB + last 64KB của large files
- Metadata re-append at EOF: compact không mất metadata

**Áp dụng cho ViePilot v2**:
- HANDOFF.log: append-only JSONL (không phải JSON rewrite)
- Parallel tool calls: batch all context reads vào 1 turn
- Lite read pattern cho logs/ files khi debug

**Decisions**:
- [x] HANDOFF.log là append-only JSONL, không rewrite
- [x] Parallel context loading: tất cả reads = 1 batch instruction

---

### Topic 9: Context Building & Bridge IDE — Bài học còn lại

**Context**: `REPORT-context-building.md` + `REPORT-bridge-ide-integration.md`

**Context Building insights**:
- SYSTEM_PROMPT_DYNAMIC_BOUNDARY: tách static vs dynamic sections
- Static: inject once, cache-friendly
- Dynamic: rebuild mỗi turn từ relevant files only
- CLAUDE.md 4-level hierarchy: enterprise → user → project → local

**Bridge IDE insights**:
- Sleep detection: gap > 60s → detect → restore context intelligently
- HybridTransport: 100ms batch window cho events
- Control point state: `requires_action` flag → client poll

**Áp dụng cho ViePilot v2**:
- AI-GUIDE.md: static section (rules, never changes) vs dynamic section (project state)
- vp-resume sleep detection: gap duration → tier selection (quick/standard/full)
- Control point: HANDOFF.json `control_point.active` flag → vp-status detection

**Decisions**:
- [x] AI-GUIDE.md static/dynamic boundary
- [x] vp-resume 3-tier: <30min (quick) / 30min–4h (standard) / >4h (full restore)
- [x] Control point state protocol via HANDOFF.json flag

---

### Topic 10: HANDOFF.json v2 Schema Design

**Schema v2**:
```json
{
  "version": 2,
  "position": {
    "phase": "02",
    "task": "2.3",
    "sub_task": "2.3.2",
    "status": "executing"
  },
  "recovery": {
    "l1_attempts": 0,
    "l2_attempts": 1,
    "l3_attempts": 0,
    "recent_blocker": false
  },
  "context": {
    "active_stacks": ["auth-service", "db-migrations"],
    "last_decision": "Use JWT with 1h expiry"
  },
  "control_point": {
    "active": false,
    "reason": null,
    "ts": null
  },
  "meta": {
    "last_written": "2026-04-02T10:30:00Z",
    "last_archived": "phase-01"
  }
}
```

**HANDOFF.log schema**:
```jsonl
{"ts":"2026-04-02T10:30:00Z","event":"task_start","task":"2.3","phase":"02"}
{"ts":"2026-04-02T10:31:00Z","event":"l2_recovery","task":"2.3","attempt":1}
{"ts":"2026-04-02T10:33:00Z","event":"task_pass","task":"2.3","duration_s":142}
```

**Decisions**:
- [x] HANDOFF.json v2 schema với `position.sub_task`, `recovery.*`, `context.active_stacks`, `control_point.*`
- [x] HANDOFF.log: append-only JSONL per event; rotate on phase complete

---

### Topic 11: TASK.md v2 Schema Design

**Fields mới**:
```yaml
## Task Metadata
type: build           # build | fix | refactor | test | docs | infra
complexity: M         # S | M | L | XL
write_scope:
  - src/auth/
  - tests/auth/
recovery_budget: M    # S: L1x1,L2x1 | M: L1x1,L2x2 | L: L1x2,L2x2,L3x1 | XL: L1x2,L2x3,L3x1
can_parallel_with: [] # task IDs that can run concurrently (Post-MVP)
recovery_overrides:
  L3:
    block: false      # true = never allow scope reduction for this task
    reason: ""
```

**Recovery Budget Table**:
| Complexity | L1 | L2 | L3 |
|---|---|---|---|
| S | 1 | 1 | 0 |
| M | 1 | 2 | 0 |
| L | 2 | 2 | 1 |
| XL | 2 | 3 | 1 |

**Decisions**:
- [x] TASK.md v2 với tất cả fields trên
- [x] recovery_overrides.L3.block = true cho compliance-sensitive tasks (Gap B)

---

### Topic 12: vp-request NLP Intake Design

**Current problem**: Menu-first → user phải classify type trước khi describe. Friction ngay từ đầu.

**v2 approach**: Description-first → NLP infer → confirm.

**2-band confidence threshold** (Q6 answer):
```
≥ 85%: auto-classify, inline confirm (non-blocking)
        "Nhận diện: 🐛 Bug — [Enter để tiếp / 'n' để đổi]"

60–84%: propose + 1 targeted question
        "Có vẻ là Enhancement hoặc Tech Debt. Đây là bug đang xảy ra hay cải tiến?"

<  60%: top-3 options (not full 6-item menu)
```

**Context boost**: `HANDOFF.json.control_point.active == true` → +20% BUG probability.

**Horizon-aware routing**: Check ROADMAP.md Post-MVP + PROJECT-CONTEXT.md trước khi route → warn nếu request scope vượt current milestone.

**Gap C**: UI direction route detection — nếu description contains UI/UX keywords → suggest `/vp-brainstorm --ui` route.

**Decisions**:
- [x] vp-request v2: NLP intake với 2-band threshold
- [x] Horizon-aware routing
- [x] Gap C: UI direction detection
- [x] Control point context boost

---

### Topic 13: Gap Analysis — 4 Gaps từ Workflow Audit

**Gap A (CRITICAL)**: crystallize không auto-populate `write_scope`, `type`, `recovery_budget` trong TASK.md → user phải fill thủ công → thường để trống → vp-auto chạy không có scope contract.
- Fix: crystallize step 9 inferring từ task description + phase context

**Gap B (HIGH)**: TASK.md cần `recovery_overrides.L3.block` per-task → compliance/correctness-critical tasks cần block scope reduction.
- Fix: field trong TASK.md + enforcement trong vp-auto recovery logic

**Gap C (MEDIUM)**: vp-request không detect UI direction intent → miss route tới `/vp-brainstorm --ui`.
- Fix: keyword detection trong NLP intake

**Gap D (MEDIUM)**: Recovery events không được log → không có audit trail → khó debug "tại sao task này took 3 attempts".
- Fix: HANDOFF.log non-blocking event emit sau mỗi recovery attempt

**Decisions**:
- [x] Gap A → MVP: crystallize auto-populate
- [x] Gap B → MVP: recovery_overrides block mechanism
- [x] Gap C → MVP: UI direction route detection
- [x] Gap D → MVP: HANDOFF.log recovery events

---

### Topic 14: 35-Task Implementation Plan — 4 Phases

**Phase 1: Foundation — Templates & State Machine** (6 tasks)

| Task | Description | Complexity |
|------|-------------|------------|
| 1.1 | Refactor TRACKER.md template → index ≤30 dòng | S |
| 1.2 | Tạo logs/ templates: decisions.md, blockers.md, version-history.md | S |
| 1.3 | Refactor PHASE-STATE.md → execution_state YAML block + sub-task tracking | M |
| 1.4 | Refactor TASK.md → type, write_scope, recovery_budget, can_parallel_with, recovery_overrides | M |
| 1.5 | Refactor AI-GUIDE.md → static/dynamic boundary | S |
| 1.6 | Refactor HANDOFF.json schema v2 | M |

**Phase 2: Execution Engine — vp-auto Rewrite** (9 tasks)

| Task | Description | Complexity |
|------|-------------|------------|
| 2.1 | 3-layer silent recovery trong autonomous.md + Gap G pre-flight | L |
| 2.2 | Typed state transitions thay thế prose workflow | M |
| 2.3 | Continuous HANDOFF.json write + Gap D HANDOFF.log events | M |
| 2.4 | Parallel context loading | S |
| 2.5 | Sub-task tracking table update trong PHASE-STATE.md | S |
| 2.6 | 3-tier validation pipeline: contract → write_scope lock → git gate | M |
| 2.7 | Control point state protocol | S |
| 2.8 | Scope drift detection | M |
| 2.9 | Recovery budget enforcement | M |

**Phase 3: Workflow Integration — Skills & Commands** (8 tasks)

| Task | Description | Complexity |
|------|-------------|------------|
| 3.1 | HANDOFF.log implementation + gitignore + rotation hook | S |
| 3.2 | vp-resume tiered context restore | M |
| 3.3 | vp-status: control point detection + recovery stats | S |
| 3.4a | crystallize update: v2 artifacts + Gap A auto-populate + Gap G compliance detection | M |
| 3.4b | crystallize backward compat verification | S |
| 3.5 | vp-evolve: generate v2 TASK.md template | S |
| 3.6 | vp-request NLP intake rewrite + Gap C UI detection + horizon-aware routing | L |
| 3.7 | `paths:` frontmatter cho vp-* skills (Post-MVP execution) | S |

**Phase 4: Verification & Documentation** (12 tasks)

| Task | Description | Complexity |
|------|-------------|------------|
| 4.1a | Integration test: small project S/M | M |
| 4.1b | Integration test: large project L/XL | M |
| 4.1c | Integration test: context reset simulation | S |
| 4.1d | Integration test: compliance domain tasks | S |
| 4.2 | Update autonomous-mode.md | S |
| 4.3 | Update quick-start.md | S |
| 4.4 | Update advanced-usage.md | S |
| 4.5 | CHANGELOG.md v2 entry | S |
| 4.6a | Stale file reference audit: SKILL.md files | S |
| 4.6b | Stale file reference audit: workflow cross-references | S |
| 4.7 | Version bump: 1.x → 2.0.0 | S |

**Total: 35 tasks** across 4 phases.

---

### Topic 15: vp-resume Tiered Restore Design

**3 tiers**:
```
< 30 min gap (quick):
  Read HANDOFF.json position + PHASE-STATE.md current task
  → Resume immediately

30 min – 4h gap (standard):
  Read HANDOFF.json (full) + PHASE-STATE.md + current TASK.md
  → Restore context summary

> 4h gap (full restore):
  Read HANDOFF.json + PHASE-STATE.md + TASK.md + active_stacks context
  + tail of HANDOFF.log (last 20 events)
  → Full context reconstruction before resume
```

**Decisions**:
- [x] 3-tier based on gap duration
- [x] `active_stacks` from HANDOFF.json feeds full restore
- [x] HANDOFF.log tail read only on full tier

---

### Topic 16: Compliance Domain Detection — Gap G

**Problem**: Solo dev không có peer review. L3 scope reduction unsafe cho binary-correctness domains. User cần biết để set `recovery_overrides.L3.block = true` — nhưng thường không biết cần làm điều này.

**Solution**: crystallize auto-detect từ `write_scope` paths:

```yaml
compliance_domains:
  auth:     ["/auth/", "/authorization/", "jwt", "oauth", "session", "rbac"]
  payment:  ["/payment/", "/billing/", "stripe", "invoice", "subscription"]
  data:     ["/migrations/", "foreign_key", "constraint"]
  crypto:   ["/crypto/", "encrypt", "hash", "password", "bcrypt", "tls"]
```

**Auto-set** khi match:
```yaml
recovery_overrides:
  L3:
    block: true
    reason: "auth domain — scope reduction unsafe"
```

**2-layer implementation** (MVP):
- crystallize step 9: path scan → auto-set
- vp-auto pre-flight: log khi L3 blocked; warn nếu compliance path detected but no block set

**Post-MVP**: description keyword scan, consensus/contract domains, auto-suggestion UI.

**Decisions**:
- [x] Gap G → MVP (path-based, 4 domains)
- [x] crystallize sets L3.block + reason comment
- [x] vp-auto pre-flight log/warn
- [x] User can manually override (set block: false)

---

### Topic 17: Persona Simulation — Solo Dev × Project Type × Experience

**Simulation**: 6 thời điểm × 5 loại dự án = 30 intersections

**Project types**: UI Design → Complex UI System → Architecture → Software Design → Large Complex System

**Time spans**: Day 1 → Week 1 → Month 1 → Month 6 → Year 1 → Year 2

**Key findings**:

1. **Recovery Layer = #1 enabler cho L/XL projects**: Với v1, first L2 failure thường = project stall. v2 silent recovery = failures invisible đến user → momentum preserved.

2. **HANDOFF continuity = multi-day project viability**: Projects > 3 ngày với v1 có systematic context loss. v2 HANDOFF.json continuous write giải quyết hoàn toàn.

3. **`write_scope` compound safety**: Day 1: prevent AI drift → Month 1: compliance mechanism → Year 1: domain-aware customization. Giá trị tăng theo thời gian.

4. **TRACKER refactor = quality-of-life, không phải enabler**: Recovery layers và HANDOFF là true blockers cho project scale.

5. **v2 power scales with user expertise**: Day 1: v2 smooth vs v1 ok. Year 2: v2 indispensable vs v1 impossible cho complex projects.

6. **`recovery_overrides: {L3: block}` = compliance mechanism, không chỉ convenience**: Fintech auth + CRDT + DB migrations cần này.

7. **Expert flip**: Year 1 users flip từ "let AI recover silently" sang "require human review for critical paths". v2 supports both modes.

**New gaps discovered**:
- Gap E: `/vp-status --all` cross-project aggregation (multi-project consultants, Year 2 use case) → Future
- Gap F: Career documentation export từ `logs/decisions.md` across projects → Future

---

### Topic 18: Open Questions — All Resolved

| # | Question | Answer |
|---|---|---|
| Q1 | Typed transitions: template hay crystallize generate? | Both — template có placeholder, crystallize populate values |
| Q2 | Scope contract: skill prompt hay vp-tools? | Skill prompt (workflow instruction) — no CLI needed |
| Q3 | `paths:` frontmatter: Claude Code support? | Confirmed supported — Task 3.7 |
| Q4 | AutoDream timing? | Post-phase-complete (Post-MVP) |
| Q5 | v2 backward-compatible? | Non-breaking — v1 projects skip new fields gracefully |
| Q6 | NLP confidence threshold? | 2-band: ≥85% auto / 60–84% 1 question / <60% top-3 |
| Q7 | HANDOFF.log rotation? | Phase-boundary, archive to logs/handoff-phase-N.log, never commit |
| Q8 | Parallel loading mechanism? | Instruction language change in autonomous.md only; no new tools needed |

---

## Summary

### Key Decisions
1. ViePilot v2 giữ nguyên spirit: doc-first, structured, git-tracked, human-in-the-loop
2. Cải tiến execution layer: typed state machine, 3-layer silent recovery, continuous state writing
3. Cải tiến context efficiency: indexed TRACKER, selective loading, parallel batch reads, static/dynamic boundary
4. Sub-agent scope contract bắt buộc + write_scope enforcement để prevent drift
5. Compliance domain detection auto-block L3 recovery cho safety-critical tasks
6. vp-request NLP intake: description-first, 2-band confidence, horizon-aware routing
7. HANDOFF.json v2 + HANDOFF.log: full execution audit trail
8. Non-breaking: v1 projects skip new TASK.md fields gracefully

### Action Items — 35 Tasks

**Phase 1: Foundation** (Tasks 1.1–1.6)
- [ ] 1.1: Refactor TRACKER.md template → index ≤30 dòng + logs/ structure
- [ ] 1.2: Tạo logs/ templates: decisions.md, blockers.md, version-history.md
- [ ] 1.3: Refactor PHASE-STATE.md → execution_state YAML block + sub-task tracking
- [ ] 1.4: Refactor TASK.md → type, write_scope, recovery_budget, can_parallel_with, recovery_overrides
- [ ] 1.5: Refactor AI-GUIDE.md → static/dynamic boundary comment
- [ ] 1.6: Refactor HANDOFF.json schema v2

**Phase 2: Execution Engine** (Tasks 2.1–2.9)
- [ ] 2.1: 3-layer silent recovery trong autonomous.md + Gap G pre-flight compliance check
- [ ] 2.2: Typed state transitions thay thế prose workflow trong autonomous.md
- [ ] 2.3: Continuous HANDOFF.json write sau mỗi sub-task + Gap D HANDOFF.log events
- [ ] 2.4: Parallel context loading — batch read instruction rewrite
- [ ] 2.5: Sub-task tracking table update trong PHASE-STATE.md
- [ ] 2.6: 3-tier validation pipeline: contract → write_scope lock → git gate
- [ ] 2.7: Control point state protocol (HANDOFF.json flag + vp-status detection)
- [ ] 2.8: Scope drift detection post-task
- [ ] 2.9: Recovery budget enforcement (parse recovery_budget + recovery_overrides)

**Phase 3: Workflow Integration** (Tasks 3.1–3.7)
- [ ] 3.1: HANDOFF.log implementation + gitignore + phase-boundary rotation hook
- [ ] 3.2: vp-resume tiered context restore (3 tiers)
- [ ] 3.3: vp-status: control point detection + recovery stats display
- [ ] 3.4a: crystallize update — v2 artifacts + Gap A auto-populate + Gap G compliance detection
- [ ] 3.4b: crystallize backward compat verification
- [ ] 3.5: vp-evolve — generate v2 TASK.md template
- [ ] 3.6: vp-request NLP intake rewrite + Gap C UI detection + horizon-aware routing
- [ ] 3.7: `paths:` frontmatter cho vp-* skills

**Phase 4: Verification & Documentation** (Tasks 4.1–4.7)
- [ ] 4.1a: Integration test — small project S/M
- [ ] 4.1b: Integration test — large project L/XL
- [ ] 4.1c: Integration test — context reset simulation
- [ ] 4.1d: Integration test — compliance domain tasks
- [ ] 4.2: Update autonomous-mode.md (recovery layers, control point, scope contract)
- [ ] 4.3: Update quick-start.md (TASK.md new fields, TRACKER.md new structure)
- [ ] 4.4: Update advanced-usage.md (recovery_overrides customization, write_scope patterns)
- [ ] 4.5: CHANGELOG.md v2 entry
- [ ] 4.6a: Stale file reference audit — SKILL.md files
- [ ] 4.6b: Stale file reference audit — workflow cross-references
- [ ] 4.7: Version bump 1.x → 2.0.0

### Open Questions
All 8 questions resolved. See Topic 18.

### Next Steps
1. Dùng `/vp-crystallize` để biến session này thành phases & tasks
2. Phase 1 MVP: Refactor templates (TRACKER, PHASE-STATE, TASK, AI-GUIDE, HANDOFF)
3. Phase 2 MVP: vp-auto rewrite (recovery, typed states, continuous HANDOFF, validation)
4. Phase 3 MVP: Skills & workflow integration (vp-resume, vp-request, crystallize, evolve)
5. Phase 4 MVP: Verification + docs + version bump

---

## Project Meta Intake (FEAT-009)
- **status**: skipped
- **Meta intake waiver**: Đây là brainstorm cho ViePilot framework itself, không phải dự án end-user. Profile binding không áp dụng cho framework development session.

# Brainstorm Session - 2026-04-03

## Session Info
- **Date**: 2026-04-03
- **Participants**: User, Claude (claude-sonnet-4-6)
- **Status**: Completed
- **Context**: Post-MVP planning sau khi v2.0.1 released (39/39 tasks complete)

---

## Product Horizon

### Post-MVP (ship after v2.0.1)

**Brainstorm Artifact Manifest + Contract:**
- `brainstorm-manifest.json` schema + sinh tự động khi `/save` `(Post-MVP)`
- crystallize Step 0A: mandatory manifest consume (trước tất cả steps khác) + fallback compat `(Post-MVP)`
- `ui_task_context_hint` → TASK.md `context_required` auto-populate per page slug `(Post-MVP)`
- `vp:decision` anchor syntax (AI-generated, user-transparent, HTML comment) `(Post-MVP)`
- Re-consume: `last_updated > consumed_at` → trigger drift check `(Post-MVP)`
- Doc-drift detection: anchor matching + risk classification (LOW/MED/HIGH) `(Post-MVP)`
- Drift report UI: per-item action (auto-update / skip / flag) `(Post-MVP)`
- Gap G re-check tự động khi HIGH risk drift chọn auto-update `(Post-MVP)`
- Multi-session manifest merge (oldest→newest per artifact type) `(Post-MVP)`
- vp-auto stale-artifact warning khi task là UI type `(Post-MVP)`

**Cross-project & Execution:**
- Gap E: `/vp-status --all` + `~/.viepilot/project-registry.json` `(Post-MVP)`
- Token budget awareness — preemptive HANDOFF write khi > 70% context `(Post-MVP)`
- Gap G extended — keyword scan thêm vào path-only detection `(Post-MVP)`
- AutoDream → STACKS.md — phase complete consolidation hook `(Post-MVP)`
- Fork state updates — background state writes sau task PASS `(Post-MVP)`

### Future / Exploratory
- Parallel task execution — dependency graph + agent coordination `(Future)`
- Team memory sync — shared TRACKER, cần server infra decision `(Future)`
- ML-based auto-approval — cần historical data `(Future)`
- Multi-agent coordinator `(Future)`

### Deferred (cần Tier 1+2 stable trước)
- Parallel task execution: cần Fork State Updates proven trước
- Team memory sync: cần server/sync infra architecture decision

### Non-goals cho Post-MVP v2.x
- Breaking changes với v2.0.x projects
- Server deployment / network service (keep local-first)
- Rewrite vp-tools CLI

---

## Project Meta Intake (FEAT-009)
- **status**: skipped
- **Meta intake waiver**: Brainstorm cho ViePilot framework itself. Profile binding không áp dụng cho framework development session.

---

## Architecture Diagram Applicability Inputs
- **Project complexity**: complex (framework tooling)
- **Services/modules**: multiple (brainstorm, crystallize, vp-auto, vp-status, manifest)
- **Event-driven**: yes (phase complete hook → AutoDream trigger)
- **Deployment shape**: local-only
- **User flow complexity**: multi-step (brainstorm → manifest → crystallize → auto)
- **Integration surface**: low (local filesystem only)

---

## Topics Discussed

### Topic 1: User Profile & Post-MVP Prioritization Context

**Context**: User profile cho prioritization — xác định weight của từng epic.

**Profile xác định**:
- Đa dạng: framework/tooling, thiết kế project nhỏ → lớn dài hạn
- Solo + collaborate
- Hệ thống high-load: Protocol Server, Enterprise Gateway, IoT Monitoring Gateway, Banking, ERP
- Target: hàng chục triệu TPS liên tục
- 4+ projects active song song

**Pain points (theo thứ tự ưu tiên)**:
1. Pain 3 — Task chạy lâu tốn token, context cut-off
2. Pain 4 — Multi-project switching, mất thời gian re-orient
3. Pain 2 — AI viết ngoài scope (đặc biệt nguy hiểm với high-TPS systems)

**Scoring matrix (Impact × Effort inverse)**:

| Epic | Score |
|------|:-----:|
| Gap E: /vp-status --all | 20 |
| Token budget awareness | 15 |
| Gap G extended | 12 |
| AutoDream → STACKS.md | 12 |
| Fork state updates | 12 |
| Brainstorm Artifact Manifest | — (new, very high) |
| Parallel task execution | 8 |
| Team memory sync | 6 |

**Decisions**:
- [x] Giữ mọi thứ đơn giản, không overengineer
- [x] Brainstorm như collaborative surface là thế mạnh độc nhất của ViePilot
- [x] Ưu tiên epics fix pain point thực tế hàng ngày trước infrastructure phức tạp

---

### Topic 2: Brainstorm → Crystallize → vp-auto Artifact Chain Gaps

**Context**: Research + simulation toàn bộ chain trong v2.0.1.

**4 drop points được phát hiện qua code audit**:

```
brainstorm artifacts
    ↓  [Drop 1: implicit inference — có thể skip]
crystallize reads notes.md + HTML
    ↓  [Drop 2: lossy abstraction — rich detail → summary]
ARCHITECTURE.md (summary only)
    ↓  [Drop 3: context_required không có ui-direction paths]
TASK.md (thiếu UI direction context)
    ↓  [Drop 4: vp-auto zero UI direction awareness]
vp-auto implements từ ARCHITECTURE.md summary
    → Kết quả khác xa design intent
```

**Drop Point 1**: `crystallize.md` Step 1A — conditional `"if brainstorm indicates frontend/UI scope"` → AI inference, không guaranteed.

**Drop Point 2**: Crystallize đọc notes.md + HTML → extract vào ARCHITECTURE.md → detail bị lost (slide-in timing, component specifics, UX walkthrough findings).

**Drop Point 3**: `templates/phase/TASK.md` `Context Required`:
```yaml
files_to_read:
  - .viepilot/ARCHITECTURE.md#{{RELEVANT_SECTION}}
  - .viepilot/SYSTEM-RULES.md#coding_rules
  {{ADDITIONAL_CONTEXT}}   ← placeholder, không auto-populate ui-direction
```

**Drop Point 4**: `autonomous.md` context loading — zero mention của `.viepilot/ui-direction/`. vp-auto không bao giờ đọc UI direction artifacts.

**3 kịch bản simulated**:
- Kịch bản A: sidebar animation detail (150ms vs 200ms, active color) lost → vp-auto implement sai
- Kịch bản B: UX walkthrough findings (CTA below fold, text truncate) lost → vp-auto re-introduce bugs
- Kịch bản C: Multi-page detail (social login, error state layout) lost → implement generic

**Verdict**: Drop Point 3 + 4 chưa từng được address ở cả v1 lẫn v2. ENH-025 (v1.9.11) chỉ fix Drop Point 1 một phần.

**Decisions**:
- [x] Option A: Artifact Manifest là đúng hướng — file có references list mà crystallize bắt buộc đọc
- [x] Manifest ở `.viepilot/brainstorm-manifest.json` (guaranteed location, crystallize luôn check)
- [x] `consumed: false` → crystallize phải consume; nếu bỏ qua → validation error

---

### Topic 3: Brainstorm Artifact Manifest — Design

**Manifest schema** (`.viepilot/brainstorm-manifest.json`):

```json
{
  "version": 1,
  "updated_at": "ISO8601",
  "sessions": [
    { "id": "session-2026-04-03", "file": "docs/brainstorm/session-2026-04-03.md", "status": "completed" }
  ],
  "artifacts": [
    {
      "type": "ui_direction",
      "session": "session-2026-04-03",
      "layout": "multi-page",
      "root": ".viepilot/ui-direction/session-2026-04-03/",
      "files": {
        "notes": ".viepilot/ui-direction/session-2026-04-03/notes.md",
        "hub": ".viepilot/ui-direction/session-2026-04-03/index.html",
        "style": ".viepilot/ui-direction/session-2026-04-03/style.css",
        "pages": [{ "slug": "login", "file": "pages/login.html" }]
      },
      "has_ux_walkthrough": true,
      "required": true,
      "consumed": false,
      "consumed_at": null,
      "last_updated": "ISO8601"
    },
    {
      "type": "product_horizon",
      "session": "session-2026-04-03",
      "path": "docs/brainstorm/session-2026-04-03.md",
      "section": "## Product horizon",
      "required": true,
      "consumed": false
    }
  ],
  "ui_task_context_hint": {
    "pages": { "login": "pages/login.html" },
    "shared_notes": ".viepilot/ui-direction/session-2026-04-03/notes.md",
    "shared_style": ".viepilot/ui-direction/session-2026-04-03/style.css"
  }
}
```

**Fix 4 drop points**:
- Drop 1: crystallize Step 0A mandatory read manifest → không còn inference
- Drop 2: `ui_task_context_hint` giữ direct path refs, không chỉ summary
- Drop 3: crystallize auto-populate `context_required` từ `ui_task_context_hint` per page slug
- Drop 4: vp-auto đọc `context_required` (đã có UI paths) → tự nhiên consume UI direction

**Multi-session merge strategy**:
- `product_horizon`: MERGE (accumulate, không overwrite tier tags)
- `ui_direction`: REPLACE với latest session
- `research_notes`: APPEND
- `architecture_inputs`: LATEST wins

**Re-consume + doc-drift**:
- Track `consumed_at` per artifact
- `last_updated > consumed_at` → trigger re-consume + drift check
- Re-consume cũng là doc-drift audit — compare new brainstorm vs existing docs
- Output: drift report per item với risk level + action options

**Decisions**:
- [x] Brainstorm sinh manifest tự động khi `/save` hoặc `/end`
- [x] Crystallize Step 0A: mandatory read trước tất cả steps, fallback nếu không có manifest
- [x] Re-consume trigger doc-drift detection (không chỉ sync)
- [x] vp-crystallize auto-register project vào `~/.viepilot/project-registry.json`

---

### Topic 4: Anchor Syntax — Decision-level Drift Detection

**Granularity chọn**: Decision-level (fine-grained, per decision anchor).

**Design principle**: AI sinh anchor tự động, user không thấy / không cần viết.

**Anchor format** (HTML comment → invisible khi render):
```markdown
<!-- vp:decision id="sidebar-behavior" session="session-2026-04-10" ts="ISO8601" -->
Sidebar collapsible mobile, slide-in 200ms, breakpoint 768px, active state: blue-600
<!-- /vp:decision -->
```

**Sinh khi nào**: Mỗi bullet trong `**Decisions**:` block → một anchor. `/save` scan + backfill anchors còn thiếu.

**ARCHITECTURE.md consumed anchor**:
```markdown
<!-- vp:consumed decision="sidebar-behavior" from="session-2026-04-03" at="ISO8601" -->
**Behavior**: Collapsible on mobile (breakpoint 768px), slide-in 200ms animation.
<!-- /vp:consumed -->
```

**Drift check algorithm**:
```
for each vp:consumed in docs:
  find matching vp:decision in latest sessions
  normalize text → compare
  if changed → DRIFT with risk:
    LOW:  visual/copy (color, label, text)
    MED:  layout/flow (component swap, structure)
    HIGH: logic/data/auth (matches compliance_domains → Gap G re-check)
```

**Drift report format**: per-item action — (a) auto-update / (b) skip / (c) flag for review.

**Docs covered**:
- ✅ ARCHITECTURE.md
- ✅ ROADMAP.md (task descriptions)
- ✅ TASK.md (objective + context_required)
- ❌ SYSTEM-RULES.md (too noisy)
- ❌ PROJECT-META.md (stable)

**Decisions**:
- [x] Decision-level granularity với anchor syntax
- [x] Anchor AI-generated, user-transparent (HTML comment)
- [x] Risk classification: LOW/MED/HIGH tự động
- [x] HIGH risk drift → Gap G compliance re-check tự động

---

### Topic 5: Remaining Post-MVP — Quick Overview

**Gap E: /vp-status --all** (Complexity: S)
- `~/.viepilot/project-registry.json` — danh sách project paths
- vp-crystallize auto-register khi crystallize project mới
- vp-status đọc registry → TRACKER.md + HANDOFF.json từng project → aggregate display
- Status icons: ● in_progress / ⚠ control_point / ✓ complete / ○ not_started

**Token Budget Awareness** (Complexity: M)
- Check sau mỗi sub-task: estimated context usage
- > 70%: warn + offer pause, HANDOFF write
- > 90%: force pause + HANDOFF write
- HANDOFF.log event: `token_budget_warning` với `used_pct`
- Heuristic estimate từ file sizes + context reads (không cần exact count)

**Gap G Extended — Keyword Scan** (Complexity: S)
- Thêm task description + objective scan bên cạnh write_scope path scan
- Keywords: password, token, session, encrypt, stripe, payment, invoice, migration, schema, bcrypt, tls
- Match → suggest L3.block = true + compliance_preflight warn
- Không auto-set (chỉ suggest) — user confirm

**AutoDream → STACKS.md** (Complexity: M)
- Trigger: sau phase complete (hook trong autonomous.md)
- Đọc tasks/*.md từ phase → extract patterns, anti-patterns, decisions
- Append to `.viepilot/STACKS.md` (project-local)
- Update `~/.viepilot/stacks/{stack}/BEST-PRACTICES.md` nếu có finding cross-project
- New file: `workflows/autodream.md`

**Fork State Updates** (Complexity: S, Risk: medium)
- State writes sau task PASS → background Agent (run_in_background: true)
- Main loop tiếp tục task tiếp theo không đợi
- Fallback: nếu fork không confirm → sync write (không để state drift)
- Cần careful testing trước ship

**Parallel Task Execution** (Complexity: L — DEFER)
- Cần `can_parallel_with` graph từ TASK.md (đã có field)
- Spawn parallel agents cho independent task clusters
- State merge: mỗi agent write file tạm → merge sau
- Defer sau Fork State Updates stable

**Decisions**:
- [x] Tier 1 (ship first): Manifest + Gap E + Gap G Extended + Token Budget
- [x] Tier 2: AutoDream + Fork State
- [x] Tier 3 (defer): Parallel Execution + Team Memory Sync + ML Auto-approval

---

## Summary

### Key Decisions

1. **Brainstorm Artifact Manifest** là epic quan trọng nhất từ session này — fix root cause verified qua code audit
2. **4 drop points** trong brainstorm→crystallize→vp-auto chain được xác định và có design fix cụ thể
3. **Re-consume = doc-drift detection** — hai-trong-một, không cần separate audit cho brainstorm changes
4. **Decision anchor** AI-generated, user-transparent — không friction cho user
5. **Milestone v2.x structure**: Tier 1 (4 epics) → Tier 2 (2 epics) → Tier 3 (defer)
6. **Gap E** là daily-use feature quan trọng nhất trong list gốc — complexity S, value cao cho 4+ projects
7. **Giữ local-first** — không server, không network service trong Post-MVP
8. **Parallel execution** defer cho đến Fork State Updates proven

### Action Items

**Tier 1 — v2.x MVP candidates:**
- [ ] Brainstorm Artifact Manifest (P1-P6 core): manifest schema + brainstorm sinh + crystallize consume
- [ ] Gap E: /vp-status --all + project-registry.json
- [ ] Gap G Extended: keyword scan + compliance_preflight extension
- [ ] Token Budget Awareness: sub-task check + HANDOFF.log event

**Tier 2:**
- [ ] AutoDream → STACKS.md: phase complete hook + workflows/autodream.md
- [ ] Fork State Updates: background Agent pattern + fallback

**Tier 3 (defer):**
- [ ] Parallel task execution (after Tier 2)
- [ ] Team memory sync (needs server decision)
- [ ] ML auto-approval (needs historical data)

### Open Questions
- Artifact Manifest: cần thêm artifact type nào ngoài ui_direction, product_horizon, research_notes, architecture_inputs?
- Anchor backfill: sessions cũ (session-2026-04-02) có cần retroactive anchor không?
- project-registry.json: user có thể manually add/remove projects hay chỉ auto-register?

### Next Steps
1. `/vp-evolve --milestone` để crystallize session này thành v2.x milestone
2. Hoặc `/vp-request` để log từng epic thành requests trước khi evolve

---

## Session Continuation — 2026-04-03 (Part 2)

**Context**: Tiếp tục session sau khi BUG-005 + BUG-006 được fix và v2.0.2 released.
**Topics**: Architecture Mermaid diagrams + Crystallize fidelity gap + vp-auto install path bug

---

### Topic 6: Architecture Mermaid Diagrams — Diagram Profile System

**Reference project**: smart-track-platform (IoT/GPS fleet tracking — Java Spring Boot + Kafka + PostgreSQL/PostGIS + Redis + React SPA)

**Current state** (smart-track-platform):
- 6 shallow mermaid diagrams: `system-overview.mermaid`, `database-schema.mermaid`, `kafka-flow.mermaid`, `deployment.mermaid`, `ui-flow.mermaid`, `tap-processing.mermaid`
- Missing: ~9 critical diagrams (sequence flows, ER detail, state machines, service boundary, API routes)

**Problem**: crystallize tạo diagrams theo template flat, không tính đến stack combination của project (microservice + event-driven + SPA → rất khác với monolith + REST).

**Design Decisions**:
- **Q4 = C**: Master overview diagram + drill-down diagrams per domain/layer
- **Q5 = B**: Detailed applicability matrix (AI matches project stacks to required diagram types)
- **Q6 = A**: Auto-update trigger sau mỗi relevant phase complete

**Proposed Diagram Profile System**:

```
crystallize step: detect stacks → select diagram profile → gen applicability matrix
                                                          ↓
architecture/
  cross/
    system-overview.mermaid         (always)
    data-flow.mermaid               (if event-driven)
    service-boundaries.mermaid      (if microservices)
  backend/
    api-routes.mermaid              (if REST/GraphQL)
    kafka-topics.mermaid            (if Kafka)
    database-er.mermaid             (if SQL DB)
    state-machines/
      device.mermaid                (per domain entity with state)
      vehicle.mermaid
  frontend/
    component-hierarchy.mermaid     (if SPA)
    page-flow.mermaid               (if multi-page)
  sequences/
    auth-flow.mermaid               (if auth module)
    ingest-flow.mermaid             (if event ingest)
    command-flow.mermaid            (if command/response pattern)
```

**Stack detection matrix** (crystallize reads STACKS.md / brainstorm stack section):
- Microservices → add: service-boundaries, api-routes per service
- Kafka/message queue → add: kafka-topics, data-flow
- SQL DB → add: database-er (detailed per domain)
- SPA frontend → add: component-hierarchy, page-flow
- Auth required → add: sequences/auth-flow
- State-heavy domain entities → add: state-machines/{entity}

**Auto-update rule**: After phase complete, autonomous.md checks: "does this phase touch architecture/{subdir}?" → if yes, flag relevant diagrams as stale → vp-auto updates them at end of phase.

**Decisions**:
- [x] Diagram profile system: stack-detection → applicability matrix
- [x] Folder structure: `architecture/cross/`, `architecture/backend/`, `architecture/frontend/`, `architecture/sequences/`, `architecture/state-machines/`
- [x] crystallize emits diagram list + stale flag trong PHASE-STATE.md (Files Changed tracking)
- [x] vp-auto updates stale diagrams at phase complete (not per task)
- [x] Project-specific diagrams (not framework-level) — smart-track là reference only

---

### Topic 7: Crystallize Fidelity Gap — Domain Entity Extraction

**Observed gap** (smart-track-platform reference):
- Brainstorm (2026-03-30) nêu rõ: tenants, users, vehicles, devices, vehicle_type, protocol_nodes, drivers, fleets
- ROADMAP phases 01-13: có Phase 03 (DB migrations), nhưng **không có** dedicated phase cho management service APIs
- Phase 04 task 4.3: "Enrichment: resolve tenant/device" → calls service layer that doesn't exist
- Crystallize over-indexed on streaming/event-driven architecture (visually prominent in brainstorm) → skipped implicit CRUD management layer

**Root cause**: crystallize không có explicit "domain entity extraction" step:
1. Không extract domain entities từ brainstorm explicitly
2. Không map entities → required service phases (management API)
3. Không cross-check: "nếu task X cần resolve {entity}, phải có service phase trước"

**Pattern**: Crystallize reads brainstorm → extracts "technically interesting" signals (streaming, async) first → generates phases for those → CRUD management APIs bị skip vì "implied by DB migrations"

**Fix Scope (Q9 = A + C)**:
- **Fix A — Entity checklist**: crystallize.md phải có explicit step "extract domain entities from brainstorm" → list entities với required CRUD endpoints → check if management service phase exists in ROADMAP
- **Fix C — Dependency validation**: sau khi gen phases, cross-check: mọi task description có "resolve/create/update/manage {entity}" → corresponding service phase phải có (warning nếu thiếu)

**Decisions**:
- [x] ENH-022: Add domain entity extraction step vào crystallize.md (Fix A + Fix C)
- [x] Entity extraction là step tường minh, không inference — AI phải list rõ entities + required phases
- [x] Dependency validation output: warning list "Task X.Y calls {entity} but no {entity}Service phase found" → user confirm or auto-add phase stub
- [x] smart-track is reference only — không fix smart-track phases trong session này

---

### Topic 8: vp-auto Install Path Confusion Bug

**Observed**: vp-auto/AI trong session BUG-005/BUG-006 fix đã cố edit `~/.claude/viepilot/` (install path) thay vì `/Users/sonicq12/DEV_PROJECTS/viepilot/` (codebase source).

**Root cause analysis**:
- vp-auto reads workflow từ `~/.claude/viepilot/workflows/autonomous.md` (install path)
- AI infers "edit target = where I read from" → confuses install path với project codebase
- `autonomous.md` + `AI-GUIDE.md` không có explicit "working directory guard"
- Không có assertion: "source of truth = `{project_cwd}`, NOT install path"

**Impact**: Framework development sessions dễ bị edit nhầm production, bypass all testing. Detected vì user corrected manually.

**Fix (BUG-007)**:
- `workflows/autonomous.md`: thêm "Working Directory Guard" block ở Initialize section — AI phải verify `{project_cwd}` trước khi bất kỳ file edit nào. Install path edit = hard stop → control point
- `templates/project/AI-GUIDE.md`: thêm explicit note "NEVER edit `~/.claude/viepilot/` or `~/.cursor/viepilot/` — those are install paths, NOT source"
- Detection heuristic: nếu task writes to `~/.{something}/viepilot/` → trigger warning

**Decisions**:
- [x] BUG-007: Working directory guard trong autonomous.md + AI-GUIDE.md template
- [x] Guard phải explicit, không chỉ comment — phải có detection condition + hard stop
- [x] Applies toàn bộ ViePilot framework projects (không chỉ viepilot self-development)

---

## Updated Summary

### New Decisions (Part 2)

9. **Diagram Profile System** — stack-detection → applicability matrix → folder structure; vp-auto updates stale diagrams at phase complete
10. **ENH-022 Crystallize Fidelity** — domain entity extraction step (Fix A) + dependency validation (Fix C); entity extraction tường minh không inference
11. **BUG-007 Working Directory Guard** — autonomous.md + AI-GUIDE.md; install path edit = hard stop, không chỉ warning

### New Action Items

**Immediate requests to create:**
- [ ] ENH-022: Crystallize domain entity extraction + dependency validation
- [ ] BUG-007: vp-auto working directory guard (install path confusion)

**Part 1 Tier 1 items still pending:**
- [ ] Brainstorm Artifact Manifest (ENH Tier 1)
- [ ] Gap E: /vp-status --all
- [ ] Gap G Extended
- [ ] Token Budget Awareness
- [ ] Diagram Profile System (new from Part 2)

### Open Questions (updated)
- ~~Artifact Manifest: cần thêm artifact type nào ngoài ui_direction, product_horizon, research_notes, architecture_inputs?~~ → **RESOLVED** (xem Part 4)
- Anchor backfill: sessions cũ có cần retroactive anchor không?
- project-registry.json: user có thể manually add/remove hay chỉ auto-register?
- Diagram Profile System: có cần separate workflow file (`workflows/diagram-sync.md`) hay embed trong crystallize + autonomous?
- BUG-007: guard nên apply cho ALL projects hay chỉ khi project = framework dev?

### Next Steps
1. `/vp-request` để tạo ENH-022 + BUG-007
2. `/vp-evolve` để plan phases cho v2.x milestone
3. `/vp-auto` để implement

---

## Session Continuation — 2026-04-03 (Part 3)

**Context**: Tiếp tục sau vp-evolve v2.1 scaffold + vp-audit. Phase 08 đang chạy (task 8.2 done).
**Topics**: Plan Mode review gate cho crystallize + Remove MVP concept + Interactive Q&A UI (BMAD-inspired)

---

### Topic 9: Plan Mode Review Gate — crystallize Extraction Visibility

**Problem confirmed từ crystallize.md analysis**:
- Steps 1 → 1A → 1B → 1C: read brainstorm → extract entities → research stack — chạy **hoàn toàn opaque**
- Step 1 `horizon validation gate` có chạy MVP/Post-MVP classification nhưng user không thấy
- Không có pause point giữa extraction và generation → user chỉ thấy output cuối
- Root cause của ENH-022 + crystallize fidelity gap

**Design decisions**:

**Q1 = C — Review scope = Full**:
crystallize Phase 1 (extraction) hiển thị tất cả:
- Entity manifest: name, type, needs_crud_api, service_phase (OK/MISSING/N/A)
- Horizon classification: feature → v1.0/v1.1/backlog/deferred decision
- Phase sequence draft: "Tôi sẽ tạo N phases theo thứ tự này..."
- Tech stack summary: stacks detected + research quality (cached vs fresh)
- Key decisions extracted: top 5-10 decision bullets từ brainstorm với decision ID
- UI direction consumed: pages list (nếu có) + components identified

**Q2 = C — Approval granularity = per-section independent**:
```
crystallize Review Gate:
  ── Entity Manifest (8 entities found) ──────────── [Approve] [Edit] [Skip]
  ── Horizon Classification (12 features) ─────────── [Approve] [Edit] [Skip]
  ── Phase Sequence (16 phases planned) ───────────── [Approve] [Edit] [Skip]
  ── Tech Stack Summary ───────────────────────────── [Approve] [Edit] [Skip]
  ── Key Decisions (7 extracted) ──────────────────── [Approve] [Edit] [Skip]
  ── UI Direction (3 pages consumed) ──────────────── [Approve] [Skip]
  ─────────────────────────────────────────────
  → All approved → proceed to generation
  → Any section "Edit" → user corrects → re-show that section
  → Any section "Skip" → proceed with defaults
```

**Q3 = B — Trigger = default on, `--no-review` to skip**:
- `vp-crystallize` → review gate on by default
- `vp-crystallize --no-review` → skip all review sections, generate directly
- Fast path for experienced users / re-runs

**Implementation path**:
- crystallize.md: add `<step name="review_gate">` between Step 1C (stack cache) and Step 2 (generate AI-GUIDE)
- Per-section approval: AI presents section → calls `node bin/vp-tools.cjs ask` (TUI) → wait for approval → continue
- "Edit" on entity manifest → user types corrections → AI re-runs entity extraction with corrections
- Connects directly to ENH-022 (entity extraction) and Phase 09 (Artifact Manifest)

**Decisions**:
- [x] crystallize splits into Extraction Phase + Generation Phase with review gate
- [x] Review gate default on, skippable with `--no-review`
- [x] Review scope = full (all 6 sections)
- [x] Per-section approval (C) — not all-or-nothing
- [x] "Edit" option per section triggers targeted re-extraction
- [x] Plan Mode (Claude Code `plan` permission) used for extraction phase: reads only, no file writes until user approves

---

### Topic 10: Remove MVP Concept — Project-arc ROADMAP

**Problem articulated**:
1. crystallize generates only MVP phases (8-10) from 20+ feature brainstorm
2. Post-MVP restart: `/vp-evolve --milestone` → archive → new ROADMAP → **context loss**
3. Autonomous framework doesn't need artificial stopping points
4. With Artifact Manifest (Phase 09), brainstorm decisions persist → no reason to re-read just for Post-MVP

**Design decisions**:

**Q4 = A — All phases upfront with full task details**:
crystallize generates the ENTIRE project arc from brainstorm:
- Phase 01: Foundation (P1, v1.0)
- Phase 02: Core entities (P1, v1.0)
- Phase 03: Core APIs (P1, v1.0)
- ...
- Phase N: Final polish (P3, v1.2)
All phases have full task files. No deferred "Post-MVP stub" — full planning from day one.

**Q5 = B+C — AutoDream + collapsible summary**:
- Done phases: AutoDream extracts decisions + patterns → STACKS.md
- ROADMAP entry stays visible but shows `[▼ collapsed]` summary after AutoDream runs
- Summary line: `Phase 03 ✅ — Core Management APIs (2026-04-01) → 3 decisions in STACKS.md`
- User can expand to see full phase details anytime
- No archiving, no context loss

**Q6 = C — Hybrid: `priority` + `release_target` per phase**:

```yaml
# In PHASE-STATE.md + ROADMAP phase header
priority: P1              # P1 = ship first | P2 = after P1 stable | P3 = enhancement
release_target: v1.0      # v1.0 | v1.1 | v1.2 | backlog
```

crystallize populates both from brainstorm horizon analysis. vp-auto can filter:
```
vp-auto --filter "priority=P1"        # run P1 phases only
vp-auto --filter "release_target=v1.0"  # run v1.0 scope
```

**Renamed concepts**:
| Old | New |
|-----|-----|
| MVP | `priority: P1` + `release_target: v1.0` |
| Post-MVP | `priority: P2` + `release_target: v1.1` |
| Future | `priority: P3` + `release_target: backlog` |
| Archive milestone | Never (collapsible summary only) |
| `vp-evolve --milestone` | Deprecated → use `vp-evolve --feature` to add phases |

**Impact on existing workflows**:
- `vp-evolve --milestone`: deprecated (remove or redirect to `--feature`)
- `crystallize.md Step 7`: generate full-arc ROADMAP (không chỉ MVP phases)
- `ROADMAP.md` template: thêm `priority` + `release_target` columns vào phase table
- `TRACKER.md`: remove "Milestone" concept → "Project" + "Current Phase"
- `TASK.md`: add `release_target` field to Task Metadata block
- `autonomous.md`: add `--filter` flag support for phase selection

**Connects to**:
- Topic 9 (review gate): user can correct horizon classification → phase `release_target` in review step
- Phase 09 (Artifact Manifest): brainstorm decisions always readable → no need to re-read on "new milestone"
- ENH-022 (entity extraction): core entities get P1 phases → not buried in Post-MVP

**Decisions**:
- [x] Remove MVP/Post-MVP/Future labels from ViePilot — replace with `priority` + `release_target`
- [x] crystallize generates full project arc (all phases + all tasks) from brainstorm
- [x] No archiving — ROADMAP grows linearly, done phases get collapsible summary
- [x] `vp-evolve --milestone` deprecated; `--feature` is the only mode
- [x] TASK.md + ROADMAP.md + PHASE-STATE.md get `priority` + `release_target` fields
- [x] `vp-auto --filter` for selective phase execution

---

### Topic 11: Interactive Q&A UI — BMAD-Inspired

**BMAD research findings**:
- BMAD's arrow-key UI is their **CLI installer** (React/Ink TUI, `npx bmad-method install`)
- Not the AI interaction layer — BMAD brainstorm Q&A is still text-based in chat
- Key library: `@clack/prompts` or `enquirer` (npm, ~50KB, no React/Ink needed)
- Arrow key navigation + multi-select via SPACE + ENTER confirm

**Proposed: `vp-tools ask` command**

```bash
node bin/vp-tools.cjs ask \
  --question "Review scope for crystallize?" \
  --single \
  "A|Minimal: entities + warnings only" \
  "B|Standard: entities + horizon + phase draft" \
  "C|Full: everything (entities + horizon + tech stack + decisions + UI direction)"

# Returns: "C"
```

```bash
node bin/vp-tools.cjs ask \
  --question "Which ENH requests to prioritize?" \
  --multi \
  "023|Hooks system (PostToolUse state sync)" \
  "024|run_in_background Fork State Updates" \
  "025|Worktree isolation for L/XL tasks" \
  "026|Plan Mode doc-first gate"

# Returns: "023,024" (comma-separated selections)
```

**Integration in brainstorm.md**:
```
For each Q&A block:
  1. `node bin/vp-tools.cjs ask --question "..." [--single|--multi] options...`
  2. TUI renders in terminal: arrow keys navigate, SPACE toggle (multi), ENTER confirm
  3. Answer returned as string → AI continues with answer
  4. Session saved with: "Q: [question] → A: [selected options]"
```

**Benefits**:
- User không cần nhớ "Q1. C" format
- Multi-select answers không cần gõ "B-C" hay "1,2,3"
- UX consistent với modern CLI tools (Vercel, Astro, create-next-app...)
- `vp-tools ask` = 1 new command in `bin/vp-tools.cjs` (~50 lines with `@clack/prompts`)

**Decisions**:
- [x] ENH-027: `vp-tools ask` command với `@clack/prompts` — arrow-key TUI cho brainstorm Q&A
- [x] `--single` mode: radio-button style, ENTER to select
- [x] `--multi` mode: checkbox style, SPACE to toggle, ENTER to confirm
- [x] brainstorm.md + crystallize.md (review gate) gọi `vp-tools ask` thay cho plain text questions
- [x] Fallback: nếu `vp-tools ask` unavailable → plain numbered list (backward compat)

---

## Updated Summary (Part 3)

### New Key Decisions

12. **crystallize Review Gate**: extraction phase visible to user, per-section approval, default on (`--no-review` to skip), Plan Mode for reads-only pass
13. **No MVP concept**: replace với `priority` (P1/P2/P3) + `release_target` (v1.0/v1.1/backlog) per phase; crystallize generates full arc; no archiving
14. **`vp-tools ask`**: arrow-key TUI Q&A command (`@clack/prompts`); replaces plain text Q&A in brainstorm + crystallize review gate

### New Action Items

- [ ] ENH-027: `vp-tools ask` — interactive TUI Q&A command (`@clack/prompts`)
- [ ] ENH-028: crystallize Review Gate — extraction phase + per-section approval + Plan Mode
- [ ] ENH-029: Remove MVP concept — `priority` + `release_target` fields; full-arc ROADMAP; `vp-evolve --milestone` deprecated

### Product Horizon (updated)

**Promote to Tier 1 (v2.1 or v2.2):**
- crystallize Review Gate (ENH-028) — HIGH value, fixes crystallize fidelity at UX level
- `vp-tools ask` (ENH-027) — S complexity, high daily UX value

**Tier 2:**
- Remove MVP / full-arc ROADMAP (ENH-029) — M complexity, requires ROADMAP + template changes; depends on Artifact Manifest (Phase 09) for context preservation to work well

**Connections**:
- ENH-028 (review gate) requires ENH-027 (vp-tools ask) for approval UX
- ENH-029 (no MVP) works best with Phase 09 (Artifact Manifest) — decisions always readable
- ENH-029 also makes ENH-022 (entity extraction) complete solution: entities get proper P1 phases in full arc

### Open Questions
- ~~`vp-tools ask`: dùng `@clack/prompts` hay `enquirer` hay `prompts`?~~ → **RESOLVED**: `@clack/prompts` (smallest, actively maintained, Astro/SvelteKit standard)
- ~~ENH-029: backward compat với existing ROADMAP.md format?~~ → **RESOLVED** (xem Part 4)
- ~~Full-arc ROADMAP: cap ở bao nhiêu phases trước khi pagination/grouping cần thiết?~~ → **RESOLVED** (xem Part 4)
- ~~BUG-007: guard nên apply cho ALL projects hay chỉ khi project = framework dev?~~ → **RESOLVED** (xem Part 4)
- ENH-029: backward compat với existing ROADMAP.md format? (need migration guidance)
- crystallize Review Gate: nếu user edits entity manifest, how does AI re-run extraction? (re-read brainstorm vs manual correction)
- Full-arc ROADMAP: cap ở bao nhiêu phases trước khi pagination/grouping cần thiết? (20? 30? 50?)

### Next Steps
1. `/vp-request` để log ENH-027, ENH-028, ENH-029
2. `/vp-auto` để tiếp tục Phase 08 (task 8.2 done, 8.3 + 8.4 còn lại)
3. After Phase 08: Phase 09 (Artifact Manifest) → nền tảng cho ENH-029

---

## Session Continuation — 2026-04-03 (Part 4)

**Context**: Resolve open questions từ Part 1–3.

---

### Topic 12: Artifact Manifest — Additional Artifact Types (Resolved)

**Open question**: Cần thêm artifact type nào ngoài `ui_direction`, `product_horizon`, `research_notes`, `architecture_inputs`?

**Analysis**: Trace 4 drop points + multi-service cluster use case.

**Decisions**:
- [x] **`domain_entities`** → **THÊM**, `required: true`
  - Schema: `entities[]` với `name`, `type`, `needs_crud_api`, `service_phase` (OK/MISSING/N/A)
  - Kết nối ENH-022 (entity extraction) + ENH-028 (review gate section "Entity Manifest")
  - AI không thể tự suy luận đúng mà không có explicit data → required
- [x] **`tech_stack`** → **THÊM**, `required: true`
  - Schema: `stacks` map per layer (backend, database, frontend, mobile, protocol, infra)
  - Ảnh hưởng trực tiếp Diagram Profile System → detect sai stack = diagram profile sai
  - `required: true` — crystallize phải consume để chọn đúng diagram profile; AI inference không đủ tin cậy
- [x] **`compliance_domains`** → **THÊM**, `required: false`
  - Schema: `per_service[]` với `service`, `risk` (HIGH/MED/LOW), `keywords[]`
  - `required: false` — chỉ có khi brainstorm explicit phân loại; AI tự judge
  - Use case: cluster project (Microservices + Web + Mobile + Protocols) — per-service compliance thay vì project-wide keyword scan
  - vp-auto đọc artifact → apply compliance_preflight đúng task thuộc service tương ứng

### Topic 13: `vp-tools ask` Library Selection (Resolved)

**Decision**: `@clack/prompts`
- ~15KB, actively maintained (Astro/SvelteKit core)
- `select()` + `multiselect()` đủ cho `--single` + `--multi` modes
- TypeScript-first, clean Ctrl+C handling
- Rejected: `enquirer` (250KB, over-engineered), `prompts` (slow maintenance)

---

### Topic 16: BUG-007 Guard Scope (Resolved)

**Decision**: Apply **ALL projects** (không chỉ framework dev)

**Two-layer approach**:
- `autonomous.md` (ALL projects): "Never write to `~/.claude/viepilot/` or `~/.cursor/viepilot/`" → hard stop
- `AI-GUIDE.md` template (framework dev only): explicit "editing source at `{project_cwd}`, NOT install path" context note

**Rationale**: Guard trong `autonomous.md` costs zero, universal safety net. Non-framework projects không có reason để reference install paths — nhưng nếu có path confusion trong bất kỳ context nào → protection có sẵn.

**Decisions**:
- [x] `autonomous.md` guard: ALL projects — hard stop nếu write to `~/.*/viepilot/`
- [x] `AI-GUIDE.md` template: framework-dev-specific source vs install distinction
- [x] ENH-029 + BUG-007 implementation scope updated accordingly

---

### Topic 15: Full-arc ROADMAP — Phase Cap (Resolved)

**Decision**: Không đặt hard cap.

**Rationale**:
- Done phases collapse to 1 line sau AutoDream → file tự compact theo tiến độ
- `release_target` (v1.0/v1.1/backlog) đã là natural grouping
- vp-auto chỉ cần đọc current phase + next, không cần toàn ROADMAP

**Soft threshold**: **15 active/pending phases** chưa collapse
- vp-auto warn nhẹ: "X phases pending — consider running AutoDream on completed phases to compact ROADMAP"
- Không hard stop, không pagination — chỉ suggest

**Decisions**:
- [x] No hard phase cap for full-arc ROADMAP
- [x] Soft threshold: 15 active/pending phases → vp-auto suggest AutoDream compact
- [x] Natural grouping via `release_target` + collapsible done phases đủ cho human readability

---

### Topic 14: ENH-029 Backward Compat — Migration Strategy (Resolved)

**Problem**: Existing projects có ROADMAP.md / PHASE-STATE.md / TASK.md không có `priority` + `release_target` fields.

**Decision**: **Option C — Auto-migrate on first touch**
- vp-auto reads phase → detects missing `priority`/`release_target` → silently adds defaults
- Defaults: `priority: P1`, `release_target: v1.0`
- Zero friction — user không cần làm gì
- Triggered by: vp-auto phase read, crystallize re-run, vp-evolve --feature

**`vp-evolve --milestone` deprecation behavior**: Silent redirect
- `vp-evolve --milestone` → tự chạy `--feature` + print notice: `[ViePilot] --milestone deprecated, running --feature instead`
- Không hard error, không block workflow

**Decisions**:
- [x] Auto-migrate on first touch (Option C) — silent, zero friction
- [x] `vp-evolve --milestone` → silent redirect to `--feature` with notice

---

**Design principle xác nhận**:
> ViePilot là Vibe coding framework — default trust AI + workflow. `required: true` chỉ cho những gì AI không thể tự suy luận đúng mà không có explicit data. Không over-control.

**Final manifest artifact types (v2.x):**
1. `ui_direction` — required: true (khi có UI)
2. `product_horizon` — required: true
3. `research_notes` — required: false
4. `architecture_inputs` — required: false
5. `domain_entities` — required: true ← **NEW**
6. `compliance_domains` — required: false ← **NEW**
7. `tech_stack` — required: true ← **NEW**

---

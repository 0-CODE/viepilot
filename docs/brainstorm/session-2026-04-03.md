# Brainstorm Session - 2026-04-03

## Session Info
- **Date**: 2026-04-03
- **Participants**: User, Claude (claude-sonnet-4-6)
- **Status**: Completed
- **Context**: Post-MVP planning sau khi v2.0.1 released (39/39 tasks complete)

---

## Product Horizon

### Post-MVP (ship after v2.0.1)

**Brainstorm Artifact Manifest + Contract:**
- `brainstorm-manifest.json` schema + sinh tự động khi `/save` `(Post-MVP)`
- crystallize Step 0A: mandatory manifest consume (trước tất cả steps khác) + fallback compat `(Post-MVP)`
- `ui_task_context_hint` → TASK.md `context_required` auto-populate per page slug `(Post-MVP)`
- `vp:decision` anchor syntax (AI-generated, user-transparent, HTML comment) `(Post-MVP)`
- Re-consume: `last_updated > consumed_at` → trigger drift check `(Post-MVP)`
- Doc-drift detection: anchor matching + risk classification (LOW/MED/HIGH) `(Post-MVP)`
- Drift report UI: per-item action (auto-update / skip / flag) `(Post-MVP)`
- Gap G re-check tự động khi HIGH risk drift chọn auto-update `(Post-MVP)`
- Multi-session manifest merge (oldest→newest per artifact type) `(Post-MVP)`
- vp-auto stale-artifact warning khi task là UI type `(Post-MVP)`

**Cross-project & Execution:**
- Gap E: `/vp-status --all` + `~/.viepilot/project-registry.json` `(Post-MVP)`
- Token budget awareness — preemptive HANDOFF write khi > 70% context `(Post-MVP)`
- Gap G extended — keyword scan thêm vào path-only detection `(Post-MVP)`
- AutoDream → STACKS.md — phase complete consolidation hook `(Post-MVP)`
- Fork state updates — background state writes sau task PASS `(Post-MVP)`

### Future / Exploratory
- Parallel task execution — dependency graph + agent coordination `(Future)`
- Team memory sync — shared TRACKER, cần server infra decision `(Future)`
- ML-based auto-approval — cần historical data `(Future)`
- Multi-agent coordinator `(Future)`

### Deferred (cần Tier 1+2 stable trước)
- Parallel task execution: cần Fork State Updates proven trước
- Team memory sync: cần server/sync infra architecture decision

### Non-goals cho Post-MVP v2.x
- Breaking changes với v2.0.x projects
- Server deployment / network service (keep local-first)
- Rewrite vp-tools CLI

---

## Project Meta Intake (FEAT-009)
- **status**: skipped
- **Meta intake waiver**: Brainstorm cho ViePilot framework itself. Profile binding không áp dụng cho framework development session.

---

## Architecture Diagram Applicability Inputs
- **Project complexity**: complex (framework tooling)
- **Services/modules**: multiple (brainstorm, crystallize, vp-auto, vp-status, manifest)
- **Event-driven**: yes (phase complete hook → AutoDream trigger)
- **Deployment shape**: local-only
- **User flow complexity**: multi-step (brainstorm → manifest → crystallize → auto)
- **Integration surface**: low (local filesystem only)

---

## Topics Discussed

### Topic 1: User Profile & Post-MVP Prioritization Context

**Context**: User profile cho prioritization — xác định weight của từng epic.

**Profile xác định**:
- Đa dạng: framework/tooling, thiết kế project nhỏ → lớn dài hạn
- Solo + collaborate
- Hệ thống high-load: Protocol Server, Enterprise Gateway, IoT Monitoring Gateway, Banking, ERP
- Target: hàng chục triệu TPS liên tục
- 4+ projects active song song

**Pain points (theo thứ tự ưu tiên)**:
1. Pain 3 — Task chạy lâu tốn token, context cut-off
2. Pain 4 — Multi-project switching, mất thời gian re-orient
3. Pain 2 — AI viết ngoài scope (đặc biệt nguy hiểm với high-TPS systems)

**Scoring matrix (Impact × Effort inverse)**:

| Epic | Score |
|------|:-----:|
| Gap E: /vp-status --all | 20 |
| Token budget awareness | 15 |
| Gap G extended | 12 |
| AutoDream → STACKS.md | 12 |
| Fork state updates | 12 |
| Brainstorm Artifact Manifest | — (new, very high) |
| Parallel task execution | 8 |
| Team memory sync | 6 |

**Decisions**:
- [x] Giữ mọi thứ đơn giản, không overengineer
- [x] Brainstorm như collaborative surface là thế mạnh độc nhất của ViePilot
- [x] Ưu tiên epics fix pain point thực tế hàng ngày trước infrastructure phức tạp

---

### Topic 2: Brainstorm → Crystallize → vp-auto Artifact Chain Gaps

**Context**: Research + simulation toàn bộ chain trong v2.0.1.

**4 drop points được phát hiện qua code audit**:

```
brainstorm artifacts
    ↓  [Drop 1: implicit inference — có thể skip]
crystallize reads notes.md + HTML
    ↓  [Drop 2: lossy abstraction — rich detail → summary]
ARCHITECTURE.md (summary only)
    ↓  [Drop 3: context_required không có ui-direction paths]
TASK.md (thiếu UI direction context)
    ↓  [Drop 4: vp-auto zero UI direction awareness]
vp-auto implements từ ARCHITECTURE.md summary
    → Kết quả khác xa design intent
```

**Drop Point 1**: `crystallize.md` Step 1A — conditional `"if brainstorm indicates frontend/UI scope"` → AI inference, không guaranteed.

**Drop Point 2**: Crystallize đọc notes.md + HTML → extract vào ARCHITECTURE.md → detail bị lost (slide-in timing, component specifics, UX walkthrough findings).

**Drop Point 3**: `templates/phase/TASK.md` `Context Required`:
```yaml
files_to_read:
  - .viepilot/ARCHITECTURE.md#{{RELEVANT_SECTION}}
  - .viepilot/SYSTEM-RULES.md#coding_rules
  {{ADDITIONAL_CONTEXT}}   ← placeholder, không auto-populate ui-direction
```

**Drop Point 4**: `autonomous.md` context loading — zero mention của `.viepilot/ui-direction/`. vp-auto không bao giờ đọc UI direction artifacts.

**3 kịch bản simulated**:
- Kịch bản A: sidebar animation detail (150ms vs 200ms, active color) lost → vp-auto implement sai
- Kịch bản B: UX walkthrough findings (CTA below fold, text truncate) lost → vp-auto re-introduce bugs
- Kịch bản C: Multi-page detail (social login, error state layout) lost → implement generic

**Verdict**: Drop Point 3 + 4 chưa từng được address ở cả v1 lẫn v2. ENH-025 (v1.9.11) chỉ fix Drop Point 1 một phần.

**Decisions**:
- [x] Option A: Artifact Manifest là đúng hướng — file có references list mà crystallize bắt buộc đọc
- [x] Manifest ở `.viepilot/brainstorm-manifest.json` (guaranteed location, crystallize luôn check)
- [x] `consumed: false` → crystallize phải consume; nếu bỏ qua → validation error

---

### Topic 3: Brainstorm Artifact Manifest — Design

**Manifest schema** (`.viepilot/brainstorm-manifest.json`):

```json
{
  "version": 1,
  "updated_at": "ISO8601",
  "sessions": [
    { "id": "session-2026-04-03", "file": "docs/brainstorm/session-2026-04-03.md", "status": "completed" }
  ],
  "artifacts": [
    {
      "type": "ui_direction",
      "session": "session-2026-04-03",
      "layout": "multi-page",
      "root": ".viepilot/ui-direction/session-2026-04-03/",
      "files": {
        "notes": ".viepilot/ui-direction/session-2026-04-03/notes.md",
        "hub": ".viepilot/ui-direction/session-2026-04-03/index.html",
        "style": ".viepilot/ui-direction/session-2026-04-03/style.css",
        "pages": [{ "slug": "login", "file": "pages/login.html" }]
      },
      "has_ux_walkthrough": true,
      "required": true,
      "consumed": false,
      "consumed_at": null,
      "last_updated": "ISO8601"
    },
    {
      "type": "product_horizon",
      "session": "session-2026-04-03",
      "path": "docs/brainstorm/session-2026-04-03.md",
      "section": "## Product horizon",
      "required": true,
      "consumed": false
    }
  ],
  "ui_task_context_hint": {
    "pages": { "login": "pages/login.html" },
    "shared_notes": ".viepilot/ui-direction/session-2026-04-03/notes.md",
    "shared_style": ".viepilot/ui-direction/session-2026-04-03/style.css"
  }
}
```

**Fix 4 drop points**:
- Drop 1: crystallize Step 0A mandatory read manifest → không còn inference
- Drop 2: `ui_task_context_hint` giữ direct path refs, không chỉ summary
- Drop 3: crystallize auto-populate `context_required` từ `ui_task_context_hint` per page slug
- Drop 4: vp-auto đọc `context_required` (đã có UI paths) → tự nhiên consume UI direction

**Multi-session merge strategy**:
- `product_horizon`: MERGE (accumulate, không overwrite tier tags)
- `ui_direction`: REPLACE với latest session
- `research_notes`: APPEND
- `architecture_inputs`: LATEST wins

**Re-consume + doc-drift**:
- Track `consumed_at` per artifact
- `last_updated > consumed_at` → trigger re-consume + drift check
- Re-consume cũng là doc-drift audit — compare new brainstorm vs existing docs
- Output: drift report per item với risk level + action options

**Decisions**:
- [x] Brainstorm sinh manifest tự động khi `/save` hoặc `/end`
- [x] Crystallize Step 0A: mandatory read trước tất cả steps, fallback nếu không có manifest
- [x] Re-consume trigger doc-drift detection (không chỉ sync)
- [x] vp-crystallize auto-register project vào `~/.viepilot/project-registry.json`

---

### Topic 4: Anchor Syntax — Decision-level Drift Detection

**Granularity chọn**: Decision-level (fine-grained, per decision anchor).

**Design principle**: AI sinh anchor tự động, user không thấy / không cần viết.

**Anchor format** (HTML comment → invisible khi render):
```markdown
<!-- vp:decision id="sidebar-behavior" session="session-2026-04-10" ts="ISO8601" -->
Sidebar collapsible mobile, slide-in 200ms, breakpoint 768px, active state: blue-600
<!-- /vp:decision -->
```

**Sinh khi nào**: Mỗi bullet trong `**Decisions**:` block → một anchor. `/save` scan + backfill anchors còn thiếu.

**ARCHITECTURE.md consumed anchor**:
```markdown
<!-- vp:consumed decision="sidebar-behavior" from="session-2026-04-03" at="ISO8601" -->
**Behavior**: Collapsible on mobile (breakpoint 768px), slide-in 200ms animation.
<!-- /vp:consumed -->
```

**Drift check algorithm**:
```
for each vp:consumed in docs:
  find matching vp:decision in latest sessions
  normalize text → compare
  if changed → DRIFT with risk:
    LOW:  visual/copy (color, label, text)
    MED:  layout/flow (component swap, structure)
    HIGH: logic/data/auth (matches compliance_domains → Gap G re-check)
```

**Drift report format**: per-item action — (a) auto-update / (b) skip / (c) flag for review.

**Docs covered**:
- ✅ ARCHITECTURE.md
- ✅ ROADMAP.md (task descriptions)
- ✅ TASK.md (objective + context_required)
- ❌ SYSTEM-RULES.md (too noisy)
- ❌ PROJECT-META.md (stable)

**Decisions**:
- [x] Decision-level granularity với anchor syntax
- [x] Anchor AI-generated, user-transparent (HTML comment)
- [x] Risk classification: LOW/MED/HIGH tự động
- [x] HIGH risk drift → Gap G compliance re-check tự động

---

### Topic 5: Remaining Post-MVP — Quick Overview

**Gap E: /vp-status --all** (Complexity: S)
- `~/.viepilot/project-registry.json` — danh sách project paths
- vp-crystallize auto-register khi crystallize project mới
- vp-status đọc registry → TRACKER.md + HANDOFF.json từng project → aggregate display
- Status icons: ● in_progress / ⚠ control_point / ✓ complete / ○ not_started

**Token Budget Awareness** (Complexity: M)
- Check sau mỗi sub-task: estimated context usage
- > 70%: warn + offer pause, HANDOFF write
- > 90%: force pause + HANDOFF write
- HANDOFF.log event: `token_budget_warning` với `used_pct`
- Heuristic estimate từ file sizes + context reads (không cần exact count)

**Gap G Extended — Keyword Scan** (Complexity: S)
- Thêm task description + objective scan bên cạnh write_scope path scan
- Keywords: password, token, session, encrypt, stripe, payment, invoice, migration, schema, bcrypt, tls
- Match → suggest L3.block = true + compliance_preflight warn
- Không auto-set (chỉ suggest) — user confirm

**AutoDream → STACKS.md** (Complexity: M)
- Trigger: sau phase complete (hook trong autonomous.md)
- Đọc tasks/*.md từ phase → extract patterns, anti-patterns, decisions
- Append to `.viepilot/STACKS.md` (project-local)
- Update `~/.viepilot/stacks/{stack}/BEST-PRACTICES.md` nếu có finding cross-project
- New file: `workflows/autodream.md`

**Fork State Updates** (Complexity: S, Risk: medium)
- State writes sau task PASS → background Agent (run_in_background: true)
- Main loop tiếp tục task tiếp theo không đợi
- Fallback: nếu fork không confirm → sync write (không để state drift)
- Cần careful testing trước ship

**Parallel Task Execution** (Complexity: L — DEFER)
- Cần `can_parallel_with` graph từ TASK.md (đã có field)
- Spawn parallel agents cho independent task clusters
- State merge: mỗi agent write file tạm → merge sau
- Defer sau Fork State Updates stable

**Decisions**:
- [x] Tier 1 (ship first): Manifest + Gap E + Gap G Extended + Token Budget
- [x] Tier 2: AutoDream + Fork State
- [x] Tier 3 (defer): Parallel Execution + Team Memory Sync + ML Auto-approval

---

## Summary

### Key Decisions

1. **Brainstorm Artifact Manifest** là epic quan trọng nhất từ session này — fix root cause verified qua code audit
2. **4 drop points** trong brainstorm→crystallize→vp-auto chain được xác định và có design fix cụ thể
3. **Re-consume = doc-drift detection** — hai-trong-một, không cần separate audit cho brainstorm changes
4. **Decision anchor** AI-generated, user-transparent — không friction cho user
5. **Milestone v2.x structure**: Tier 1 (4 epics) → Tier 2 (2 epics) → Tier 3 (defer)
6. **Gap E** là daily-use feature quan trọng nhất trong list gốc — complexity S, value cao cho 4+ projects
7. **Giữ local-first** — không server, không network service trong Post-MVP
8. **Parallel execution** defer cho đến Fork State Updates proven

### Action Items

**Tier 1 — v2.x MVP candidates:**
- [ ] Brainstorm Artifact Manifest (P1-P6 core): manifest schema + brainstorm sinh + crystallize consume
- [ ] Gap E: /vp-status --all + project-registry.json
- [ ] Gap G Extended: keyword scan + compliance_preflight extension
- [ ] Token Budget Awareness: sub-task check + HANDOFF.log event

**Tier 2:**
- [ ] AutoDream → STACKS.md: phase complete hook + workflows/autodream.md
- [ ] Fork State Updates: background Agent pattern + fallback

**Tier 3 (defer):**
- [ ] Parallel task execution (after Tier 2)
- [ ] Team memory sync (needs server decision)
- [ ] ML auto-approval (needs historical data)

### Open Questions
- Artifact Manifest: cần thêm artifact type nào ngoài ui_direction, product_horizon, research_notes, architecture_inputs?
- Anchor backfill: sessions cũ (session-2026-04-02) có cần retroactive anchor không?
- project-registry.json: user có thể manually add/remove projects hay chỉ auto-register?

### Next Steps
1. `/vp-evolve --milestone` để crystallize session này thành v2.x milestone
2. Hoặc `/vp-request` để log từng epic thành requests trước khi evolve

# Task 107.4 — Tests + CHANGELOG + version bump

## Objective
Write ≥10 unit tests for ENH-072, update CHANGELOG.md with [2.41.0], bump `package.json` to 2.41.0.

## Paths
tests/unit/phase107-enh072-version-update-check.test.js
CHANGELOG.md
package.json

## File-Level Plan

### `tests/unit/phase107-enh072-version-update-check.test.js`

Test groups (≥10 tests total):

**Group 1 — checkLatestVersion cache logic (4 tests)**
- returns cached result when cache is fresh (< 24h) — no network call
- returns `{ upToDate: true }` when cache file does not exist (new install) and network fails
- writes cache file after successful network fetch
- `opts.force` bypasses fresh cache and makes network call

**Group 2 — checkLatestVersion failure handling (3 tests)**
- network error → returns `{ upToDate: true }` (no throw)
- response timeout → returns `{ upToDate: true }` (no throw)
- malformed JSON response → returns `{ upToDate: true }` (no throw)

**Group 3 — vp-tools check-update subcommand (4 tests)**
- `--silent` flag present in `bin/vp-tools.cjs` dispatch
- `--json` flag handled (output shape documented)
- `--force` flag passed through to checkLatestVersion
- help/list output includes `check-update`

**Group 4 — SKILL.md version_check blocks (2 tests)**
- all 17 vp-* SKILL.md files contain `<version_check>` block
- `<version_check>` block positioned after `</greeting>` in each file

Use `opts.cacheFile` injection (temp file) for cache tests to avoid touching `~/.viepilot/`.
Mock `https.get` with a stub module or use `opts._fetchFn` injection pattern.

### `CHANGELOG.md`
Add section:
```markdown
## [2.41.0] — 2026-04-24

### Added
- ENH-072: `checkLatestVersion()` in `lib/viepilot-update.cjs` — fetches npm registry, caches 24h in `~/.viepilot/update-cache.json`
- ENH-072: `vp-tools check-update [--silent|--json|--force]` subcommand
- ENH-072: `<version_check>` block in all 17 `vp-*` SKILL.md files — non-blocking update notice banner after greeting
```

### `package.json`
- `"version"`: `"2.40.0"` → `"2.41.0"`

## Best Practices
- Use `opts.cacheFile` and `opts._fetchFn` injection in `checkLatestVersion` for testability
- Test file isolation: use `os.tmpdir()` paths, clean up in `afterEach`

## Verification
```bash
npx jest tests/unit/phase107-enh072-version-update-check.test.js --no-coverage
# Expected: ≥10 tests pass, 0 fail
npm test 2>&1 | tail -5
# Expected: all suites pass
```

## Acceptance Criteria
- [ ] ≥10 tests in test file, all passing
- [ ] Full test suite passes (no regressions)
- [ ] CHANGELOG.md has `[2.41.0]` section
- [ ] `package.json` version = `"2.41.0"`

# Task 50.1 — templates/architect/style.css: ID badge + action button styles

**Phase**: 50 | **Status**: planned

## Paths
- `templates/architect/style.css`

## Objective
Add CSS classes for the item ID badge and Approve/Edit action buttons. Must use existing CSS custom properties (color palette already defined in style.css). Buttons hidden by default, shown on row/card hover.

## File-Level Plan
### style.css additions
- `.arch-item-actions` — wrapper div for badge + buttons; inline-flex; gap
- `.arch-id-badge` — small monospace pill; muted color; non-intrusive
- `.arch-btn` — base button: small, compact, border-radius, cursor pointer
- `.arch-btn-approve` — green tint (success color); ✅ icon text
- `.arch-btn-edit` — blue/muted tint (accent color); ✏️ icon text
- `.arch-btn.copied` — brief "Copied!" state: opacity change
- Hover reveal: `tr:hover .arch-item-actions` / `.card-item:hover .arch-item-actions` → visible; default opacity 0

## Acceptance Criteria
- [ ] `.arch-id-badge` class defined
- [ ] `.arch-btn-approve` and `.arch-btn-edit` classes defined
- [ ] Hover-reveal pattern using existing selectors
- [ ] No new color values — only uses existing CSS variables

# Task 78.4 — SKILL.md Files + Contract Tests

## Objective
Add "Adapter Compatibility" section to `skills/vp-{crystallize,brainstorm,request}/SKILL.md`
and write contract tests asserting the adapter-aware instruction blocks are present in all
affected workflow files.

## Paths
```
skills/vp-crystallize/SKILL.md
skills/vp-brainstorm/SKILL.md
skills/vp-request/SKILL.md
tests/unit/vp-enh048-askuserquestion.test.js
```

## SKILL.md Updates

### Section to add to each SKILL.md
Append an **Adapter Compatibility** section:

```markdown
## Adapter Compatibility

### AskUserQuestion Tool
This skill uses adapter-aware interactive prompts. Behavior depends on your adapter:

| Adapter | Interactive Prompts | Notes |
|---------|---------------------|-------|
| Claude Code (terminal) | ✅ `AskUserQuestion` tool | Click-to-select UI, multi-select, previews |
| Claude Code (VS Code ext) | ⚠️ Partial | Terminal yes; VS Code UI pending [#12609](https://github.com/anthropics/claude-code/issues/12609) |
| Cursor (Plan Mode) | ⚠️ Partial | `AskQuestion` in Plan Mode only |
| Cursor (Agent/Skills) | ❌ Text fallback | AskQuestion not available in Agent Mode |
| Codex CLI | ❌ Text fallback | Native tool N/A; community MCP available |
| Antigravity (native) | ❌ Text fallback | Artifact model, no raw tool calls |

When `AskUserQuestion` is not available, the skill automatically falls back to
plain-text numbered list prompts — no configuration required.
```

### Specific placement
- `vp-crystallize/SKILL.md`: append before final `---` or at end of file
- `vp-brainstorm/SKILL.md`: same
- `vp-request/SKILL.md`: same

## Contract Tests

File: `tests/unit/vp-enh048-askuserquestion.test.js`

Test suite structure:
```javascript
describe('ENH-048: AskUserQuestion adapter-aware pattern', () => {
  // --- crystallize.md ---
  test('crystallize.md has Adapter Compatibility table', ...)
  test('crystallize.md has AskUserQuestion spec on project type question', ...)
  test('crystallize.md preserves text fallback list', ...)

  // --- brainstorm.md ---
  test('brainstorm.md has Adapter Compatibility table', ...)
  test('brainstorm.md has AskUserQuestion spec on at least one question block', ...)

  // --- request.md ---
  test('request.md has AskUserQuestion spec on type detection question', ...)
  test('request.md preserves text fallback list', ...)

  // --- evolve.md ---
  test('evolve.md has AskUserQuestion spec on intent question', ...)

  // --- SKILL.md files ---
  test('vp-crystallize SKILL.md has Adapter Compatibility section', ...)
  test('vp-brainstorm SKILL.md has Adapter Compatibility section', ...)
  test('vp-request SKILL.md has Adapter Compatibility section', ...)
})
```

Each test: `fs.readFileSync` the workflow/skill file, `expect(content).toContain(...)` the
expected marker string.

Key marker strings to assert:
- `'Adapter-aware prompt'` in each workflow
- `'AskUserQuestion'` in each workflow
- `'text fallback'` in each workflow (fallback preserved)
- `'Adapter Compatibility'` in each SKILL.md

## Acceptance Criteria
- [ ] `skills/vp-crystallize/SKILL.md` has Adapter Compatibility section with 6-row table
- [ ] `skills/vp-brainstorm/SKILL.md` has Adapter Compatibility section
- [ ] `skills/vp-request/SKILL.md` has Adapter Compatibility section
- [ ] `tests/unit/vp-enh048-askuserquestion.test.js` created with ≥10 tests
- [ ] All tests pass: `npm test -- --testPathPattern=enh048`
- [ ] Each test asserts both AUQ spec presence AND text fallback preservation

## Verification
```bash
npm test -- --testPathPattern=enh048
# Expected: ≥10 tests pass, 0 fail

grep -n "Adapter Compatibility" skills/vp-crystallize/SKILL.md
grep -n "Adapter Compatibility" skills/vp-brainstorm/SKILL.md
grep -n "Adapter Compatibility" skills/vp-request/SKILL.md
# Expected: 1 match each
```

# Task 78.5 — CHANGELOG + Version 2.15.0

## Objective
Update CHANGELOG.md with `[2.15.0]` section documenting ENH-048, and bump
`package.json` version from `2.14.0` to `2.15.0`.

## Paths
```
CHANGELOG.md
package.json
```

## CHANGELOG.md Update

Add new section at the top (after `# Changelog` heading):

```markdown
## [2.15.0] — 2026-04-13

### Changed
- **ENH-048** — AskUserQuestion adapter-aware integration across all vp-* workflows
  - `workflows/crystallize.md`: project type, UI direction, polyrepo prompt blocks
    now include `AskUserQuestion` spec for Claude Code + text fallback for all other adapters
  - `workflows/brainstorm.md`: session type, scope, and related-feature questions updated
  - `workflows/request.md`: request type detection, severity, priority, and brainstorm
    routing questions updated
  - `workflows/evolve.md`: intent detection, complexity, and brainstorm routing questions updated
  - `skills/vp-crystallize/SKILL.md`: added Adapter Compatibility section
  - `skills/vp-brainstorm/SKILL.md`: added Adapter Compatibility section
  - `skills/vp-request/SKILL.md`: added Adapter Compatibility section

### Added
- Adapter compatibility research: Claude Code (✅), Cursor Agent Mode (❌), Codex CLI (❌),
  Antigravity native (❌) — with graceful text-prompt fallback for all non-Claude-Code adapters
- `tests/unit/vp-enh048-askuserquestion.test.js` — ≥10 contract tests
```

## package.json Update

Change `"version"` field:
```json
"version": "2.15.0"
```

## Acceptance Criteria
- [ ] CHANGELOG.md has `[2.15.0]` section at top with ENH-048 bullet points
- [ ] `package.json` version = `"2.15.0"`
- [ ] Version consistent between package.json and CHANGELOG heading

## Verification
```bash
node -e "console.log(require('./package.json').version)"
# Expected: 2.15.0

grep "2.15.0" CHANGELOG.md
# Expected: 1 match (version heading)
```

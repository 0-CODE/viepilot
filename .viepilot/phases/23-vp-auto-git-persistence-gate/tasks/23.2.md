# Task 23.2 — Enforce persistence gate in autonomous workflow

## Objective
Embed executable command checks in `autonomous.md` so PASS transitions require durable git persistence.

## Paths
- `workflows/autonomous.md`

## Implementation Notes
- Add command block for persistence checks:
  - `git status --porcelain`
  - `git rev-parse --abbrev-ref --symbolic-full-name @{u}`
  - `git rev-list --count @{u}..HEAD`
- Define hard-stop branch when any check fails.
- Ensure state update order references this gate before PASS completion updates.

## Verification
`rg "Git Persistence Gate|git rev-list --count" workflows/autonomous.md`

# Task 23.4 — Add verification/tests for persistence behavior

## Objective
Add lightweight CLI verification command and tests for git persistence conditions.

## Paths
- `bin/vp-tools.cjs`
- `tests/unit/validators.test.js`

## File-Level Plan
- Add `vp-tools git-persistence` command returning JSON checks:
  - clean_worktree
  - upstream_configured
  - ahead_count
  - ready_for_persisted_pass
- Add unit tests for command output shape and status code.

## Verification
```bash
npx jest tests/unit/validators.test.js --no-coverage
node bin/vp-tools.cjs git-persistence
```

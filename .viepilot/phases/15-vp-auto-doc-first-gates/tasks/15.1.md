# Task 15.1 — Doc-first gate sequence in autonomous workflow

## Objective

Add an explicit **pre-execution documentation gate** to `workflows/autonomous.md` so implementation cannot start until task contract validation and required pre-flight documentation steps are satisfied.

## Paths (create / modify)

| Path | Action |
|------|--------|
| `workflows/autonomous.md` | Modify — insert ordered gate before “Execute Task” / implementation |

## Implementation notes

- Define a numbered sequence, for example:
  1. Validate task contract (existing section — keep as first hard block).
  2. **Pre-execution gate**: require task file (or PHASE-STATE) to record planned file paths + intent **before** code edits; optionally require `in_progress` on current task in `PHASE-STATE.md` before first file write.
  3. Create start tag `vp-p{phase}-t{task}` **after** gate passes (or document if tag is created at gate boundary).
  4. Only then proceed to implementation steps.
- Add explicit **STOP** wording if gate is skipped (mirror existing “blocked” behavior for missing contract).
- Must remain compatible with stack preflight (order: contract → doc gate → stack preflight → implement).

## Best practices

- One canonical ordering; avoid duplicate contradictory “state-first” paragraphs.
- Keep steps verifiable by a human auditor (checklist, not vague “ensure”).

## Verification

```bash
grep -n "Pre-execution\|doc-first\|documentation gate" workflows/autonomous.md
```

Expect: at least one dedicated subsection and ordering before implementation language.

### Expected

- Grep finds pre-execution / doc-first gate section.
- Implementation subsection clearly follows the gate in file order.

## State update checklist

- [ ] After PASS: note completion in this file; update `PHASE-STATE.md` task 15.1 → done.

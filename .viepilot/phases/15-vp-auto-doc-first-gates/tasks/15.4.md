# Task 15.4 — Audit heuristics for execute-first / docs-later (BUG-001)

## Objective

Extend `workflows/audit.md` (Tier 1 or Tier 2) with **documented heuristics** that can surface suspected “implementation before task documentation/state” ordering issues.

## Paths

| Path | Action |
|------|--------|
| `workflows/audit.md` | Modify — add subsection under Tier 1e or new 1f |

## Implementation notes

- Add guidance (not necessarily automated code) such as:
  - Compare git commit timestamps vs `PHASE-STATE.md` last-updated for the active task.
  - Flag if multiple implementation files changed before task status left `not_started`.
  - Flag if task markdown lacks “Implementation notes” / plan while git shows edits in task scope.
- Severity: medium by default; document as heuristic (false positives possible).

## Best practices

- Label as **heuristic** — avoid claiming certainty from git alone.
- Point auditors to manual confirmation.

## Verification

```bash
grep -n "execute-first\|docs-later\|doc-first\|BUG-001" workflows/audit.md
```

### Expected

- Audit workflow references execute-first/docs-later or BUG-001 pattern.

## State update checklist

- [ ] After PASS: `PHASE-STATE.md` task 15.4 → done; update `.viepilot/requests/BUG-001.md` acceptance ticks when verified.

# Task 15.2 — Mirror doc-first gate in `skills/vp-auto/SKILL.md`

## Objective

Align `skills/vp-auto/SKILL.md` with `workflows/autonomous.md` so the skill instructs the model to **never implement before** the doc-first / pre-execution gate passes.

## Paths

| Path | Action |
|------|--------|
| `skills/vp-auto/SKILL.md` | Modify — `<objective>` and/or `<process>` ordering |

## Implementation notes

- Repeat the same canonical order as workflow: validate contract → pre-doc gate → stack preflight → implement → verify → state updates.
- Add a single clear rule: **Do not write or modify product files until pre-execution documentation is recorded** (task file + PHASE-STATE as specified in workflow).

## Best practices

- Keep skill text concise; defer full step text to `workflows/autonomous.md` via execution_context.
- Bump skill `version` in frontmatter if project convention expects it (patch bump).

## Verification

```bash
grep -n "doc-first\|Pre-execution\|before.*implement" skills/vp-auto/SKILL.md
```

### Expected

- Skill mentions doc-first or equivalent ordering relative to implementation.

## State update checklist

- [ ] After PASS: `PHASE-STATE.md` task 15.2 → done.

# Task 122.4 — Tests + CHANGELOG + version bump

## Objective

Write ≥10 unit tests covering the BROWNFIELD-TRACE.md spec (initialization, per-category update,
coverage gate, gap filling log, step completion). Update CHANGELOG [2.47.0] and bump to 2.47.0.

## Paths

```
tests/unit/phase122-enh081-brownfield-trace-log.test.js
CHANGELOG.md
package.json
```

## File-Level Plan

### tests/unit/phase122-enh081-brownfield-trace-log.test.js

≥10 tests in `describe('Phase 122 — ENH-081: Brownfield Scan Trace Log')`:

**Trace initialization (≥3):**
1. `BROWNFIELD-TRACE.md` creation spec present (initialization section)
2. Resume detection: `scan_complete` case — AUQ with "Resume" + "Re-scan" options
3. Interrupted scan detection: `scanning` in-progress case — AUQ with "Resume from Cat {N}"
4. All 13 categories pre-populated as "planned" in template

**Per-category update protocol (≥3):**
5. On-start: row status → "scanning" + timestamp spec present
6. On-complete: status transitions table (planned → scanning → done/assumed/skipped)
7. Files Read Log: `[x]` (read) and `[ ]` (expected but not present) format documented
8. Example: Signal 5 (API Contract Files) example present

**Coverage gate + Gap Filling + Step Completion (≥3):**
9. Coverage gate: warn if "planned" rows found before Scan Report
10. Coverage gate: non-blocking + sets `scan_complete`
11. Gap Filling Log: append spec present
12. Step Completion: 0-C row update spec present
13. Step Completion: 0-D row has 3 cases (done/skipped/N/A)

**package.json + CHANGELOG:**
14. version = 2.47.0
15. CHANGELOG [2.47.0] with ENH-081 mention

### CHANGELOG.md [2.47.0]

```markdown
## [2.47.0] - 2026-05-06

### Added
- **ENH-081** `BROWNFIELD-TRACE.md` — real-time scan trace log for brownfield crystallize
  - Created at Step 0-B start (before any signal category runs) with all 13 categories
    pre-populated as "planned"
  - Resume detection: if prior trace shows `scan_complete` → offer skip re-scan; if
    trace shows `scanning` (interrupted) → offer resume from in-progress category
  - Per-category update protocol: row status (planned → scanning → done/assumed/skipped),
    Files Read Log (`[x]` read / `[ ]` expected but missing), elapsed duration
  - Coverage gate: warns if any category still "planned" before Scan Report presented;
    non-blocking; sets Status → `scan_complete` in trace
  - Gap Filling Log: appends user answers during interactive gap-filling (field | status | source | value)
  - Step Completion: tracks 0-B scanner / 0-C brainstorm stub / 0-D UI workspace completion
    with timestamps
```

### package.json version

Bump `"version": "2.46.1"` → `"version": "2.47.0"`

## Acceptance Criteria

- [ ] ≥10 tests (15 recommended) in test file
- [ ] All tests pass: `npm test`
- [ ] `package.json` version = `2.47.0`
- [ ] CHANGELOG [2.47.0] with ENH-081 + all major feature points

## Verification

```bash
npm test -- --testPathPatterns=phase122
grep '"version": "2.47.0"' package.json
grep '\[2.47.0\]' CHANGELOG.md
```

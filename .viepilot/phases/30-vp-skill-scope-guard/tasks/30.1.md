# Task 30.1 — Define vp-only scope policy contract

## Objective
Định nghĩa rõ policy mặc định chỉ dùng skill `vp-*` và cơ chế explicit opt-in khi cần tham chiếu skill ngoài framework.

## Paths
- `workflows/*.md` (policy statement points)
- `skills/vp-*/SKILL.md` (scope guard statement)
- `docs/skills-reference.md`, `docs/user/features/*.md`

## File-Level Plan
- Viết policy wording thống nhất (default + opt-in + fallback behavior).
- Chốt terminology để áp dụng đồng bộ cho task 30.2/30.3/30.4.

## Verification
- `rg "vp-\*|opt-in|external skills|scope guard" skills workflows docs`

## Status
done

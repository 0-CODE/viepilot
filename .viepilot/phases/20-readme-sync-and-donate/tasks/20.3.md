# Task 20.3 — Add fallback/guard when `cloc` is missing

## Objective
Prevent workflow failure when `cloc` is unavailable.

## Scope
- Detect `cloc` presence.
- Log actionable guidance if missing (brew/apt/choco).
- Evaluate optional installer hook for best-effort `cloc` install with explicit user consent.
- Skip metric update safely without breaking flow.

## Verification
```bash
# simulate missing cloc and check fallback behavior
```

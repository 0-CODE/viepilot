# Task 20.1 — Design README LOC auto-sync flow with `cloc`

## Objective
Define where and when README metrics are recalculated during workflow execution.

## Scope
- Map current README metric block format.
- Define update trigger inside `vp-auto` flow.
- Define stable `cloc` command and parsing strategy.

## Verification
```bash
# design review in phase artifacts
```

# Task 5.2 — Revert crystallize.md workflow path convention

## Meta
- **Phase**: 05
- **Task**: 5.2
- **Status**: not_started

## Task Metadata
```yaml
type: fix
complexity: S
write_scope:
  - "workflows/crystallize.md"
recovery_budget: S
can_parallel_with: [5.1]
recovery_overrides:
  L1:
    block: false
  L2:
    block: false
  L3:
    block: false
    reason: ""
```

## Objective

Revert `.claude/viepilot/` → `.cursor/viepilot/` trong `workflows/crystallize.md` (7 template references).

**Root cause**: Task 4.6b đổi các template path references trong crystallize.md về `.claude/`. Nhưng workflow files cũng phải dùng `.cursor/viepilot/` làm source convention — khi install Claude Code, script sẽ thêm rewrite step cho `claudeViepilotDir/workflows/` (sau khi fix Task 5.3).

## Paths
- `workflows/crystallize.md`

## Implementation Notes
```bash
sed -i '' 's|\.claude/viepilot/|.cursor/viepilot/|g' workflows/crystallize.md
```

Verify:
```bash
grep -n '\.claude/viepilot/' workflows/crystallize.md && echo "STALE" || echo "clean"
grep -n '\.cursor/viepilot/templates/' workflows/crystallize.md | head -5
```

## Acceptance Criteria
- [ ] `grep '\.claude/viepilot/' workflows/crystallize.md` → 0 results
- [ ] `grep '\.cursor/viepilot/templates/' workflows/crystallize.md` → ≥7 results
- [ ] Commit với message `fix(workflows): BUG-A revert crystallize.md template path convention .claude → .cursor`

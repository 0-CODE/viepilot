# Task 5.3 — Fix install script: add workflow rewrite step for Claude Code

## Meta
- **Phase**: 05
- **Task**: 5.3
- **Status**: not_started

## Task Metadata
```yaml
type: fix
complexity: M
write_scope:
  - "lib/viepilot-install.cjs"
  - "tests/"
recovery_budget: M
can_parallel_with: []
recovery_overrides:
  L1:
    block: false
  L2:
    block: false
  L3:
    block: false
    reason: ""
```

## Objective

Thêm `rewrite_paths_in_dir` step cho `claudeViepilotDir/workflows/` và `claudeViepilotDir/templates/` trong install script để khi install Claude Code target, các path references trong workflow files được rewrite từ `.cursor/viepilot/` → `.claude/viepilot/`.

**Root cause (BUG-B)**: Hiện tại install script chỉ rewrite `claudeSkillsDir` (skills directory):
```javascript
// lib/viepilot-install.cjs line 195-201
steps.push({
  kind: 'rewrite_paths_in_dir',
  dir: claudeSkillsDir,       // ~/.claude/skills/ — CHỈ skills
  glob: '**/*.md',
  from: '.cursor/viepilot',
  to: '.claude/viepilot',
});
```

Nhưng `~/.claude/viepilot/workflows/` (và templates/) cũng cần rewrite tương tự.

## Paths
- `lib/viepilot-install.cjs` (add 2 rewrite steps sau line 201)

## File-Level Plan

**Sau step rewrite hiện tại (line 201), thêm 2 steps:**

```javascript
// Rewrite paths in mirrored workflow files
steps.push({
  kind: 'rewrite_paths_in_dir',
  dir: path.join(claudeViepilotDir, 'workflows'),
  glob: '**/*.md',
  from: '.cursor/viepilot',
  to: '.claude/viepilot',
});
// Rewrite paths in mirrored template files
steps.push({
  kind: 'rewrite_paths_in_dir',
  dir: path.join(claudeViepilotDir, 'templates'),
  glob: '**/*.md',
  from: '.cursor/viepilot',
  to: '.claude/viepilot',
});
```

**Exact insertion point**: After closing `});` of skills rewrite step (current line 201), before closing `}` of `if (claudeViepilotDir)` block (current line 202).

## Test Coverage

Kiểm tra existing test file structure:
```bash
ls tests/unit/
```

Verify test covers `rewrite_paths_in_dir` step count for claude-code target — expected là 3 steps (skills + workflows + templates) thay vì 1.

Nếu test hiện có assert count → cần cập nhật expected count.

## Acceptance Criteria
- [ ] `lib/viepilot-install.cjs` có 3 `rewrite_paths_in_dir` steps khi target = claude-code
- [ ] `buildInstallPlan({installTargets:['claude-code']}).steps` chứa step với `dir` = `~/.claude/viepilot/workflows`
- [ ] `buildInstallPlan({installTargets:['claude-code']}).steps` chứa step với `dir` = `~/.claude/viepilot/templates`
- [ ] Existing tests vẫn pass sau khi update
- [ ] Commit với message `fix(install): BUG-B add rewrite_paths_in_dir for claude workflows + templates dirs`

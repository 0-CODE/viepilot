# Task 27.4 — Documentation

## Objective
Người dùng và AI biết cách gọi `info` / `update`; README metrics/skills count cập nhật sau thêm 2 skill.

## Paths
- `docs/skills-reference.md` — thêm sections `/vp-info`, `/vp-update` (hoặc regenerate từ workflow scan sau khi có skill — manual append OK)
- `docs/dev/cli-reference.md` — mục `info`, `update`
- `docs/user/quick-start.md` — một đoạn ngắn “Kiểm tra phiên bản / cập nhật”
- `README.md` — bảng CLI nếu có; badge Skills **14 → 16** sau khi thêm 2 skill; `npm run readme:sync`

## File-Level Plan
- Mô tả flags đầy đủ; ví dụ `vp-tools info --json`, `vp-tools update --dry-run`.
- Ghi chú: `npx viepilot` user có thể dùng global `vp-tools` sau `npm i -g viepilot`.

## Verification
- `rg "vp-tools info" docs/` có hit.
- Skills count trong README khớp `ls skills/*/SKILL.md | wc -l`.

## Acceptance Criteria
- [x] skills-reference có 2 section mới (đã ship ở task 27.3; verify lại).
- [x] cli-reference + quick-start cập nhật.

## Implementation Notes
- **2026-04-01:** Tag `viepilot-vp-p27-t4` sau gate. Thêm `### info`, `### update` vào `docs/dev/cli-reference.md` (flags, ví dụ, JSON fields cho `info --json`). Thêm khối ngắn trong `docs/user/quick-start.md` sau Step 1. README: số subcommand `vp-tools` = 17 (gồm `help`), tổng CLI surface 18 nếu tính `viepilot` installer; chạy `npm run readme:sync` (~24,971+ LOC). Sửa `docs/README.md` mô tả CLI/skills index.

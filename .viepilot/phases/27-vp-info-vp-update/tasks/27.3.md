# Task 27.3 — Skills `vp-info` + `vp-update`

## Objective
Hai skill Cursor gương lệnh CLI để agent luôn gọi đúng contract.

## Paths
- `skills/vp-info/SKILL.md` (new)
- `skills/vp-update/SKILL.md` (new)

## File-Level Plan
- **vp-info**: invocation `/vp-info`; execution_context trỏ `bin/vp-tools.cjs`; process: chạy `node path/to/vp-tools.cjs info` hoặc `info --json` khi cần parse; success criteria liệt kê fields.
- **vp-update**: invocation `/vp-update`; flags `--dry-run`, `--yes`, `--global`; cảnh báo không chạy trong repo user không phải viepilot nếu chỉ muốn upgrade global — document.
- Frontmatter: `name`, `description`, `version: 0.1.0`.

## Best practices
- Mirror style `skills/vp-status/SKILL.md` (adapter + objective + process).

## Verification
- File tồn tại và có `execution_context` / command line rõ ràng.

## Acceptance Criteria
- [x] Cả hai skill có trong `skills/` và được nhắc trong phase 27.4 `skills-reference`.

## Implementation Notes
- **2026-04-01:** Doc-first satisfied; tag `viepilot-vp-p27-t3` trước commit shipping. Tạo `skills/vp-info/SKILL.md` + `skills/vp-update/SKILL.md` (adapter + objective + process + success_criteria); `execution_context` → `bin/vp-tools.cjs`. Bổ sung mục `/vp-info` và `/vp-update` trong `docs/skills-reference.md`; README skills **16** + bảng Skills Reference.

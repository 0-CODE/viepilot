# Task 27.2 — CLI `vp-tools update`

## Objective
Entrypoint cập nhật ViePilot lên bản npm mới nhất với `--dry-run`, xác nhận tương tác, hoặc `--yes` cho automation.

## Paths
- `lib/viepilot-info.cjs` — reuse `fetchLatestNpmVersion`, `readInstalledVersion`, `resolveViepilotPackageRoot`
- `lib/viepilot-update.cjs` (new) — `planUpdate({ global, dryRun, yes })` returns shell command or runs `npm install -g viepilot@latest` / `npm install viepilot@latest` trong thư mục global prefix
- `bin/vp-tools.cjs` — command `update`

## File-Level Plan
- Phát hiện **global** vs **local**: `--global` flag; mặc định: nếu `resolveViepilotPackageRoot` nằm trong `node_modules/viepilot` của project → `npm install viepilot@latest` tại project root; nếu global npm root → `npm install -g viepilot@latest`.
- Nếu đã latest → exit 0 + message (no-op).
- `--dry-run`: chỉ in lệnh sẽ chạy, không spawn.
- Non-interactive: bắt buộc `--yes` hoặc chỉ `--dry-run`; nếu không có TTY và không `--yes` → exit 1 + hướng dẫn.
- Document rollback: `npm install -g viepilot@<prev>` hoặc chạy `npx viepilot install` lại.

## Best practices
- Dùng `spawnSync` với `stdio: inherit` khi thật sự chạy npm.
- Không xóa `.viepilot` của user project.

## Verification
- `node bin/vp-tools.cjs update --dry-run` — exit 0, in planned command.
- `npm test` — mock hoặc chỉ test planner không gọi npm.

## Acceptance Criteria
- [x] `update --dry-run` an toàn mặc định.
- [x] `update --yes` chạy được trong CI (có network) hoặc skip e2e trong test.
- [x] Đã latest → message rõ, exit 0.

## Implementation Notes
- **2026-04-01 (doc-first):** `PHASE-STATE` → 27.2 `in_progress`; tag `viepilot-vp-p27-t2` before shipping commit.
- `classifyInstall`: khớp **global path trước** hậu tố `node_modules/viepilot` (tránh nhầm `.../lib/node_modules/viepilot` thành local).
- Rollback: in `npm install -g viepilot@<previous>` sau planned command.
- **2026-04-01 (done):** `lib/viepilot-update.cjs`, `vp-tools update`, tests; tags `viepilot-vp-p27-t2` / `viepilot-vp-p27-t2-done`.

# Task 145.2 — workflows/autonomous.md: TodoWrite→TaskCreate + Monitor + PushNotification

## Objective
Fix two stale `TodoWrite` references in `workflows/autonomous.md` (disabled since Claude Code
v2.1.142), add a Monitor-based quality gate pattern for watching test runs, and add
PushNotification on phase complete so users get notified when stepping away.

## Paths
- `workflows/autonomous.md`

## File-Level Plan

### Fix 1: TodoWrite → TaskCreate (lines 262 and 345)

**Line 262 (current):**
```
- Activate shared TodoWrite task list for teammate coordination
```
**Replace with:**
```
- Activate shared TaskCreate/TaskList task list for teammate coordination
```

**Line 345 (current):**
```
- Write all pending task IDs to shared `TodoWrite` list at phase start
```
**Replace with:**
```
- Write all pending task IDs to shared task list via `TaskCreate` at phase start
```

### Fix 2: Add Monitor quality-gate pattern

Find the section describing quality gate / automated verification (grep for "quality_gate"
or "Run defined commands" or "Verify Task"). After the automated verification commands block,
add a note:

```markdown
**Monitor pattern (ENH-099) — long-running verifications:**
For tests that run in watch mode or dev servers, use the `Monitor` tool instead of blocking
`Bash`:
```
Monitor: run `npm test -- --watchAll=false` and react when output contains FAIL
```
This keeps the agentic loop active while the test runner completes.
```

### Fix 3: Add PushNotification on phase complete

Find the "Phase Complete" section (grep for "Phase Complete" or "phase complete"). After the
summary output block, add:

```markdown
**PushNotification (ENH-099, optional):**
After phase quality gate passes, call `PushNotification` with a summary message so the user
receives a desktop + phone alert when stepping away from long-running phases:
- Message: "Phase {N} — {name} complete ✅ ({tasks} tasks, {tests} tests)"
- Only available on Anthropic-hosted Claude Code (not Bedrock/Vertex).
```

## Acceptance Criteria
- [ ] `grep -c "TodoWrite" workflows/autonomous.md` → `0`
- [ ] `grep "TaskCreate\|task list" workflows/autonomous.md | grep -i "teammate\|phase start"` → ≥1 hit
- [ ] `grep "Monitor" workflows/autonomous.md` → ≥1 hit
- [ ] `grep "PushNotification" workflows/autonomous.md` → ≥1 hit

## Complexity: S
## Parallel: ✅ with 145.1 + 145.3

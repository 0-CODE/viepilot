# Task 145.1 — skills/vp-auto/SKILL.md: Add 8 New Claude Code Tools

## Objective
Update the Claude Code adapter `## C. Tool Usage` section in `skills/vp-auto/SKILL.md` to
document 8 new built-in tools discovered in ENH-099 research. These tools are high-value for
the vp-auto orchestration workflow and are currently undocumented.

## Paths
- `skills/vp-auto/SKILL.md`

## File-Level Plan

### skills/vp-auto/SKILL.md — Claude Code adapter (id="claude-code") ## C. Tool Usage

**Current state (lines 64-72):**
```
## C. Tool Usage
Use Claude Code tools: `Bash` (shell), `Read` (file), `Edit` + `Write` (file write/patch),
`Grep` (search), `Glob` (file patterns), `LS`, `WebSearch`, `WebFetch`,
`Agent` (spawn subagent — multi-level nesting supported)
Interactive: `AskUserQuestion` (deferred — preload via ToolSearch before first call)
```

**Target state — expand to:**
```
## C. Tool Usage
Use Claude Code tools: `Bash` (shell), `Read` (file), `Edit` + `Write` (file write/patch),
`Grep` (search), `Glob` (file patterns), `LS`, `WebSearch`, `WebFetch`,
`Agent` (spawn subagent — multi-level nesting supported)
Interactive: `AskUserQuestion` (deferred — preload via ToolSearch before first call)

**Additional tools (ENH-099):**
- `Monitor` — run command in background, each output line fed back to Claude; use for
  `npm test --watch`, dev server tail, CI polling
- `CronCreate` / `CronDelete` / `CronList` — in-session scheduled prompts (survive
  `--resume`); use for periodic quality checks during long phases
- `EnterWorktree` / `ExitWorktree` — isolated git worktree per task; auto-cleanup on exit;
  use for parallel task execution without branch conflicts (not available to subagents)
- `LSP` — code intelligence: type errors after each edit, jump to definition, find
  references; requires code intelligence plugin; replaces post-edit `tsc --noEmit`
- `PushNotification` — desktop + phone push on phase complete or control-point pause
  (Anthropic infra only; not Bedrock/Vertex)
- `EnterPlanMode` / `ExitPlanMode` — plan gate before implementation: EnterPlanMode
  switches to read-only planning; ExitPlanMode presents plan for approval before any file edits
```

## Acceptance Criteria
- [ ] `grep "Monitor" skills/vp-auto/SKILL.md` → ≥1 hit
- [ ] `grep "CronCreate" skills/vp-auto/SKILL.md` → ≥1 hit
- [ ] `grep "EnterWorktree" skills/vp-auto/SKILL.md` → ≥1 hit
- [ ] `grep "LSP" skills/vp-auto/SKILL.md` → ≥1 hit
- [ ] `grep "PushNotification" skills/vp-auto/SKILL.md` → ≥1 hit
- [ ] `grep "EnterPlanMode" skills/vp-auto/SKILL.md` → ≥1 hit

## Complexity: S
## Parallel: ✅ with 145.2 + 145.3

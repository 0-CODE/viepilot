# Task 54.4 — Jest contract tests: FEAT-012 staleness hook

**Phase**: 54 | **Status**: planned

## Paths
- `tests/unit/brainstorm-staleness-hook.test.js`

## Objective
Contract tests for the brainstorm staleness hook and the `hooks install` subcommand.
4 groups covering hook script behavior, HTML patching, install command, and docs.

## File-Level Plan

### brainstorm-staleness-hook.test.js — ~20 tests

**Group 1: Hook script — input parsing + session discovery (5 tests)**
- Hook file exists at `lib/hooks/brainstorm-staleness.cjs`
- Hook file has shebang `#!/usr/bin/env node`
- `findActiveSession(fakeHome)` returns null when no session files exist
- `findActiveSession(cwd)` returns most-recently-modified notes.md when multiple exist
- `findActiveSession(cwd)` finds docs/brainstorm/session-*.md as fallback

**Group 2: Staleness detection (5 tests)**
- `detectStaleItems("c4 context diagram", dir)` returns architecture.html
- `detectStaleItems("entity relationship table", dir)` returns erd.html
- `detectStaleItems("no matching content", dir)` returns empty array
- `detectStaleItems()` handles multiple keyword matches → multiple pages
- Keywords are case-insensitive

**Group 3: HTML patching (5 tests)**
- `markStaleInFile(htmlPath, reason)` adds data-arch-stale="true" to elements with data-arch-id
- `markStaleInFile` is idempotent (second call doesn't duplicate attrs)
- `markStaleInFile` adds data-arch-stale-note with reason
- `markStaleInFile` returns true when changes made, false when already stale
- `markStaleInFile` does not touch elements without data-arch-id

**Group 4: hooks install subcommand (5 tests)**
- `vp-tools hooks install` (via spawn) exits 0
- After install, `~/.claude/settings.json` contains Stop hook entry
- Re-running `hooks install` is idempotent (same entry, not duplicated)
- `hooks install --adapter cursor-agent` exits 0 with "not supported" message
- `hooks install --adapter bogus` exits 1 with error

## Acceptance Criteria
- [ ] ~20 new tests all pass
- [ ] All prior tests still pass (558+ → ~578 total)
- [ ] npm test green

# Task 1.6: Refactor HANDOFF.json template → v2 schema

## Meta
- **Phase**: 01-foundation
- **Status**: not_started
- **Complexity**: M
- **Dependencies**: None
- **Git Tag**: vp2-p1-t1.6

## Objective

Nếu có `templates/project/HANDOFF.json` — update lên v2 schema. Nếu không có — tạo mới. Schema v2 phải có tất cả top-level keys: version, position, recovery, context, control_point, meta.

Cũng update crystallize.md để generate HANDOFF.json từ v2 template (hoặc initialize với v2 schema).

## Paths
```yaml
files_to_create:
  - templates/project/HANDOFF.json    # if not exists
files_to_modify:
  - templates/project/HANDOFF.json    # if exists (update to v2)
  - workflows/crystallize.md          # Step 9: initialize HANDOFF.json with v2 schema
```

## File-Level Plan
- `templates/project/HANDOFF.json`: Write v2 schema với version=2, position (phase/task/sub_task/status), recovery (l1/l2/l3_attempts, recent_blocker), context (active_stacks, last_decision), control_point (active/reason/ts), meta (last_written/last_archived). All values = null/0/false/[] defaults.
- `workflows/crystallize.md` Step 9: Verify crystallize generates HANDOFF.json from v2 template (hoặc hardcode v2 schema inline). Update Step 9 instruction nếu cần.

## Context Required
```yaml
files_to_read:
  - .viepilot/HANDOFF.json  # reference: v2 schema này đã implement
  - docs/brainstorm/session-2026-04-02.md  # Topic 10: HANDOFF.json v2 schema
  - workflows/crystallize.md  # Step 9 hiện tại
```

## v2 Schema Reference
```json
{
  "version": 2,
  "position": {
    "phase": null,
    "task": null,
    "sub_task": null,
    "status": "not_started"
  },
  "recovery": {
    "l1_attempts": 0,
    "l2_attempts": 0,
    "l3_attempts": 0,
    "recent_blocker": false
  },
  "context": {
    "active_stacks": [],
    "last_decision": null
  },
  "control_point": {
    "active": false,
    "reason": null,
    "ts": null
  },
  "meta": {
    "last_written": null,
    "last_archived": null
  }
}
```

## Acceptance Criteria
- [ ] `templates/project/HANDOFF.json` exists với v2 schema
- [ ] All 6 top-level keys present: version, position, recovery, context, control_point, meta
- [ ] `position.sub_task` field exists (new in v2)
- [ ] `context.active_stacks` array exists (new in v2)
- [ ] `control_point.*` object exists (new in v2)
- [ ] crystallize Step 9 generates HANDOFF.json (verify instruction references template or inline schema)

## Do / Don't
### Do
- Check `templates/project/HANDOFF.json` exists before deciding create vs modify
- Keep version field as integer (not string)

### Don't
- Không thêm project-specific data vào template — all values null/0/false/[]

## Verification
```yaml
manual:
  - description: "JSON syntax valid (parse test)"
    required: true
  - description: "crystallize generates HANDOFF.json with v2 schema on new project"
    required: false
```

## State Update Checklist
- [ ] Update PHASE-STATE.md task 1.6 → done
- [ ] Update TRACKER.md + HANDOFF.json
- [ ] Git commit: `feat(templates): HANDOFF.json v2 schema (position.sub_task, recovery.*, context.active_stacks, control_point)`

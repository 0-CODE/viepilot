# Task 1.4: Refactor TASK.md → v2 metadata fields

## Meta
- **Phase**: 01-foundation
- **Status**: not_started
- **Complexity**: M
- **Dependencies**: None
- **Git Tag**: vp2-p1-t1.4

## Objective

Refactor `templates/phase/TASK.md` để thêm `## Task Metadata` section với 5 new fields:
- `type` (build/fix/refactor/test/docs/infra)
- `write_scope` (list of files/dirs)
- `recovery_budget` (S/M/L/XL)
- `can_parallel_with` (list of task IDs)
- `recovery_overrides` (L1/L2/L3 block booleans)

## Paths
```yaml
files_to_modify:
  - templates/phase/TASK.md
```

## File-Level Plan
- `templates/phase/TASK.md`: Thêm `## Task Metadata` section ngay sau `## Meta` section. Chứa YAML block với tất cả 5 new fields và inline comments giải thích. `recovery_overrides` phải có L3.block + L3.reason sub-fields. Tất cả values là `{{PLACEHOLDER}}` để crystallize/evolve populate.

## Context Required
```yaml
files_to_read:
  - docs/brainstorm/session-2026-04-02.md  # Topic 11: TASK.md v2 schema
  - .viepilot/SYSTEM-RULES.md#quality_gates  # Recovery budget table
```

## Task Metadata Block Format
```yaml
## Task Metadata

type: "{{TASK_TYPE}}"              # build | fix | refactor | test | docs | infra
complexity: "{{COMPLEXITY}}"       # S | M | L | XL (maps to recovery budget)
write_scope:
  - "{{WRITE_SCOPE_PATH_1}}"       # Files/dirs this task may modify
  # - "{{WRITE_SCOPE_PATH_2}}"
recovery_budget: "{{COMPLEXITY}}"  # S: L1x1,L2x1 | M: L1x1,L2x2 | L: L1x2,L2x2,L3x1 | XL: L1x2,L2x3,L3x1
can_parallel_with: []              # Task IDs that can run concurrently (Post-MVP)
recovery_overrides:
  L1:
    block: false                   # true = skip L1 recovery entirely
  L2:
    block: false                   # true = skip L2 recovery entirely
  L3:
    block: false                   # true = never attempt scope reduction for this task
    reason: ""                     # Required when block: true — explain why
```

## Acceptance Criteria
- [ ] `## Task Metadata` section present after `## Meta`
- [ ] All 5 fields present: type, complexity, write_scope, recovery_budget, can_parallel_with
- [ ] `recovery_overrides` has L1, L2, L3 sub-blocks each with `block` boolean
- [ ] L3 has `reason` field
- [ ] Inline comments explain each field
- [ ] Recovery budget table referenced (comment or note)
- [ ] Existing TASK.md sections preserved (Objective, Paths, Acceptance Criteria, etc.)
- [ ] All values are `{{PLACEHOLDER}}` — not hardcoded

## Do / Don't
### Do
- Add recovery_overrides.L3.reason as required-when-block-true (comment explains this)
- Keep existing TASK.md structure intact; only add new section

### Don't
- Không make any field required at the template level (vp-auto gracefully defaults)
- Không thay đổi `## State Update Checklist` section

## Verification
```yaml
manual:
  - description: "YAML block syntax valid"
    required: true
  - description: "Recovery budget mapping comment accurate (S/M/L/XL attempt counts)"
    required: true
```

## State Update Checklist
- [ ] Update PHASE-STATE.md task 1.4 → done
- [ ] Update TRACKER.md + HANDOFF.json
- [ ] Git commit: `feat(templates): TASK.md v2 metadata (type, write_scope, recovery_budget, recovery_overrides)`

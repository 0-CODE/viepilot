# Task 1.3: Refactor PHASE-STATE.md → execution_state block + sub-task tracking

## Meta
- **Phase**: 01-foundation
- **Status**: not_started
- **Complexity**: M
- **Dependencies**: None
- **Git Tag**: vp2-p1-t1.3

## Objective

Refactor `templates/phase/PHASE-STATE.md` để thêm:
1. `execution_state:` YAML block với typed transitions
2. Sub-task tracking table (bên cạnh task-level table đã có)

## Paths
```yaml
files_to_modify:
  - templates/phase/PHASE-STATE.md
```

## File-Level Plan
- `templates/phase/PHASE-STATE.md`: Thêm `## Execution State` section ngay sau `## Overview`. Section này chứa YAML block với `current_state`, `available_transitions`, và `recovery_budget`. Thêm `## Sub-task Tracking` section sau `## Task Status` table với columns: sub-task, description, status, attempts.

## Context Required
```yaml
files_to_read:
  - .viepilot/ARCHITECTURE.md#data-flow
  - docs/brainstorm/session-2026-04-02.md  # Topic 7: typed transitions design
```

## Execution State Block Format
```yaml
## Execution State

execution_state:
  current: "{{CURRENT_TASK_ID}}"        # e.g., task_1_1
  status: not_started                   # not_started | executing | recovering_l1 | recovering_l2 | recovering_l3 | control_point | pass | skip
  available_transitions:
    on_start:     "→ executing"
    on_pass:      "→ update_state → next_task"
    on_fail_l1:   "→ recovering_l1 (max {{L1_MAX}} attempts) → re_verify"
    on_fail_l2:   "→ recovering_l2 (max {{L2_MAX}} attempts) → re_verify"
    on_fail_l3:   "→ recovering_l3 (max 1 attempt) → re_verify"
    on_exhausted: "→ control_point"
  recovery_budget:
    l1_used: 0
    l2_used: 0
    l3_used: 0
```

## Acceptance Criteria
- [ ] `execution_state:` YAML block present in template
- [ ] `status` field has all valid states listed as comment
- [ ] `available_transitions` covers all recovery paths
- [ ] Sub-task tracking table present
- [ ] Existing Task Status table preserved
- [ ] Template backward-compatible (old PHASE-STATE.md without this block still usable)

## Do / Don't
### Do
- Keep existing `## Task Status` table intact
- Add execution_state block between Overview and Task Status

### Don't
- Không xóa bất kỳ existing section nào
- Không make execution_state required — Phase 2 sẽ populate it; v1 phases gracefully skip

## Verification
```yaml
manual:
  - description: "YAML block syntax valid (no parse errors)"
    required: true
  - description: "All 8 status values documented"
    required: true
```

## State Update Checklist
- [ ] Update PHASE-STATE.md task 1.3 → done
- [ ] Update TRACKER.md + HANDOFF.json
- [ ] Git commit: `feat(templates): PHASE-STATE.md execution_state block + sub-task tracking`

# Task 1.1: Refactor TRACKER.md template → index ≤30 dòng

## Meta
- **Phase**: 01-foundation
- **Status**: not_started
- **Complexity**: S
- **Dependencies**: None
- **Git Tag**: vp2-p1-t1.1

## Objective

Refactor `templates/project/TRACKER.md` từ blob template (chứa Decision Log, Version History, Blockers inline) thành index-only template ≤30 dòng với pointers tới `logs/` files.

## Paths
```yaml
files_to_modify:
  - templates/project/TRACKER.md
```

## File-Level Plan
- `templates/project/TRACKER.md`: Remove Decision Log table, Version History table, Blockers section. Replace với pointers tới `logs/decisions.md`, `logs/blockers.md`, `logs/version-history.md`. Keep: Current State, Progress table, Next Action. Total ≤30 dòng.

## Context Required
```yaml
files_to_read:
  - .viepilot/ARCHITECTURE.md#module-architecture
  - .viepilot/SYSTEM-RULES.md#coding_rules
  - .viepilot/TRACKER.md  # reference: cách .viepilot/TRACKER.md của chính project này trông như thế nào
```

## Acceptance Criteria
- [ ] Template rendered ≤30 dòng (không đếm blank lines)
- [ ] Có pointers tới 3 logs/ files
- [ ] Không còn Decision Log table inline
- [ ] Không còn Version History table inline
- [ ] Current State + Progress + Next Action sections vẫn present
- [ ] `{{PLACEHOLDER}}` format unchanged

## Do / Don't
### Do
- Dùng `.viepilot/TRACKER.md` của project này làm reference cho format mới
- Giữ `## Current State` và `## Progress` sections
- Add comment explaining logs/ lazy-loading strategy

### Don't
- Không xóa tất cả context — vẫn cần Phase progress table
- Không hardcode paths — giữ `{{LOG_DIR}}` placeholder nếu cần

## Verification
```yaml
manual:
  - description: "Count non-blank lines in rendered template"
    required: true
  - description: "Check crystallize generates TRACKER.md with correct format"
    required: false
```

## State Update Checklist
- [ ] Update `.viepilot/phases/01-foundation/PHASE-STATE.md` task 1.1 → done
- [ ] Update `.viepilot/TRACKER.md` current task
- [ ] Update `.viepilot/HANDOFF.json` position
- [ ] Git commit: `refactor(templates): TRACKER.md → index ≤30 lines + logs/ pointers`

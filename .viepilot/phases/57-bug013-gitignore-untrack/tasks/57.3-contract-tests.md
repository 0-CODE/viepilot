# Task 57.3 — Contract tests for BUG-013 GITIGNORE-AWARE STAGING RULE

## Goal
Add `tests/unit/vp-bug013-gitignore-staging-rule.test.js` with 3 contract tests verifying `autonomous.md` contains the required rule text.

## Paths
```
tests/unit/vp-bug013-gitignore-staging-rule.test.js
```

## Tests to write

```js
describe('BUG-013 GITIGNORE-AWARE STAGING RULE — never stage gitignored files', () => {

  test('autonomous.md: has GITIGNORE-AWARE STAGING RULE (BUG-013) block in commit section')
  // Expect: /⛔.*GITIGNORE-AWARE STAGING RULE.*BUG-013/
  // Expect: text contains "git check-ignore"
  // Expect: text contains ".viepilot/"

  test('autonomous.md: staging rule appears after git add in commit block')
  // Expect: BUG-013 rule position is after the "git add {relevant files}" text

  test('autonomous.md: git persistence gate has BUG-013 note about ?? lines')
  // Expect: "BUG-013" appears near the porcelain gate section
  // Expect: text near BUG-013 contains "??" referencing untracked lines

})
```

## Acceptance Criteria
- [ ] All 3 tests pass
- [ ] `npm test` green — no regression on BUG-012, BUG-009, or other existing tests
- [ ] Test file uses same `read(relPath)` helper pattern as other contract tests

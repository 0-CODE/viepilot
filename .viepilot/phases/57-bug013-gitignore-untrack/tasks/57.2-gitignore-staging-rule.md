# Task 57.2 — Add GITIGNORE-AWARE STAGING RULE to autonomous.md

## Goal
Add `⛔ GITIGNORE-AWARE STAGING RULE (BUG-013)` to `workflows/autonomous.md` in two places:
1. **Commit block** (after `git add {relevant files}`) — never stage gitignored files
2. **Git Persistence Gate** — porcelain check ignores `??` lines (untracked-only is CLEAN)

## Paths
```
workflows/autonomous.md
```

## Implementation Notes

### Insert 1 — After the `git add {relevant files}` line (around line 232)

Replace the atomic commit block:
```markdown
6. Atomic commits per logical unit:
   ```bash
   git add {relevant files}
   git commit -m "{type}({scope}): {description}"
   git push
   ```
```

With:
```markdown
6. Atomic commits per logical unit:
   ```bash
   git add {relevant files}
   git commit -m "{type}({scope}): {description}"
   git push
   ```

   ⛔ **GITIGNORE-AWARE STAGING RULE (BUG-013)**: Before staging any file, check if it is
   gitignored. NEVER stage or commit gitignored files — they are internal state, not
   shipping artifacts.
   ```bash
   # Check before staging:
   git check-ignore -q {file} && echo "IGNORED — skip" || git add {file}
   ```
   `.viepilot/` is ALWAYS gitignored in ViePilot repos. Never run `git add .viepilot/`.
```

### Insert 2 — In the Git Persistence Gate block, after the porcelain check description

After the line:
```
# Must have no unstaged/staged residue for this task
git status --porcelain
```

Add:
```markdown
# NOTE (BUG-013): `??` lines in porcelain output are UNTRACKED files — they are NOT
# a dirty state. The gate only fails when tracked files have modifications (M, A, D).
# Gitignored files (like .viepilot/) must never be staged, so they appear as ?? and
# should NOT cause a gate failure.
# Porcelain is CLEAN when output contains ONLY ?? lines or is completely empty.
```

## Acceptance Criteria
- [ ] `autonomous.md` has `⛔ GITIGNORE-AWARE STAGING RULE (BUG-013)` text in commit block
- [ ] Block contains `git check-ignore` usage guidance
- [ ] Block explicitly states `.viepilot/` must never be staged
- [ ] Git persistence gate section has BUG-013 note about `??` lines being acceptable
- [ ] No existing BUG-012 or BUG-003 text removed

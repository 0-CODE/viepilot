# Task 21.3: Map v2 artifacts to v3 ownership and projections

## Meta
- **Phase**: 21-planning-source-compiler-boundary
- **Status**: done
- **Complexity**: M
- **Dependencies**: 21.1, 21.2
- **Git Tag**: `${TAG_PREFIX}-p21-t21.3`

## Task Metadata

```yaml
type: "build"
complexity: "M"
write_scope:
  - ".viepilot/HANDOFF.json"
  - ".viepilot/PROJECT-CONTEXT.md"
  - ".viepilot/ARCHITECTURE.md"
  - ".viepilot/SPEC.md"
  - ".viepilot/compat-map.json"
  - ".viepilot/schemas/compat-map.schema.json"
  - ".viepilot/phases/21-planning-source-compiler-boundary/PHASE-STATE.md"
  - ".viepilot/phases/21-planning-source-compiler-boundary/tasks/21.3.md"
  - "tests/unit/vp-phase21-compat-map-contracts.test.js"
recovery_budget: "M"
can_parallel_with: []
worktree_isolation: "auto"
recovery_overrides:
  L1:
    block: false
  L2:
    block: false
  L3:
    block: false
    reason: ""
```

## Objective

Classify the current `.viepilot` artifact set against the v3 model so canonical inputs, runtime/projection outputs, reference files, and migration-only leftovers have explicit ownership boundaries.

## Context Required

- `.viepilot/PROJECT-CONTEXT.md`
- `.viepilot/ROADMAP.md`
- `.viepilot/ARCHITECTURE.md`
- `.viepilot/SPEC.md`
- `.viepilot/schemas/planning-source.schema.json`
- `.viepilot/schemas/planning-source-inventory.md`
- `.viepilot/schemas/runtime-state.schema.json`
- `.viepilot/schemas/execution-graph.schema.json`
- `.viepilot/schemas/active-packet.schema.json`
- `.viepilot/schemas/runtime-artifact-contracts.md`
- `docs/brainstorm/session-2026-04-04.md`

## Paths

- `.viepilot/HANDOFF.json`
- `.viepilot/PROJECT-CONTEXT.md`
- `.viepilot/ARCHITECTURE.md`
- `.viepilot/SPEC.md`
- `.viepilot/compat-map.json`
- `.viepilot/schemas/compat-map.schema.json`
- `.viepilot/phases/21-planning-source-compiler-boundary/PHASE-STATE.md`
- `.viepilot/phases/21-planning-source-compiler-boundary/tasks/21.3.md`
- `tests/unit/vp-phase21-compat-map-contracts.test.js`

## File-Level Plan

- `.viepilot/compat-map.json`: add a machine-readable ownership map that classifies current `.viepilot` files by ordered rules and points each class to its v3 owner or disposition.
- `.viepilot/schemas/compat-map.schema.json`: define the schema for the compatibility map artifact so later compiler work can validate it.
- `.viepilot/PROJECT-CONTEXT.md`: name `compat-map.json` as the ownership and migration bridge between current repo artifacts and future canonical runtime/planning artifacts.
- `.viepilot/ARCHITECTURE.md`: add `compat-map.json` to the planning/compiler boundary and keep projections versus canonical ownership explicit.
- `.viepilot/SPEC.md`: register the compatibility map as a domain entity / contract reference beside planning source and runtime artifacts.
- `tests/unit/vp-phase21-compat-map-contracts.test.js`: verify schema parse, key doc references, and full `.viepilot` coverage with exactly one compat-map rule per file.

## Implementation Notes

- Use ordered classification rules so exact-path entries can override broad prefixes or regex groups without ambiguity.
- Keep the map focused on ownership and disposition, not implementation behavior. Phase 22 will decide how projections are rendered; this task only states who owns truth.
- Treat `TRACKER.md`, `ROADMAP.md`, `ROADMAP-INDEX.md`, `HANDOFF.json`, and active `PHASE-STATE.md` files as compatibility or projection outputs rather than canonical runtime truth.
- Treat phase/task/request markdown and brainstorm-derived artifacts as compiler/planning inputs until `planning-source.json` replaces them.
- Preserve room for legacy and audit-only files by labeling them `migration_only` or `reference` rather than forcing them into canonical runtime ownership.

## Acceptance Criteria

- [ ] `.viepilot/compat-map.json` exists and parses against a checked-in schema.
- [ ] Current `.viepilot` files are classified by explicit ordered rules with no uncovered paths.
- [ ] Context/architecture/spec language all reference the compatibility map consistently.
- [ ] Projection files remain clearly non-canonical relative to planning source and runtime artifacts.

## Best Practices

- [ ] Prefer machine-readable ownership map before prose-only explanation.
- [ ] Keep rule precedence explicit so path coverage is deterministic.
- [ ] Separate canonical owners from compatibility projections and migration leftovers.
- [ ] Preserve local-first, CommonJS-friendly verification without adding new dependencies.

## Verification

- `node -e "const fs=require('fs'); JSON.parse(fs.readFileSync('.viepilot/schemas/compat-map.schema.json','utf8')); JSON.parse(fs.readFileSync('.viepilot/compat-map.json','utf8')); console.log('compat map artifacts valid')"` -> exits 0 and prints validation message.
- `npm test -- --runInBand tests/unit/vp-phase21-compat-map-contracts.test.js` -> exits 0 and the compat-map contract test passes.
- `rg -n "compat-map\\.json|compatibility map" .viepilot/PROJECT-CONTEXT.md .viepilot/ARCHITECTURE.md .viepilot/SPEC.md` -> shows consistent ownership-map references.

## Expected Outcome

- Phase 21 ends with an explicit bridge from current v2-era `.viepilot` artifacts to future v3 canonical ownership and projection rules.
- Phase 22 can compile and render projections with a concrete compatibility target instead of inferring one from prose.

## Progress Notes

- Task contract was expanded to pass the doc-first gate before implementation started.
- Added `.viepilot/compat-map.json` and `.viepilot/schemas/compat-map.schema.json` to classify current `.viepilot` artifacts by ordered ownership rules.
- Added Jest coverage to ensure every current `.viepilot` file matches exactly one compatibility-map rule.

## Verification Results

- Compat-map JSON/schema parse: pass
- Jest contract test (`tests/unit/vp-phase21-compat-map-contracts.test.js`): pass
- Reference sync check (`rg -n "compat-map\\.json|compatibility map"` across project context, architecture, and spec): pass

## Control Point

- Implementation is ready locally, but the task is paused at the git persistence gate.
- Start tag created: `viepilot-vp-p21-t21.3`
- PASS is intentionally withheld until there is an explicit decision on commit/push behavior for these changes.
- Git persistence is satisfied once the task commit is pushed to `origin/v2`; this task can then be marked PASS.

# Task 96.3 — ENH-063 Crystallize Side: Admin YAML Export + Template Section

## Objective
Thêm crystallize export cho `## admin` YAML từ architect `notes.md` → `## Admin & Governance` table vào `PROJECT-CONTEXT.md`. Thêm `## Admin & Governance` section vào project template. Update `skills/vp-crystallize/SKILL.md`.

## Paths
- `workflows/crystallize.md`
- `templates/project/PROJECT-CONTEXT.md`
- `skills/vp-crystallize/SKILL.md`

## Implementation Notes

### workflows/crystallize.md — Step 1D extension: admin export

Trong Step 1D (Consume Architect Artifacts), sau item 6 (`apis.html` export), thêm item 7:

```markdown
7. **`admin.html` / `notes.md ## admin`** (if exists — ENH-063) → append to `.viepilot/PROJECT-CONTEXT.md`:
   ```markdown
   ## Admin & Governance (from Architect Mode)
   | Capability | Required | Phase | Notes |
   |-----------|----------|-------|-------|
   | User management UI | yes | 2 | invite, deactivate, role assign |
   | Audit log | yes | 3 | all write operations, 90-day retention |
   | Monitoring dashboard | optional | 3 | error rate, latency, active users |
   | Billing management | no | — | not in scope |

   ### Admin Personas
   | Persona | Key Capabilities |
   |---------|-----------------|
   | super_admin | user_management, billing, system_config, audit_log_view |
   | org_admin | invite_users, role_assign, reporting |
   ```
```

Renumber existing item 7 (feature-map cross-reference) → item 8.
Renumber item 8 (Record in working notes) → item 9.

Add to "Record in working notes":
```
- `admin_imported`: {true/false}
- `admin_capabilities_count`: {count if admin present, else "n/a"}
```

### templates/project/PROJECT-CONTEXT.md — Add ## Admin & Governance section

Find the end of the file (or after `## Skills` section if present). Append:

```markdown
## Admin & Governance

> Generated by crystallize from architect notes.md ## admin (ENH-063).
> Update manually or re-run /vp-crystallize after updating brainstorm artifacts.

| Capability | Required | Phase | Notes |
|-----------|----------|-------|-------|
| User management UI | — | — | — |
| Monitoring dashboard | — | — | — |
| Audit log | — | — | — |
| Billing management | — | — | — |

### Admin Personas
| Persona | Key Capabilities |
|---------|-----------------|
| — | — |
```

### skills/vp-crystallize/SKILL.md — Append Step 1D admin export note

Thêm vào section documenting Step 1D:

```markdown
### Admin & Governance Export (ENH-063)
If `admin.html` or `notes.md ## admin` exists in architect workspace:
- Append `## Admin & Governance` table to `.viepilot/PROJECT-CONTEXT.md`
- Columns: Capability | Required | Phase | Notes
- Append Admin Personas table
```

## Verification
```bash
grep -c "Admin & Governance" workflows/crystallize.md           # ≥1
grep -c "admin.html" workflows/crystallize.md                   # ≥1
grep -c "Admin & Governance" templates/project/PROJECT-CONTEXT.md  # ≥1
grep -c "ENH-063" workflows/crystallize.md                      # ≥1
```

## Acceptance Criteria
- [ ] `workflows/crystallize.md` Step 1D: exports `notes.md ## admin` → `## Admin & Governance` in PROJECT-CONTEXT.md
- [ ] Admin Personas table exported
- [ ] `admin_imported` and `admin_capabilities_count` in working notes record
- [ ] `templates/project/PROJECT-CONTEXT.md`: `## Admin & Governance` section added with placeholder rows
- [ ] `skills/vp-crystallize/SKILL.md`: Step 1D admin export documented
- [ ] Existing Step 1D items correctly renumbered (no numbering conflict)

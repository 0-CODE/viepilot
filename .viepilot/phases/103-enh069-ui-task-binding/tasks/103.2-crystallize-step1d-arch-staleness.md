# Task 103.2 — crystallize Step 1D: arch_to_ui_sync noted + feature-map resolution + design staleness

## Meta
- **Phase**: 103
- **Task**: 103.2
- **Status**: planned
- **Complexity**: M
- **Gaps Addressed**: Gap 5, Gap 7, Gap 8
- **Req**: ENH-069

## Objective
Extend crystallize Step 1D (reads `notes.md` architect data) to:
1. **Gap 5**: Convert `arch_to_ui_sync` entries with `status: noted` into UI Pages → Component Map rows (if screen not already covered by pages/*.html).
2. **Gap 7**: Require explicit resolution of `feature-map.html` ↔ `phases_inventory` discrepancies — not just "list for confirm" — before Step 7 runs.
3. **Gap 8**: Cross-check architectural decisions (decisions[]) against pages/*.html to detect "design staleness" and emit a warning when a decision with a UI implication is not reflected in the prototype.

## Paths
```
workflows/crystallize.md
```

## File-Level Plan

### `workflows/crystallize.md` — Step 1D additions

#### Sub-step 1D-a: arch_to_ui_sync noted → Component Map (Gap 5)

In Step 1D, after reading `notes.md → ## arch_to_ui_sync`:
```
For each arch_to_ui_sync entry with status: noted:
  - Check if the referenced ui_implication (screen/state/component) exists in pages/*.html
  - If NOT: add a row to UI Pages → Component Map with:
      Prototype: [no page yet — arch_to_ui_sync source]
      Target component: {inferred component path}
      Phase: {inferred from phases_inventory or arch decision phase}
      Source: arch_to_ui_sync
      Status: pending
  - If YES: no action needed (already covered by page binding from Step 1A)
```

#### Sub-step 1D-b: feature-map discrepancy explicit resolution (Gap 7)

Replace current "list for confirm" behavior in Step 1D item 11 with:
```
For each feature-map.html ↔ phases_inventory discrepancy found:
  MUST be resolved before Step 7 with one of:
    (a) add-to-inventory: add feature to phases_inventory and mark for ROADMAP task
    (b) descoped: mark feature as removed/descoped with reason in working notes
    (c) design-only: flag as design artifact only (no implementation task)
  Log resolution in working notes as:
    ## Feature-Map Resolutions
    | Feature | Discrepancy | Resolution | Reason |
  Step 7 is BLOCKED until all discrepancies are resolved (no silent continuation)
```

#### Sub-step 1D-c: design staleness check (Gap 8)

After reading `notes.md → decisions[]` and having pages/*.html content from Step 1A:
```
For each decision in decisions[] that has a ui_implication:
  - Cross-check: does the relevant page/*.html visually represent this decision?
  - Heuristic: search for keywords from the ui_implication in the page's HTML
  - If NOT represented:
      Emit ⚠️ Design Staleness Warning:
        "Decision '{title}' (ui_implication: {text}) is not reflected in {page}.html"
      Add a task to working notes: "update {page}.html to reflect {decision}" 
      This task precedes the component implementation task in the UI Pages → Component Map
```

## Acceptance Criteria
- [ ] `grep -c "arch_to_ui_sync" workflows/crystallize.md` ≥ 3 (read + action + component-map update)
- [ ] `grep -c "design-only\|Design-only\|design_only" workflows/crystallize.md` ≥ 1
- [ ] `grep -c "Feature-Map Resolutions\|feature-map.*resolution\|discrepan" workflows/crystallize.md` ≥ 2
- [ ] `grep -c "Design Staleness\|design staleness\|staleness" workflows/crystallize.md` ≥ 2
- [ ] `grep -c "BLOCKED until" workflows/crystallize.md` ≥ 1

## Verification Commands
```bash
grep -c "arch_to_ui_sync" workflows/crystallize.md
grep -c "Design Staleness" workflows/crystallize.md
grep -c "BLOCKED until" workflows/crystallize.md
```

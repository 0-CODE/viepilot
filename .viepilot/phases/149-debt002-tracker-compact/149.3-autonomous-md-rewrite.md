# Task 149.3 — workflows/autonomous.md: rewrite-not-append + size guard + Decision Log cap

## Objective
Change `workflows/autonomous.md` so that:
1. **Step 1 Initialize**: auto-compact TRACKER.md before reading it if the file exceeds 400 lines
2. **Step 5 Phase Complete / tracker-agent**: instruct rewrite (find-and-replace) of the
   Current State block — not append — so TRACKER.md never grows past ~60 lines
3. **Decision Log**: tracker-agent is instructed to cap the table at 20 rows (archive older)

## Paths
- workflows/autonomous.md

## Implementation Notes

### Change 1 — Step 1: size guard before TRACKER.md read

Find the `Load context:` block near line 74:
```markdown
Load context:
```bash
cat .viepilot/AI-GUIDE.md
cat .viepilot/TRACKER.md
```
```

**Replace** with:
```markdown
Load context:
```bash
cat .viepilot/AI-GUIDE.md

# Size guard — auto-compact if TRACKER.md is bloated (> 400 lines)
if [ -f ".viepilot/TRACKER.md" ] && [ "$(wc -l < .viepilot/TRACKER.md)" -gt 400 ]; then
  echo "⚠️ TRACKER.md is large ($(wc -l < .viepilot/TRACKER.md) lines) — auto-compacting..."
  node bin/vp-tools.cjs tracker compact --keep 5
fi

cat .viepilot/TRACKER.md
```
```

### Change 2 — Step 5 tracker-agent: rewrite-not-append

Find the tracker-agent spawn block at step 6 (around line 906):
```markdown
6. Update TRACKER.md current state — spawn tracker-agent:
   ```
   Agent({ subagent_type: "tracker-agent",
     description: "Update TRACKER.md — phase {N} complete",
     prompt: "operation: update-current-state. data: Last completed phase {N} — {phase_name}. version: {version}."
   })
   ```
```

**Replace** with:
```markdown
6. Update TRACKER.md current state — spawn tracker-agent:
   ```
   Agent({ subagent_type: "tracker-agent",
     description: "Update TRACKER.md — phase {N} complete",
     prompt: "operation: rewrite-current-state. data: Last completed phase {N} — {phase_name}. version: {version}. IMPORTANT: REPLACE the entire '## Current State' section (find the section, overwrite it) — do NOT append new lines. The section must stay ≤ 20 lines total. Also cap '## Decision Log' table at 20 rows — if > 20 rows exist, archive older rows to TRACKER-HISTORY.md."
   })
   ```
```

**Key change:** `operation: update-current-state` → `operation: rewrite-current-state` with explicit
instruction to replace (not append) and cap Decision Log.

### Change 3 — Add tracker compact to the help/toolbox section (optional prose note)

Near the tracker-agent section or the vp-tools command list, add a note:
> Rescue bloated TRACKER.md: `node bin/vp-tools.cjs tracker compact [--keep N]`

## Acceptance Criteria
- [ ] `grep "rewrite-current-state" workflows/autonomous.md` → ≥ 1 hit
- [ ] `grep "auto-compacting" workflows/autonomous.md` → ≥ 1 hit (size guard)
- [ ] `grep "cap.*Decision Log\|Decision Log.*cap\|20 rows" workflows/autonomous.md` → ≥ 1 hit
- [ ] `grep "update-current-state" workflows/autonomous.md` → 0 hits (old operation removed)
- [ ] No other content in autonomous.md changed (surgical edit only)

## Best Practices
- Surgical edits only — find exact strings, replace minimal context
- Do not reformat surrounding content

## Verification
```bash
grep -n "rewrite-current-state\|auto-compacting\|Decision Log.*20" workflows/autonomous.md
```

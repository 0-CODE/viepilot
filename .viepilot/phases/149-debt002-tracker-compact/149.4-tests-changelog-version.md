# Task 149.4 — Contract tests + CHANGELOG [3.12.0] + version bump

## Objective
Write contract tests for DEBT-002 deliverables, bump version to 3.12.0, and add CHANGELOG entry.
Verifies: `lib/tracker-compact.cjs` exports, CLI subcommand, autonomous.md rewrite-not-append
instruction, and version number.

## Paths
- tests/unit/phase149-debt002-tracker-compact.test.js
- CHANGELOG.md
- package.json

## Dependencies
- Tasks 149.1, 149.2, 149.3 must all be complete first

## File-Level Plan

### tests/unit/phase149-debt002-tracker-compact.test.js (new)

Test groups:

**A) lib/tracker-compact.cjs exports**
- `require('./lib/tracker-compact.cjs')` has `compact` and `rewriteCurrentState` exports
- `compact` is a function
- `rewriteCurrentState` is a function

**B) rewriteCurrentState behavior**
- Given content with `## Current State\nold stuff\n## Next Section`, calling
  `rewriteCurrentState(content, '## Current State\nnew stuff\n')` replaces the block
- Result contains `new stuff`
- Result does NOT contain `old stuff`
- Result still contains `## Next Section`

**C) compact dry-run**
- Create a temp TRACKER.md with > 400 lines (Current State with 100 stanzas + Decision Log with 50 rows)
- Call `compact(path, { keep: 5, dryRun: true })`
- Returns object with `{ linesRemoved, rowsArchived, trackerLines }` all numbers
- No files written (TRACKER-HISTORY.md does not exist after dry-run)
- Original TRACKER.md unchanged

**D) compact live run**
- Same temp file
- Call `compact(path, { keep: 5 })`
- TRACKER.md line count < 100 after compact
- TRACKER-HISTORY.md exists and contains archived content
- No data lost: original lines ≈ new TRACKER.md lines + new TRACKER-HISTORY.md section lines

**E) bin/vp-tools.cjs tracker compact subcommand**
- `fs.readFileSync('bin/vp-tools.cjs', 'utf8')` contains `tracker` and `compact`
- Source contains `tracker-compact` (require reference)

**F) autonomous.md rewrite-not-append contract**
- `fs.readFileSync('workflows/autonomous.md', 'utf8')` contains `rewrite-current-state`
- Does NOT contain `update-current-state` (old operation removed)
- Contains `auto-compact` or `auto-compacting`
- Contains Decision Log cap instruction (`20 rows` or `cap.*Decision Log`)

**G) version = 3.12.0**
- `require('./package.json').version === '3.12.0'`

### CHANGELOG.md (modify)
Add `## [3.12.0] - 2026-05-25` section above existing [3.11.0]:
```markdown
## [3.12.0] - 2026-05-25
### Changed
- `workflows/autonomous.md`: tracker-agent now rewrites Current State block (find-and-replace)
  instead of appending — TRACKER.md stays ≤ 20 lines regardless of phase count (DEBT-002)
- `workflows/autonomous.md`: auto-compact guard — if TRACKER.md > 400 lines, runs
  `vp-tools tracker compact` before reading (DEBT-002)
- `workflows/autonomous.md`: Decision Log capped at 20 rows; older rows archived to TRACKER-HISTORY.md

### Added
- `lib/tracker-compact.cjs`: compact() + rewriteCurrentState() — rescue tool for bloated TRACKER.md files
- `bin/vp-tools.cjs`: `tracker compact [--keep N]` subcommand
```

### package.json (modify)
- `"version": "3.11.0"` → `"version": "3.12.0"`

## Acceptance Criteria
- [ ] All tests in phase149 test file pass
- [ ] `node -e "console.log(require('./package.json').version)"` → `3.12.0`
- [ ] `grep "\[3.12.0\]" CHANGELOG.md` → ≥ 1 hit
- [ ] Full test suite passes (all existing tests still green)
- [ ] Git clean + pushed before PASS

## Verification
```bash
npx jest tests/unit/phase149-debt002-tracker-compact.test.js
node -e "console.log(require('./package.json').version)"
grep "\[3.12.0\]" CHANGELOG.md
npx jest
```

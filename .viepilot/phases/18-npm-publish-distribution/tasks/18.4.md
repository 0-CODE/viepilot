# Task 18.4 — Add post-publish smoke verification flow

## Objective
Define and automate smoke tests to verify npm package integrity after publish.

## Paths
| Path | Action |
|------|--------|
| `scripts/` (optional) | Add/Modify |
| `docs/dev/deployment.md` | Modify |

## Implementation notes
- Validate install path from npm registry (`npx viepilot install --help`).
- Check dist-tag/version resolution and package tarball contents.
- Capture quick failure triage steps if smoke tests fail.

## Verification
```bash
npx viepilot --help
npx viepilot install --help
```

## State update checklist
- [ ] Update PHASE-STATE to `done` when merged.

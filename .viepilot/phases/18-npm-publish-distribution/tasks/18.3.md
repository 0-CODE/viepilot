# Task 18.3 — Add secure npm release workflow (token-based)

## Objective
Implement secure publish workflow using NPM token secrets without exposing credentials.

## Paths
| Path | Action |
|------|--------|
| `.github/workflows/*` | Add/Modify |
| `docs/dev/deployment.md` | Modify (if needed) |

## Implementation notes
- Use `NPM_TOKEN` secret and minimal permission scope.
- Ensure workflow logs do not print sensitive values.
- Gate publish behind explicit trigger/tag or release event.

## Verification
```bash
# Validate workflow syntax and required secrets references
```

## State update checklist
- [x] Update PHASE-STATE to `done` when merged.

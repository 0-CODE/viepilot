# Task 18.1 — Harden package metadata for npm publish

## Objective
Ensure npm package metadata and published payload are clean, minimal, and accurate for public distribution.

## Paths
| Path | Action |
|------|--------|
| `package.json` | Modify |
| `.npmignore` or `files` field | Add/Modify |

## Implementation notes
- Validate `name`, `bin`, `repository`, `homepage`, `license`, and keywords.
- Restrict publish artifacts to required runtime files only.
- Prevent leaking internal/planning files into npm tarball.

## Verification
```bash
npm pack --dry-run
```

## State update checklist
- [x] Update PHASE-STATE to `done` when merged.

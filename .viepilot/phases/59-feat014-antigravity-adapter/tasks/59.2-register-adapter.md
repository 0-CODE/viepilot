# Task 59.2 — Register antigravity in lib/adapters/index.cjs

## Goal
Add `'antigravity'` to the adapters map. No aliases needed (Antigravity has one product, one install path).

## Paths
```
lib/adapters/index.cjs
```

## Implementation Notes

In `lib/adapters/index.cjs`, add to the `adapters` object:

```js
const adapters = {
  'claude-code':  require('./claude-code.cjs'),
  'cursor':       require('./cursor.cjs'),
  'cursor-agent': require('./cursor.cjs'),   // alias
  'cursor-ide':   require('./cursor.cjs'),   // alias
  'antigravity':  require('./antigravity.cjs'),  // ← add this
};
```

Also update the `listAdapters()` deduplication comment to reflect 3 unique adapters.

## Acceptance Criteria
- [ ] `getAdapter('antigravity')` resolves without error
- [ ] `listAdapters()` returns 3 unique adapters (claude-code, cursor, antigravity)
- [ ] Existing aliases (cursor-agent, cursor-ide) still work

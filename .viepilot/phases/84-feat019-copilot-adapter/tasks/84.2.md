# Task 84.2 — `lib/viepilot-install.cjs` — add `copilot` install target

## Objective
Update the installer to support `--adapter copilot`, enabling users to install
vp-* skills into `~/.config/gh-copilot/` with a single command.

## Paths
- `lib/viepilot-install.cjs`

## Changes Required

### 1. Add `copilot` to the adapter detection / auto-detect list
The installer's adapter auto-detection logic checks `isAvailable()` for each known
adapter. Since `copilot` is now registered in `index.cjs`, it should auto-detect when
`~/.config/gh-copilot/` exists or `gh` is installed.

Verify the auto-detect loop iterates `listAdapters()` — if so, no change needed here
(copilot will auto-detect via `isAvailable`). If adapters are hardcoded, add `copilot`.

### 2. Add `copilot` to the explicit `--adapter` allowed values
If the installer validates `--adapter` against a whitelist, add `copilot`:
```js
const VALID_ADAPTERS = ['claude-code', 'cursor', 'antigravity', 'codex', 'copilot'];
```

### 3. Add copilot-specific post-install note
After install, print:
```
✓ GitHub Copilot adapter installed → ~/.config/gh-copilot/

Next: Open Copilot Chat in VS Code and type /vp-status to get started

Note: For GitHub Copilot CLI, ensure 'gh' CLI + Copilot extension are installed.
      Skills are installed to: ~/.config/gh-copilot/skills/
```

### 4. Add `copilot` to the "Next actions" adapter list
In the post-install adapter table shown to users:
```
| GitHub Copilot | ~/.config/gh-copilot/ | /vp-status in Copilot Chat |
```

## Acceptance Criteria
- [ ] `viepilot install --adapter copilot` runs without error
- [ ] Files are written to `~/.config/gh-copilot/` (or dry-run shows correct paths)
- [ ] `copilot` appears in auto-detect when `~/.config/gh-copilot/` exists
- [ ] Post-install message mentions Copilot Chat + `gh` CLI
- [ ] `copilot` listed in adapter options table

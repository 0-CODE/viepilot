# Task 76.2 — crystallize.md label fix + vp-docs SKILL.md + tests + CHANGELOG 2.13.0

## Objective
Fix the "GitHub username?" label in `workflows/crystallize.md` Step 0. Update `skills/vp-docs/SKILL.md` if it documents the old variable names. Write ≥5 tests for the URL parsing patterns. Bump version to 2.13.0.

## Paths
```
workflows/crystallize.md
skills/vp-docs/SKILL.md
tests/unit/vp-enh046-git-forge.test.js
CHANGELOG.md
package.json
```

## File-Level Plan

### `workflows/crystallize.md`
- In Step 0 (project context prompt), find: `GitHub username? (optional)` or similar
- Replace with: `Git remote / username? (optional — e.g. github.com/johndoe, gitlab.com/org)`
- If there is a `github_username:` field in the session stub template, rename to `git_remote_info:`

### `skills/vp-docs/SKILL.md`
- Grep for `GITHUB_OWNER`, `GITHUB_REPO` — if present, update to `GIT_OWNER`, `GIT_REPO`
- If not present, no change needed

### `tests/unit/vp-enh046-git-forge.test.js`
Write tests that verify `workflows/documentation.md` contains the new parsing patterns. Test as content-pattern tests (same style as other unit tests in the suite).

Minimum 5 tests:
1. `GIT_HOST` variable defined
2. `GIT_OWNER` variable defined
3. `GIT_REPO` variable defined
4. `GITHUB_OWNER` / `GITHUB_REPO` not in variable-assignment lines
5. Azure DevOps branch present (`dev\.azure\.com`)
6. SSH format branch present (`git@`)
7. HTTPS format branch present
8. `workflows/crystallize.md` Step 0 uses forge-agnostic wording (no bare "GitHub username")

### `CHANGELOG.md`
Add section above `[2.12.0]`:
```markdown
## [2.13.0] - 2026-04-13

### ENH-046: Git Forge Agnostic Remote URL Parsing
- `workflows/documentation.md`: replace GitHub-only regex with multi-forge URL parser
  (GitHub SSH/HTTPS, GitLab SSH/HTTPS, Bitbucket, Azure DevOps, Gitea/self-hosted)
- Variables renamed: `GITHUB_OWNER`/`GITHUB_REPO` → `GIT_OWNER`/`GIT_REPO`; added `GIT_HOST`
- `workflows/crystallize.md`: Step 0 label generalized from "GitHub username" to forge-agnostic wording
```

### `package.json`
- `"version": "2.12.0"` → `"version": "2.13.0"`

## Acceptance Criteria
- [ ] `workflows/crystallize.md` no longer says "GitHub username" in Step 0
- [ ] `skills/vp-docs/SKILL.md` free of old variable names (or unchanged if not referenced)
- [ ] `tests/unit/vp-enh046-git-forge.test.js` exists with ≥5 passing tests
- [ ] `CHANGELOG.md` has `[2.13.0]` section
- [ ] `package.json` version = `"2.13.0"`

## Verification
```bash
npm test -- --testPathPattern=vp-enh046
# Expected: all tests pass

grep -n "GitHub username" workflows/crystallize.md
# Expected: 0 matches
```

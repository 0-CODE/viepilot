# Task 19.3 — Add npx viepilot uninstall command

## Objective
Provide uninstall command for removing installed assets with interactive confirm and `--yes` mode.

## Paths
| Path | Action |
|------|--------|
| `bin/viepilot.cjs` | Modify |
| `install.sh` / `dev-install.sh` | Modify (if shared helper required) |

## Implementation notes
- Add command: `viepilot uninstall`.
- Remove installed skills/workflows/templates/bin safely.
- Print uninstall summary and remaining manual cleanup notes.

## Verification
```bash
node bin/viepilot.cjs uninstall --yes
```

## State update checklist
- [ ] Update PHASE-STATE to `done` when merged.

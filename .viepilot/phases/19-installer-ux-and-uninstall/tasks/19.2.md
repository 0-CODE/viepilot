# Task 19.2 — Fix symlink install mode causing missing skill detection

## Objective
Remove or harden symlink strategy so installed skills/workflows are consistently discoverable.

## Paths
| Path | Action |
|------|--------|
| `install.sh` | Modify |
| `dev-install.sh` | Modify |
| `bin/viepilot.cjs` | Modify (if installer orchestration changed) |

## Implementation notes
- Reproduce and document missing-skills case.
- Replace fragile symlink behavior with reliable copy/link strategy.
- Ensure install remains idempotent across reruns.

## Verification
```bash
# run install and verify skill paths exist and are readable
```

## State update checklist
- [ ] Update PHASE-STATE to `done` when merged.

# Task 29.1 — Brainstorm signals + crystallize diagram applicability matrix

## Objective
Bổ sung hợp đồng **complexity-gated** cho architecture diagrams: brainstorm phải thu thập tín hiệu, crystallize Step 4 phải quyết định matrix `required | optional | N/A` cho 6 loại diagram trước khi generate ARCHITECTURE.

## Paths
- `workflows/brainstorm.md` — thêm architecture signal checklist + session template section cho diagram applicability input
- `workflows/crystallize.md` — Step 4: rules tạo matrix và ràng buộc generate diagram theo matrix
- `.viepilot/phases/29-crystallize-architecture-mermaid/PHASE-STATE.md`
- `.viepilot/TRACKER.md`, `.viepilot/ROADMAP.md`, `.viepilot/HANDOFF.json`, `CHANGELOG.md` (task progress)

## File-Level Plan
- **Brainstorm**: dưới topic Architecture, thêm câu hỏi bắt buộc: số service/module, event-driven/webhook, deployment topology/env count, user journey complexity, integration boundaries.
- **Brainstorm session template**: thêm section `## Architecture diagram applicability inputs` để crystallize đọc được tín hiệu đã chốt.
- **Crystallize Step 4**: thêm matrix 6 loại (`system-overview`, `data-flow`, `event-flows`, `module-dependencies`, `deployment`, `user-use-case`) với tiêu chí và output:
  - `required` => bắt buộc có `mermaid`
  - `optional` => có thể gộp/tối giản
  - `N/A` => phải có lý do rõ trong ARCHITECTURE
- Không đổi code runtime ở task này; chỉ update workflow contract.

## Verification
- `npm test`
- `rg "diagram applicability|required|N/A|event-flows|module-dependencies" workflows/brainstorm.md workflows/crystallize.md`

## Status
done (2026-04-01)

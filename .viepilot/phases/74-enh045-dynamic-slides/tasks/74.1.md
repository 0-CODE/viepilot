# Task 74.1 — workflows/proposal.md: dynamic slide count + designConfig

## Goal
Remove slide hardcap; add `designConfig` to manifest; add content-aware split rules; add DESIGN SELECTION AI prompt block.

## Paths
```
workflows/proposal.md
```

## Implementation Notes
1. Line 247: replace `slides.length MUST equal PROPOSAL_TYPES[typeId].slides` with dynamic rule:
   - Base skeleton per type still defines minimum slide structure
   - AI may add extra slides when content warrants (technicalNarrative > 4 paras → split; team > 4 members → 2 slides; phases > 4 → 2 timeline slides)
2. Add `designConfig` field to manifest schema: `{ colorPalette, layoutStyle, fontPair }`
   - 3 layoutStyle values: `modern-tech`, `enterprise`, `creative`
3. Add DESIGN SELECTION block to the AI prompt in Step 4 — AI must choose `designConfig` based on project context (sector, audience, tone)
4. Update success_criteria: replace slide count check with "slide count matches content volume (≥ base skeleton)"

## Acceptance Criteria
- [ ] "slides.length MUST equal" hardcap line removed
- [ ] designConfig field documented in manifest schema
- [ ] 3 layoutStyle values listed (modern-tech, enterprise, creative)
- [ ] Content-aware split rules documented
- [ ] DESIGN SELECTION AI prompt block added
- [ ] Success criteria updated

# Task 74.4 — Tests + CHANGELOG 2.11.0

## Goal
Contract tests for ENH-045; version bump to 2.11.0.

## Paths
```
tests/unit/vp-enh045-dynamic-slides.test.js
CHANGELOG.md
package.json
```

## Implementation Notes

### tests/unit/vp-enh045-dynamic-slides.test.js
Follow pattern of vp-feat011 test file. Assert content presence in source files.

```js
describe('ENH-045 Dynamic Slides + Visual Design', () => {
  describe('workflows/proposal.md', () => {
    test('hardcap rule removed — no "slides.length MUST equal" text')
    test('designConfig field documented in manifest schema')
    test('modern-tech layoutStyle listed')
    test('enterprise layoutStyle listed')
    test('creative layoutStyle listed')
    test('content-aware split rules documented (technicalNarrative/team/phases)')
    test('DESIGN SELECTION block present in proposal.md')
  })
  describe('lib/proposal-generator.cjs', () => {
    test('DESIGN_CONFIGS exported')
    test('getDesignConfig exported')
    test('getDesignConfig: fintech context returns enterprise config')
    test('getDesignConfig: startup context returns creative config')
    test('getDesignConfig: default (no sector) returns modern-tech config')
  })
  describe('scripts/gen-proposal-pptx.cjs', () => {
    test('PALETTES map present with modern-tech, enterprise, creative keys')
    test('addTimelineGanttSlide function defined')
    test('addTeamCardSlide function defined')
    test('addInvestmentVisualSlide function defined')
    test('3 project-proposal variant filenames referenced in script')
  })
  describe('templates/proposal/pptx', () => {
    test('project-proposal-modern-tech.pptx exists')
    test('project-proposal-enterprise.pptx exists')
    test('project-proposal-creative.pptx exists')
  })
})
```

Minimum: **≥12 tests**.

### CHANGELOG.md
Move ENH-045 content from `[Unreleased]` to new `## [2.11.0] - 2026-04-13` section.

### package.json
`"version": "2.10.1"` → `"2.11.0"`

## Acceptance Criteria
- [ ] Test file exists with ≥12 tests
- [ ] All tests pass: `npm test`
- [ ] CHANGELOG.md has `## [2.11.0]` section
- [ ] package.json version = `2.11.0`

# Task 43.1 — brainstorm.md: Architect Design Mode section

**Phase**: 43 | **Status**: todo

## What
Add `### Architect Design Mode` section to `workflows/brainstorm.md` after `### Landing Page Deep-Dive`.

## Spec
Section must include:
- **Activation rules**: `--architect` flag OR auto-heuristic trigger
- **Workspace layout**: table of all 9 files in `.viepilot/architect/{session-id}/`
- **Dialogue cadence**: per-decision update, incremental rule, diff indicator (`data-updated`)
- **Mermaid.js diagram types**: architecture (`graph TD`/`C4Context`), data-flow (`sequenceDiagram`), feature-map (`mindmap`)
- **`/review-arch` command**: triggers decision summary + open questions list

## Acceptance
- [ ] Section exists in brainstorm.md
- [ ] All 9 workspace files documented in table
- [ ] Dialogue cadence with incremental update rule present
- [ ] `/review-arch` behavior documented
- [ ] Mermaid diagram types listed

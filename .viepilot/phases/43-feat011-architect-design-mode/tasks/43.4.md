# Task 43.4 — brainstorm.md: auto-activate heuristic

**Phase**: 43 | **Status**: todo

## What
Document auto-activate heuristic for Architect Design Mode in `workflows/brainstorm.md`.

## Heuristic Spec
Trigger conditions (ANY of):
1. Named components/services count ≥ 3: detect words followed by "Service", "API", "Module", "Layer", "Gateway", "Handler" OR PascalCase multi-word names in context of system design
2. Tech stack explicitly proposed: user mentions ≥1 known framework/infra keyword (React, Vue, Spring, Node, Django, PostgreSQL, MySQL, Redis, AWS, GCP, Azure, Docker, Kubernetes, etc.)
3. User explicitly picks "Architecture" topic from brainstorm menu

Auto-activate prompt (shown once per session):
```
💡 Tôi nhận thấy bạn đang thiết kế kiến trúc với nhiều components/services.

Kích hoạt **Architect Design Mode** để tôi tạo HTML visualization (architecture diagram, decision log, tech stack, feature map)?

1. Có — tạo workspace và bắt đầu generate
2. Không — tiếp tục brainstorm text-only
3. Nhắc lại sau (khi có ≥5 components)
```

- Option 1: create `.viepilot/architect/{session-id}/` from templates, generate initial HTML with known data
- Option 2: continue, set `architect_mode = false` for session
- Option 3: increase threshold to 5, re-evaluate later

## Acceptance
- [ ] 3 trigger conditions documented
- [ ] Prompt text present with 3 options
- [ ] Option 3 "remind later" behavior documented

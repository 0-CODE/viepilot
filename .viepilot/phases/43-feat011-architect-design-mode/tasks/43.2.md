# Task 43.2 — HTML templates: 7 pages + style.css

**Phase**: 43 | **Status**: todo

## What
Create `templates/architect/` directory with 8 files (7 HTML + 1 CSS).

## Files to create

### style.css
- CSS variables: `--bg`, `--text`, `--accent`, `--border`, `--updated-bg`
- Dark mode: `html.dark { --bg: #1a1a1a; ... }`
- `.updated { border-left: 3px solid var(--accent); background: var(--updated-bg); }`
- `.badge-updated { font-size: 0.7em; background: var(--accent); color: white; padding: 2px 6px; border-radius: 3px; }`
- Sidebar nav: `<nav class="sidebar">` responsive
- Mermaid container: `.mermaid { max-width: 100%; overflow-x: auto; }`

### index.html (hub)
- `<title>Architect Design — {project}</title>`
- Sidebar with links to all 6 content pages
- Main area: project summary, last updated, open questions count badge
- Dark mode toggle button

### architecture.html
- Heading: System Architecture
- Mermaid `graph TD` block (placeholder comment)
- Component table: Name | Type | Responsibilities | Tech | Dependencies
- Placeholder instructions (HTML comment)

### data-flow.html
- Heading: Data Flow
- Mermaid `sequenceDiagram` block (placeholder)
- Flow narrative table: Step | Actor | Action | Result

### decisions.html
- Heading: Architecture Decisions (ADR Log)
- Table: # | Date | Topic | Options | Chosen | Rationale | Status (badge: decided/open/deferred)
- Status badges with color coding

### tech-stack.html
- Heading: Tech Stack
- Layer table: Layer | Technology | Why chosen | Alternatives considered
- Layers: Frontend, Backend, API Layer, Database, Cache, Infra, CI/CD, Monitoring

### tech-notes.html
- Heading: Technical Notes
- 3-column grid (on desktop): Assumptions | Risks | Open Questions
- Each column: list with add-item placeholder

### feature-map.html
- Heading: Feature Map
- Table: Feature | Layer | Phase | Tier (MVP/Post-MVP/Future) | Status | Notes
- Filter by Tier (JS-based, no library)

## Acceptance
- [ ] `templates/architect/` directory with 8 files
- [ ] All HTML files reference `./style.css`
- [ ] All HTML files have dark mode toggle
- [ ] All HTML files link back to `index.html`
- [ ] `.updated` class in style.css
- [ ] Mermaid CDN `<script>` in architecture.html + data-flow.html
- [ ] Open in browser without errors (no external dependencies except Mermaid CDN)

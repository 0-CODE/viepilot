# Task 43.5 — crystallize.md: Step 1C — Consume Architect Artifacts

**Phase**: 43 | **Status**: todo

## What
Add `<step name="consume_architect_artifacts">` (Step 1C) to `workflows/crystallize.md` after `<step name="research_stack_official">` (Step 1B).

## Spec
```xml
<step name="consume_architect_artifacts">
## Step 1C: Consume Architect Artifacts (FEAT-011)
```

### Detection
```bash
ls -la .viepilot/architect/ 2>/dev/null
```
Select most recent session directory (latest mtime or session-id sort).

### When artifacts exist
1. Read `notes.md` YAML frontmatter:
   - `decisions[]` → append to `.viepilot/ARCHITECTURE.md` under new heading `## Architecture Decisions (Architect Design Mode — {session_id})`; format as table: # | Topic | Chosen | Rationale | Status
   - `tech_stack{}` → compare with brainstorm-derived tech stack; if conflict → flag for user: "Conflict: brainstorm says X, architect mode says Y — which wins?"
   - `open_questions[]` status=open → surface list: "The following questions were unresolved at end of Architect Design Mode — resolve before proceeding or mark as deferred"

2. `feature-map.html` → cross-reference with `## Product horizon` from brainstorm:
   - Features in architect mode marked MVP but not in brainstorm horizon (or vice versa) → flag discrepancy

3. Record working notes: `architect_session_id`, `decisions_imported_count`, `open_questions_open_count`

### When artifacts don't exist (and complex architecture detected)
If brainstorm session has ≥5 named services/components:
```
💡 Dự án có kiến trúc phức tạp nhưng chưa có Architect Design Mode artifacts.

Bạn muốn:
1. Quay lại /vp-brainstorm --architect trước (khuyến nghị cho dự án phức tạp)
2. Tiếp tục crystallize — tôi sẽ derive architecture từ brainstorm text
```
Not a hard block — option 2 proceeds normally.

## Acceptance
- [ ] Step 1C exists in crystallize.md after Step 1B
- [ ] YAML parse → ARCHITECTURE.md import documented
- [ ] Tech stack conflict detection documented
- [ ] Open questions surface behavior documented
- [ ] Feature-map vs horizon cross-reference documented
- [ ] Soft suggestion (not hard block) for missing artifacts

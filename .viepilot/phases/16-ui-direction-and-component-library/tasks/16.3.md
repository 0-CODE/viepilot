# Task 16.3 — UI component curation workflow (21st.dev)

## Objective
Create workflow + skill to ingest and classify UI component references/prompts and save reusable assets into global and project-local stores.

## Paths
| Path | Action |
|------|--------|
| `workflows/ui-components.md` | Add |
| `skills/vp-ui-components/SKILL.md` | Add |
| `~/.viepilot/ui-components/{component}/{variant}/` | Runtime global storage |
| `.viepilot/ui-components/{component}/{variant}/` | Runtime project storage |

## Implementation notes
- Define metadata schema: source, tags, stack hints, accessibility notes, variants.
- Support input types: prompt text, URL, snippet, screenshot reference note.
- Include dedup strategy and naming conventions.

## Verification
```bash
ls workflows | grep ui-components.md
ls skills | grep vp-ui-components
```

## State update checklist
- [x] Update PHASE-STATE to `done` when merged.

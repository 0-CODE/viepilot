# Task 16.1 — Brainstorm UI mode with live HTML direction

## Objective
Add a UI-first mode to brainstorm workflow that creates and updates a direction prototype (`index.html`, `style.css`, `notes.md`) as decisions evolve.

## Paths
| Path | Action |
|------|--------|
| `workflows/brainstorm.md` | Modify |
| `skills/vp-brainstorm/SKILL.md` | Modify |
| `.viepilot/ui-direction/{session-id}/` | Create (runtime artifact) |

## Implementation notes
- Add a `--ui`/`--design` branch (or equivalent trigger) in brainstorm process.
- Define artifact write/update loop per decision change.
- Ensure `notes.md` captures design rationale and links to references provided by user.

## Verification
```bash
grep -n "ui-direction\\|index.html\\|style.css\\|notes.md" workflows/brainstorm.md
```

## State update checklist
- [x] Update PHASE-STATE to `done` when merged.

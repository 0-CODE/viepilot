# Task 16.4 — Install bootstrap for stock UI components

## Objective
Update install scripts to provision baseline UI stock components for both stable and dev installation modes.

## Paths
| Path | Action |
|------|--------|
| `install.sh` | Modify |
| `dev-install.sh` | Modify |
| `ui-components/` (repo seed) | Add (if needed) |

## Implementation notes
- Ensure both installers create/symlink UI component directories.
- Copy/symlink baseline stock set from repo into target (`~/.cursor/viepilot` or project runtime path).
- Keep install idempotent.

## Verification
```bash
grep -n "ui-components" install.sh dev-install.sh
```

## State update checklist
- [x] Update PHASE-STATE to `done` when merged.

# Task 104.3 — Tests (≥12) + CHANGELOG [2.39.0] + version bump

## Meta
- **Phase**: 104
- **Task**: 104.3
- **Status**: planned
- **Complexity**: S
- **Req**: ENH-070

## Objective
Write contract tests for ENH-070, update CHANGELOG with [2.39.0], bump version 2.38.0 → 2.39.0.

## Paths
```
tests/unit/phase104-enh070-audit-autolog.test.js
CHANGELOG.md
package.json
```

## File-Level Plan

### `tests/unit/phase104-enh070-audit-autolog.test.js` — ≥12 tests

```javascript
const audit = fs.readFileSync('workflows/audit.md')
const skill = fs.readFileSync('skills/vp-audit/SKILL.md')

// Group A — Auto-Log Gate (audit.md)
1. contains "Auto-Log Gate"
2. contains "auto-logged by vp-audit"
3. contains "Duplicate detection"
4. contains "auto_logged_new"
5. contains tier-to-type mapping table (Tier 1 → BUG / Tier 4 → ENH)
6. contains TRACKER.md update instruction in gate block

// Group B — --no-autolog flag (audit.md)
7. contains "--no-autolog" ≥ 2 occurrences
8. contains "skip entire Auto-Log Gate" or equivalent skip condition

// Group C — Post-audit routing banner (audit.md)
9. contains "Post-Audit Routing Banner" or similar
10. contains "vp-evolve" in routing options ≥ 2 occurrences
11. contains "Skip — review later" or equivalent skip option

// Group D — SKILL.md documentation
12. skill contains "--no-autolog"
13. skill contains "Auto-Log Behavior"
14. skill contains "Duplicate detection" or "duplicate"
```

### `CHANGELOG.md` — [2.39.0]
```markdown
## [2.39.0] — 2026-04-22

### Added
- **ENH-070** — vp-audit auto-log gaps as requests + direct vp-evolve routing
  - `workflows/audit.md`: Auto-Log Gate fires after all tiers; each ⚠️/⛔ finding auto-creates `.viepilot/requests/{TYPE}-{N}.md` with `Source: auto-logged by vp-audit`
  - Duplicate detection: existing open requests with matching title/files are updated (re-detected note) instead of creating duplicates
  - TRACKER.md backlog updated automatically after audit
  - Post-audit routing banner shows logged IDs + recommends `/vp-evolve` as next action
  - AUQ prompt: "Plan fix phase → /vp-evolve" as Recommended option
  - `--no-autolog` flag disables auto-logging (report-only mode)
  - `skills/vp-audit/SKILL.md`: documents Auto-Log Behavior + `--no-autolog` flag
```

### `package.json` — version bump
- `"version": "2.38.0"` → `"version": "2.39.0"`

## Acceptance Criteria
- [ ] `npm test` all pass (≥12 new tests in phase104 file)
- [ ] `package.json` version = "2.39.0"
- [ ] `CHANGELOG.md` has `## [2.39.0]` section

## Verification Commands
```bash
node -e "const p=require('./package.json'); console.log(p.version)"
grep -c "2.39.0" CHANGELOG.md
npx jest tests/unit/phase104-enh070-audit-autolog.test.js
```

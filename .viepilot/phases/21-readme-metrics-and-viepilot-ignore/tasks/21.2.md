# Task 21.2 — Add verify/re-sync guidance

## Objective
Ensure maintainers can re-run README metric sync safely.

## Paths
- `README.md`

## Implementation Notes
- Keep guidance on `npm run readme:sync` and fallback behavior when `cloc` is missing.

## Verification
Manual read-through in README metric section.

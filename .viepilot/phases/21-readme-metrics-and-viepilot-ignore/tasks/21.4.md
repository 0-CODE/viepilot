# Task 21.4 — Untrack `.viepilot` from repository index

## Objective
Remove `.viepilot` from tracked git index while preserving local files.

## Paths
- `.viepilot/`

## File-Level Plan
- Execute `git rm -r --cached .viepilot`.
- Confirm local files still exist in workspace.

## Verification
```bash
git status --short
```

# Task 3.1: HANDOFF.log + gitignore + phase-boundary rotation

## Meta
- **Phase**: 03-workflow-integration
- **Status**: not_started
- **Complexity**: S
- **Dependencies**: 2.3 (HANDOFF.log emit), 2.7 (control point state)
- **Git Tag**: vp2-p3-t3.1

## Objective

3 deliverables:
1. Add `.gitignore` entries cho HANDOFF.log và archive files
2. Add phase-boundary rotation hook trong `workflows/autonomous.md`
3. Verify HANDOFF.log is JSONL append-only (from Task 2.3)

## Paths
```yaml
files_to_modify:
  - .gitignore                      # thêm entries cho HANDOFF files
  - workflows/autonomous.md         # phase complete → rotation hook
```

## File-Level Plan
- `.gitignore`: Thêm section `# ViePilot runtime state (do not commit)` với entries: `.viepilot/HANDOFF.log`, `.viepilot/logs/handoff-*.log`, `.viepilot/HANDOFF.json`
- `workflows/autonomous.md` phase complete hook: Khi tất cả tasks trong phase PASS/SKIP → `mv .viepilot/HANDOFF.log .viepilot/logs/handoff-phase-{N}.log` → create fresh `HANDOFF.log` → update `HANDOFF.json.meta.last_archived = "phase-{N}"`

## Acceptance Criteria
- [ ] `.gitignore` has 3 entries: HANDOFF.log, logs/handoff-*.log, HANDOFF.json
- [ ] `git status` clean after writing HANDOFF.log (not tracked)
- [ ] Phase complete hook: rotates HANDOFF.log to logs/handoff-phase-N.log
- [ ] Fresh HANDOFF.log created after rotation (empty)
- [ ] HANDOFF.json.meta.last_archived updated after rotation

## Do / Don't
### Do
- Check if `.gitignore` already has HANDOFF entries before adding (idempotent)
- Rotation is in autonomous.md phase_complete step (not separate script)

### Don't
- Không gitignore `.viepilot/` directory itself — only runtime state files

## Verification
```yaml
manual:
  - description: "git status after writing HANDOFF.log → not listed as untracked"
    required: true
  - description: "Phase complete → HANDOFF.log renamed to handoff-phase-N.log"
    required: true
```

## State Update Checklist
- [ ] Update PHASE-STATE.md task 3.1 → done
- [ ] Update TRACKER.md + HANDOFF.json
- [ ] Git commit: `chore: gitignore HANDOFF runtime files + phase rotation hook`

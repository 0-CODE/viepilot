# Task 3.3: vp-status — control point detection + recovery stats

## Meta
- **Phase**: 03-workflow-integration
- **Status**: not_started
- **Complexity**: S
- **Dependencies**: 2.7 (control_point state protocol), 2.3 (HANDOFF.log)
- **Git Tag**: vp2-p3-t3.3

## Objective

Update vp-status workflow (hoặc skill) để detect và display:
1. Control point active banner (khi HANDOFF.json.control_point.active == true)
2. Recovery stats (L1/L2/L3 attempt counts từ HANDOFF.json.recovery.*)

## Paths
```yaml
files_to_modify:
  - workflows/status.md             # or wherever vp-status logic lives
  - skills/vp-status/SKILL.md       # if status display is in skill
```

## File-Level Plan
- vp-status workflow: Thêm control_point detection block sau reading HANDOFF.json. Nếu `control_point.active == true` → display red banner. Thêm recovery stats section sau progress display.

## Control Point Banner Format
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 ⚠ CONTROL POINT ACTIVE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 Task: {position.task}
 Reason: {control_point.reason}
 Since: {control_point.ts}

 Run /vp-auto to resolve
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Recovery Stats Format
```
Recovery Activity:
  L1 (lint/format): {l1_attempts} attempts
  L2 (test fix):    {l2_attempts} attempts
  L3 (scope reduce):{l3_attempts} attempts
```

## Acceptance Criteria
- [ ] vp-status reads HANDOFF.json at start
- [ ] control_point.active == true → banner displayed prominently
- [ ] banner shows task, reason, timestamp
- [ ] Recovery stats shown when any attempt count > 0
- [ ] Recovery stats not shown when all counts = 0 (clean run)

## Do / Don't
### Do
- Show control point banner FIRST, before progress — it's the most important info
- Only show recovery stats when non-zero (no clutter for clean runs)

### Don't
- Không show recovery stats as errors — they are normal operation

## Verification
```yaml
manual:
  - description: "Trigger control point → run /vp-status → banner visible"
    required: true
  - description: "No control point → /vp-status shows no banner"
    required: true
```

## State Update Checklist
- [ ] Update PHASE-STATE.md task 3.3 → done
- [ ] Update TRACKER.md + HANDOFF.json
- [ ] Git commit: `feat(skills): vp-status control point detection + recovery stats`

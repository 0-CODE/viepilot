# Task 3.4b: crystallize backward compat verification

## Meta
- **Phase**: 03-workflow-integration
- **Status**: not_started
- **Complexity**: S
- **Dependencies**: 3.4a (crystallize v2 updates)
- **Git Tag**: vp2-p3-t3.4b

## Objective

Verify crystallize v2 changes không break v1 project structure. Specifically: new template fields are optional, v1 project artifacts remain valid, vp-auto gracefully handles TASK.md without v2 fields.

## Paths
```yaml
files_to_modify:
  - workflows/crystallize.md   # add backward compat note/guard if needed
  - workflows/autonomous.md    # verify graceful skip for missing fields (may already be done in 2.9)
```

## File-Level Plan
- `workflows/crystallize.md`: Add backward compat note in Step 10 — new fields populated when crystallize generates fresh; existing v1 TASK.md files left unchanged.
- `workflows/autonomous.md`: Verify (read) that recovery_budget, write_scope, recovery_overrides all have graceful defaults when missing.

## Backward Compat Checklist
```
TASK.md missing recovery_budget → default to M (Task 2.9)
TASK.md missing write_scope → scope drift detection skipped (Task 2.8)
TASK.md missing recovery_overrides → no overrides applied (Task 2.9)
TASK.md missing type → treated as build (Task 2.1)

PHASE-STATE.md missing execution_state block → vp-auto works without it (legacy mode)
HANDOFF.json version == 1 → vp-auto reads available fields, uses defaults for missing
TRACKER.md blob format → vp-auto reads it (won't crash on unexpected content)
```

## Acceptance Criteria
- [ ] v1 TASK.md (no new fields) → vp-auto runs without error
- [ ] v1 HANDOFF.json (version: 1, no sub_task) → vp-resume works
- [ ] v1 TRACKER.md (blob format) → vp-auto reads Current State without crash
- [ ] crystallize v2 generates correct NEW files; old files untouched
- [ ] Note in crystallize.md documents v1/v2 transition behavior

## Do / Don't
### Do
- Test với simulated v1 TASK.md (no Task Metadata section) → vp-auto runs
- Add explicit comment in autonomous.md: "graceful default if field missing"

### Don't
- Không thêm migration script — v1→v2 is opt-in (users can keep v1 files)

## Verification
```yaml
manual:
  - description: "Create v1-style TASK.md → run vp-auto → no crash on missing fields"
    required: true
  - description: "crystallize on new project → v2 artifacts; crystallize on v1 project → no changes to existing files"
    required: false
```

## State Update Checklist
- [ ] Update PHASE-STATE.md task 3.4b → done
- [ ] Update TRACKER.md + HANDOFF.json
- [ ] Git commit: `docs(workflows): crystallize backward compat notes + v1 graceful defaults`

# Task 3.5: vp-evolve — generate v2 TASK.md template

## Meta
- **Phase**: 03-workflow-integration
- **Status**: not_started
- **Complexity**: S
- **Dependencies**: 1.4 (TASK.md v2 template), 3.4a (crystallize v2)
- **Git Tag**: vp2-p3-t3.5

## Objective

Update `workflows/evolve.md` để khi add feature hoặc new milestone, tất cả generated TASK.md files dùng v2 template (với Task Metadata section) thay vì v1 template.

## Paths
```yaml
files_to_modify:
  - workflows/evolve.md
```

## File-Level Plan
- `workflows/evolve.md` Step 3A (Add Feature) và 3B (New Milestone): Update task generation instruction để reference `templates/phase/TASK.md` v2 (updated in Task 1.4). Pre-populate `type: build` as default cho new feature tasks. Apply same Gap A auto-populate logic as crystallize (or reference crystallize logic).

## Changes Needed
```
evolve.md Step 3A Generate Phase Tasks:
  Before: Create tasks using basic template
  After:  Create tasks using v2 TASK.md template with:
          - type: build (default for new features)
          - write_scope: infer from feature description
          - recovery_budget: set from complexity estimate
          - recovery_overrides: apply Gap G if compliance paths
```

## Acceptance Criteria
- [ ] evolve.md references v2 TASK.md template
- [ ] New tasks from /vp-evolve have Task Metadata section
- [ ] type: build default applied to new feature tasks
- [ ] Gap G compliance detection applied (same logic as crystallize 3.4a)

## Do / Don't
### Do
- Reference crystallize logic rather than duplicate — "apply same auto-populate as crystallize Step 10"
- Keep evolve.md changes minimal — just update template reference and add pre-populate instruction

### Don't
- Không duplicate the compliance domain logic — reference crystallize implementation

## Verification
```yaml
manual:
  - description: "Run /vp-evolve add feature → generated TASK.md has Task Metadata section"
    required: true
```

## State Update Checklist
- [ ] Update PHASE-STATE.md task 3.5 → done
- [ ] Update TRACKER.md + HANDOFF.json
- [ ] Git commit: `feat(workflows): vp-evolve generate v2 TASK.md template`

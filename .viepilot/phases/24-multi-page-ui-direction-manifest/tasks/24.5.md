# Task 24.5 — User docs + optional verification

## Objective
Publish the contract for end users and add a cheap drift check (optional script or documented grep).

## Paths
- `docs/user/features/ui-direction.md`
- `docs/skills-reference.md` (only if `/vp-brainstorm` section needs a one-line update)
- `CHANGELOG.md` (phase complete entry)

## Implementation Notes
- Replace or extend artifact tree diagram with multi-page layout + hook description.
- Optional: `scripts/verify-ui-direction-pages.cjs` or document `find .viepilot/ui-direction -name '*.html'` checklist — only if low cost.

## Verification
- `npm test` still passes; manual read of ui-direction doc for clarity.

## Acceptance Criteria
- [ ] New users can follow doc to set up multi-page direction without reading internal phase SPEC.

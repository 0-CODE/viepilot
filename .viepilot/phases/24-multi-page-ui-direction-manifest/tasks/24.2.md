# Task 24.2 — Update brainstorm workflow (multi-page + hooks)

## Objective
Encode multi-page UI Direction behavior and mandatory manifest/notes updates in `workflows/brainstorm.md`.

## Paths
- `workflows/brainstorm.md`

## Implementation Notes
- Extend **UI Direction Mode** section:
  - When user scopes a new page → create/update `pages/{slug}.html`.
  - After any page add/rename/remove → update hub `index.html` links.
  - **Hook (mandatory)**: append or sync the `## Pages` inventory in `notes.md` (or manifest file) with slug, title, purpose, key sections, navigation edges.
- Add explicit checklist step before “session saved” / topic complete.

## Verification
- `rg "pages/|Pages inventory|manifest" workflows/brainstorm.md`

## Acceptance Criteria
- [ ] Workflow text is actionable by an AI executor without ambiguity.

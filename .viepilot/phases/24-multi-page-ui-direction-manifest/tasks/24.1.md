# Task 24.1 — Define artifact layout + manifest contract

## Objective
Lock a single, documented contract for multi-page UI Direction artifacts and the page manifest format.

## Paths
- `docs/user/features/ui-direction.md` (contract section draft or full update in 24.5 if split)
- `.viepilot/phases/24-multi-page-ui-direction-manifest/SPEC.md` (reference only)

## File-Level Plan
- Propose directory layout under `.viepilot/ui-direction/{session-id}/`:
  - `index.html` (hub: links to all pages)
  - `pages/{slug}.html` (one per screen)
  - `style.css` (shared)
  - `notes.md` (rationale + **structured `## Pages` table**)
  - **OR** `pages-manifest.json` listing `id`, `file`, `title`, `purpose`, `primary_actions`, `nav_to[]`
- Choose one source of truth for Crystallize: recommend `notes.md` human table + optional JSON duplicate only if needed (prefer one file to avoid drift).
- Document backward compatibility: sessions with only `index.html` + `notes.md` remain valid.

## Verification
- Peer review: SPEC.md success criteria align with chosen contract.

## Acceptance Criteria
- [ ] Written contract ready for workflow/skill edits in tasks 24.2–24.4.

# Task 24.4 — Update crystallize workflow + skill

## Objective
Ensure `/vp-crystallize` consumes the page inventory and each page file when building UI architecture.

## Paths
- `workflows/crystallize.md`
- `skills/vp-crystallize/SKILL.md`

## Implementation Notes
- After discovering `ui-direction/{session-id}/`, glob or list `pages/*.html` (if present).
- Read `notes.md` **Pages** section (or manifest) first for site map; then skim each page HTML for section structure.
- Legacy path: if no `pages/`, keep current behavior (`index.html` only).
- Success criteria: architecture plan explicitly references **all listed pages**.

## Verification
- `rg "pages/|Pages|manifest|ui-direction" workflows/crystallize.md skills/vp-crystallize/SKILL.md`

## Acceptance Criteria
- [ ] Crystallize cannot silently ignore multi-page artifacts when they exist.

# Task 31.3 — Installer: ensure global profile dirs + profile-map seed

## Objective
`buildInstallPlan` / `applyInstallPlan` tạo `~/.viepilot/profiles` (mkdir recursive) và seed `~/.viepilot/profile-map.md` **chỉ khi file chưa tồn tại**, khớp contract `docs/dev/global-profiles.md`.

## Paths
- `lib/viepilot-install.cjs` — thêm `userDataDir` (~/.viepilot), steps `mkdir` + step mới `write_file_if_missing`
- `tests/unit/viepilot-install.test.js` — assert plan + apply trên fake HOME
- `docs/user/quick-start.md` — một dòng về `~/.viepilot/profiles` + `profile-map.md` sau install

## Implementation Notes
1. Step mới: `{ kind: 'write_file_if_missing', path, content }` — idempotent; không ghi đè user edits.
2. Nội dung seed: title + intro ngắn + bảng markdown header row đúng 6 cột (không secrets).
3. `formatPlanLines` + `applyInstallPlan` xử lý step mới.
4. `plan.paths` mở rộng: `viepilotUserDataDir`, `viepilotProfilesDir`, `viepilotProfileMapPath` (tên rõ, tránh nhầm `.cursor/viepilot`).

## File-Level Plan
| File | Action |
|------|--------|
| `lib/viepilot-install.cjs` | Constants seed + steps + switch branch |
| `tests/unit/viepilot-install.test.js` | 2 tests (plan contains steps; apply creates map) |
| `docs/user/quick-start.md` | Short bullet under install / post-install |

## Verification
- `npm run verify:release`

## Status
done

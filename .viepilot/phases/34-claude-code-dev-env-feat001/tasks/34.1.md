# Task 34.1 — Claude Code setup guide

## Objective
Add **`docs/user/claude-code-setup.md`** with reproducible steps for Claude Code users.

## Paths (shipping)
- `docs/user/claude-code-setup.md` (new)

## File-Level Plan
1. **Create** `docs/user/claude-code-setup.md` — single canonical guide (Vietnamese, aligned with [Quick Start](user/quick-start.md) tone).
2. **Sections**: IDE comparison (Cursor vs Claude Code paths); official links `code.claude.com` (skills + `.claude` directory); `npx viepilot install --target claude-code` (note: installer still mirrors bundle under `~/.cursor/` per `lib/viepilot-install.cjs`); explicit steps to copy/symlink `skills/vp-*` → `~/.claude/skills/`; workflows + `execution_context` pointing at `~/.cursor/viepilot/`; `npm i -g` / `node .../vp-tools.cjs`; `/vp-crystallize` + `templates/project/`; verify `vp-tools info`.
3. **Conventions**: Use relative links to `../getting-started.md`, `quick-start.md`, `../skills-reference.md`.

## Content checklist (minimum)
- [x] Compare **Cursor** paths (`~/.cursor/skills`, project rules) vs **Claude Code** (project `.claude/`, user `~/.claude/`) with links to https://code.claude.com/docs/en/skills and https://code.claude.com/docs/en/claude-directory
- [x] Install options: `npm i -g viepilot` vs clone + `node <repo>/bin/vp-tools.cjs info`
- [x] How to surface bundled **`skills/vp-*`** and **`workflows/*.md`** (copy vs symlink; warn about updates on `npm update`)
- [x] Bootstrap **`.viepilot/`** (reference `/vp-crystallize` or templates under `templates/project/`)
- [x] Verification: **`vp-tools info`** (or `node .../vp-tools.cjs info --json`)

## Acceptance Criteria
- [x] File committed; markdown renders; no broken internal links to other `docs/` pages you reference
- [x] FEAT-001 acceptance criteria §1 satisfied by this doc

## Status
✅ Done

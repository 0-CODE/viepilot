# Task 34.3 — Jest contracts for FEAT-001 docs

## Objective
Prevent drift: assert the new guide exists and retains required anchor strings.

## Paths (shipping)
- `tests/unit/vp-feat001-claude-code-docs-contracts.test.js` (new)

## File-Level Plan
1. **Add** Jest file mirroring `vp-fe010-*` pattern (`read()` helper, `ROOT`).
2. **Assert** `docs/user/claude-code-setup.md` contains: `vp-tools`, `.claude`, `code.claude.com/docs/en/skills`, symlink loop / installer honesty (`~/.cursor/skills`), ENH-021 chain.
3. **Assert** entrypoints reference the guide: `getting-started.md`, `quick-start.md`, `faq.md`, `docs/README.md` each contain `claude-code-setup.md`.

## Acceptance Criteria
- [x] Test reads `docs/user/claude-code-setup.md` and asserts presence of: `vp-tools`, `.claude`, and at least one official Claude Code doc URL (`code.claude.com`)
- [x] `npm test` passes

## Status
✅ Done

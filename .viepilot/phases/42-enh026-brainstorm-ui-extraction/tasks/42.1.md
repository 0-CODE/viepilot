# Task 42.1 — brainstorm.md: UI signal keywords + background extraction spec

**Phase**: 42 | **Status**: todo

## What
Add `### Background UI Extraction (silent mode)` section to `workflows/brainstorm.md` after `### UI Direction Mode`.

## Spec
- **UI signal keywords list** (minimum): màu, màu sắc, color, layout, màn hình, screen, page, trang, button, nút, form, biểu mẫu, mobile, responsive, giao diện, UI, UX, design, dashboard, sidebar, header, footer, modal, popup, icon, theme
- **Threshold rule**: ≥3 unique signal occurrences in session → activate background accumulation
- **Silent accumulation**: buffer `ui_idea_buffer[]`, non-blocking
- **Surface triggers**: (a) topic ends, (b) `/save` or `/review` command, (c) ≥5 signals accumulated
- **Auto-write path**: user confirm option 1 → append `## Background extracted ideas` to `.viepilot/ui-direction/{session-id}/notes.md` (create dir + file if missing)

## Acceptance
- [ ] Section exists in brainstorm.md
- [ ] Keyword list ≥ 20 terms
- [ ] Threshold, accumulation, surface rules documented
- [ ] "non-blocking" / "silent" explicitly stated

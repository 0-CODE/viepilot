# Task 42.3 — crystallize.md: Step 1A → hard gate

**Phase**: 42 | **Status**: todo

## What
Upgrade `<step name="consume_ui_direction">` in `workflows/crystallize.md` from soft-warn to hard gate.

## Spec
Add before artifact check:
1. **UI scope detection**: scan brainstorm session files for UI signal keywords (same list as 42.1); if ≥3 found → `ui_scope_detected = true`
2. **Hard gate block**:
   ```
   ⚠️ UI Direction artifacts missing

   Phiên brainstorm cho thấy dự án có UI scope nhưng `.viepilot/ui-direction/` chưa có artifacts.

   1. Quay lại /vp-brainstorm --ui để tạo direction trước (khuyến nghị)
   2. Tiếp tục với assumptions — ghi vào ARCHITECTURE.md và proceed
   ```
3. **Option 2 path**: append block to `.viepilot/ARCHITECTURE.md`:
   ```markdown
   ## UI Direction Assumptions
   > Auto-generated by crystallize (ENH-026 gate bypassed by user)
   - UI scope detected in brainstorm but no ui-direction artifacts present
   - Proceeding without visual direction; architecture decisions may need UI review later
   ```
4. **Multi-page check**: preserve existing `## Pages inventory` validation — it still applies when notes.md exists

## Acceptance
- [ ] Hard gate with 2-option dialogue present in step
- [ ] Option 2 writes `## UI Direction Assumptions` to ARCHITECTURE.md
- [ ] Original multi-page inventory check preserved
- [ ] Gate only triggers when `ui_scope_detected = true` (not always)

# Task 42.5 — Jest contract tests: ENH-026

**Phase**: 42 | **Status**: todo

## What
New file `tests/vp-enh026-ui-extraction-contracts.test.cjs` with 5 contract tests.

## Tests

### Test 1: Background extraction keyword list coverage
```js
test('brainstorm.md background extraction: signal keyword list has ≥20 terms', () => {
  const content = fs.readFileSync('workflows/brainstorm.md', 'utf8');
  // Must contain background extraction section
  expect(content).toMatch(/Background UI Extraction/i);
  // Must contain ≥20 signal keywords listed
  const keywordSection = // extract section
  const keywords = // count listed items
  expect(keywords.length).toBeGreaterThanOrEqual(20);
});
```

### Test 2: Surface dialogue has 3 options
```js
test('brainstorm.md: surface confirmation dialogue has 3 options', () => {
  const content = fs.readFileSync('workflows/brainstorm.md', 'utf8');
  expect(content).toMatch(/UI Direction Mode/); // option 2 links to it
  // dialogue has options 1, 2, 3
  expect(content).toMatch(/1\. Lưu.*notes\.md/);
  expect(content).toMatch(/2\. Lưu.*UI Direction Mode/);
  expect(content).toMatch(/3\. Bỏ qua/);
});
```

### Test 3: Crystallize hard gate present
```js
test('crystallize.md Step 1A: hard gate language present', () => {
  const content = fs.readFileSync('workflows/crystallize.md', 'utf8');
  expect(content).toMatch(/⚠️ UI Direction artifacts missing/);
  expect(content).toMatch(/\/vp-brainstorm --ui/);
});
```

### Test 4: Crystallize option 2 writes to ARCHITECTURE.md
```js
test('crystallize.md: option 2 gate bypass writes to ARCHITECTURE.md', () => {
  const content = fs.readFileSync('workflows/crystallize.md', 'utf8');
  expect(content).toMatch(/UI Direction Assumptions/);
  expect(content).toMatch(/ARCHITECTURE\.md/);
});
```

### Test 5: Multi-page Pages inventory check preserved
```js
test('crystallize.md Step 1A: Pages inventory check preserved', () => {
  const content = fs.readFileSync('workflows/crystallize.md', 'utf8');
  expect(content).toMatch(/Pages inventory/);
  expect(content).toMatch(/pages\/\*\.html/);
});
```

## Acceptance
- [ ] 5 tests pass
- [ ] `npm test` total ≥ baseline + 5

# Task 55.3 — Contract tests for BUG-011 path guards

## Goal
Add a new test file `tests/unit/vp-bug011-ui-direction-path-guard.test.js` with 3 contract tests verifying the disambiguation guards are in place.

## Paths
```
tests/unit/vp-bug011-ui-direction-path-guard.test.js
```

## Tests to write

```
describe('BUG-011 ui-direction path disambiguation guard', () => {

  test('brainstorm.md: confirmation dialogue option 1 uses full .viepilot/ path, not bare ui-direction/')
  // Expect: option 1 text matches /\.viepilot\/ui-direction\/.+\/notes\.md/
  // Reject: bare `ui-direction/notes.md` (no .viepilot/ prefix)

  test('crystallize.md: consume_ui_direction step has PATH GUARD block before UI Scope Detection')
  // Expect: /⛔.*PATH GUARD.*BUG-011/
  // Expect: guard appears BEFORE "UI Scope Detection" section
  // Expect: guard mentions "IGNORE" or "ignore" for root/ui-direction

  test('crystallize.md: PATH GUARD names .viepilot/ui-direction as the ONLY valid path')
  // Expect: guard text contains `.viepilot/ui-direction/` as the valid path
  // Expect: guard explicitly references root-level `ui-direction/` as invalid/ignored

})
```

## Acceptance Criteria
- [ ] All 3 tests pass
- [ ] All 6 existing ENH-026 tests still pass
- [ ] `npm test` green (no regressions)
- [ ] Test file uses same `read(relPath)` helper pattern as other contract tests

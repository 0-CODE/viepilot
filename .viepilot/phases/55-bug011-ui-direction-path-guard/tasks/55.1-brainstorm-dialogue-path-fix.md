# Task 55.1 — Fix brainstorm.md:416 ambiguous path in confirmation dialogue

## Goal
Replace the ambiguous `ui-direction/notes.md` in the Background UI Extraction confirmation dialogue with the full, unambiguous path `.viepilot/ui-direction/{session-id}/notes.md`.

## Paths
```
workflows/brainstorm.md
```

## Change

In the confirmation dialogue template (around line 416), change option 1 from:
```
1. Save to ui-direction/notes.md (background extraction)
```
to:
```
1. Save to .viepilot/ui-direction/{session-id}/notes.md (background extraction)
```

This makes the target path unambiguous — LLM cannot confuse it with a root-level `ui-direction/` folder.

## Acceptance Criteria
- [ ] Option 1 in confirmation dialogue shows `.viepilot/ui-direction/{session-id}/notes.md`
- [ ] No other text in the dialogue template is changed
- [ ] All existing ENH-026 tests still pass (the test checks `/1\.\s.*notes\.md/` which still matches)

# Task 55.2 — Add PATH GUARD to crystallize.md consume_ui_direction step

## Goal
Add an explicit PATH GUARD block at the very beginning of the `consume_ui_direction` step in `crystallize.md`, before the UI Scope Detection section. This prevents the LLM from reading `{root}/ui-direction/` even if it exists.

## Paths
```
workflows/crystallize.md
```

## Change

At the start of the `<step name="consume_ui_direction">` block (after the `## Step 1A: Consume UI Direction Artifacts (for UI/UX projects)` heading), insert:

```markdown
> ⛔ **PATH GUARD (BUG-011):** The ONLY valid ui-direction path is `.viepilot/ui-direction/`.
> If a `{root}/ui-direction/` directory exists at the project root — **IGNORE it completely**.
> It is user-managed reference material, NOT ViePilot artifacts.
> Never read, glob, or reference any file under `{root}/ui-direction/` in this workflow.
```

The guard must appear **before** the UI Scope Detection section so it takes effect before any file scanning begins.

## Acceptance Criteria
- [ ] Guard block with `⛔ PATH GUARD (BUG-011)` text appears at start of consume_ui_direction step
- [ ] Guard explicitly names `.viepilot/ui-direction/` as the ONLY valid path
- [ ] Guard explicitly says to IGNORE `{root}/ui-direction/`
- [ ] Position: before `### UI Scope Detection (ENH-026 hard gate)` line
- [ ] Existing ENH-026 tests still pass (guard is additive — no existing logic removed)

# Task 97.2 — ENH-065 Crystallize Side: Content Export + Template Section

## Objective
Thêm crystallize export cho `## content` YAML từ architect `notes.md` → `## Content Management` table vào `PROJECT-CONTEXT.md`. Thêm `## Content Management` section vào project template. Update `skills/vp-crystallize/SKILL.md`.

## Paths
- `workflows/crystallize.md`
- `templates/project/PROJECT-CONTEXT.md`
- `skills/vp-crystallize/SKILL.md`

## Implementation Notes

### workflows/crystallize.md — Step 1D extension: content export

Trong Step 1D, sau item 7 (`admin.html` / `notes.md ## admin` export), thêm item 8:

```markdown
8. **`content.html` / `notes.md ## content`** (if exists — ENH-065) → append to `.viepilot/PROJECT-CONTEXT.md`:
   ```markdown
   ## Content Management (from Architect Mode)
   | Content Type | Created By | Lifecycle | Key Fields | Phase |
   |-------------|-----------|-----------|-----------|-------|
   | article | admin, author | draft → review → published → archived | title, body, slug, tags, seo_meta | 2 |
   | product | admin | draft → published → discontinued | name, description, price, images, category | 1 |

   ### Media & Storage
   | Storage | CDN | Types | Max Size |
   |---------|-----|-------|----------|
   | S3 | CloudFront | image, pdf | 10 MB |

   ### Localization
   | Locales | Fallback |
   |---------|---------|
   | en | en |
   ```
```

Renumber existing item 8 (feature-map cross-reference) → item 9.
Renumber item 9 (Record in working notes) → item 10.

Add to "Record in working notes":
```
- `content_imported`: {true/false}
- `content_types_count`: {count if content present, else "n/a"}
```

### templates/project/PROJECT-CONTEXT.md — Add ## Content Management section

After `## Admin & Governance` section, append:

```markdown
## Content Management

> Generated by crystallize from architect notes.md ## content (ENH-065).
> Update manually or re-run /vp-crystallize after updating brainstorm artifacts.

| Content Type | Created By | Lifecycle | Key Fields | Phase |
|-------------|-----------|-----------|-----------|-------|
| — | — | — | — | — |

### Media & Storage
| Storage | CDN | Types | Max Size |
|---------|-----|-------|----------|
| — | — | — | — |

### Localization
| Locales | Fallback |
|---------|---------|
| — | — |
```

### skills/vp-crystallize/SKILL.md — Append Step 1D content export note

After `**Admin & Governance Export (ENH-063):**` bullet, add:

```markdown
**Content Management Export (ENH-065):**
- Step 1D item 8: if `content.html` or `notes.md ## content` exists in architect workspace → append `## Content Management` table to `.viepilot/PROJECT-CONTEXT.md` (columns: Content Type | Created By | Lifecycle | Key Fields | Phase) + Media/Storage and Localization tables. Records `content_imported` and `content_types_count` in working notes.
```

## Verification
```bash
grep -c "Content Management" workflows/crystallize.md           # ≥1
grep -c "content.html" workflows/crystallize.md                 # ≥1
grep -c "Content Management" templates/project/PROJECT-CONTEXT.md  # ≥1
grep -c "ENH-065" workflows/crystallize.md                      # ≥1
```

## Acceptance Criteria
- [ ] `workflows/crystallize.md` Step 1D item 8: exports `notes.md ## content` → `## Content Management` in PROJECT-CONTEXT.md
- [ ] Media/Storage and Localization sub-tables exported
- [ ] `content_imported` and `content_types_count` in working notes record
- [ ] `templates/project/PROJECT-CONTEXT.md`: `## Content Management` section added with placeholder rows
- [ ] `skills/vp-crystallize/SKILL.md`: Step 1D content export documented
- [ ] Existing Step 1D items correctly renumbered (8→9, 9→10)

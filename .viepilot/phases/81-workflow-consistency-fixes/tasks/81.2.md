# Task 81.2 — workflows/crystallize.md: Brownfield Execution Path Table (ENH-051)

## Objective
Add an explicit "Brownfield Execution Path" reference table to `workflows/crystallize.md`
clarifying which sub-steps run, are skipped, or are conditional when `--brownfield` mode is
active. Eliminates ambiguity about Steps 1 / 1A–1D.

## Paths
- `workflows/crystallize.md`

## File-Level Plan

### Insert — after Step 0-C (brownfield stub generation), before Step 1

Add a clearly-boxed table immediately after the brownfield stub generation step and before
the `<step name="analyze_brainstorm">` (Step 1) block:

```markdown
### Brownfield Execution Path

When `--brownfield` is active, the following table governs which steps execute:

| Step | Name | Brownfield behavior | Rationale |
|------|------|---------------------|-----------|
| 0 | Collect metadata | **RUN** | Always needed |
| 0-B | Brownfield scanner | **RUN** | Core brownfield step |
| 0-C | Generate brainstorm stub | **RUN** | Creates session-brownfield-import.md |
| 1 | Analyze brainstorm | **RUN (stub only)** | Reads brownfield stub; skips greenfield checks |
| 1A | UI direction gate | **CONDITIONAL** — run if `.viepilot/ui-direction/` already exists in project |
| 1B | Stack research cache | **CONDITIONAL** — skip if brownfield scanner already populated stack cache |
| 1C | Architect artifact consumption | **SKIP** — no architect HTML workspace in brownfield import |
| 1D | Architect auto-activate suggestion | **SKIP** — no scope brainstorm; architect mode not applicable |
| 2+ | All subsequent steps | **RUN** — same as greenfield from Step 2 onward |

> **Implementation note for AI agents:** When `IS_BROWNFIELD=true`, check each conditional
> step against the stated condition before executing. Do not skip step 1 entirely — read
> the brownfield stub to extract stack + gap data for Step 2 (AI-GUIDE.md generation).
```

## Implementation Notes
- The table should be inside the `<step name="brownfield_scanner">` block or immediately after it, before `<step name="analyze_brainstorm">`
- Keep existing Step 1 logic — just make the table the entry-point decision tree
- The `IS_BROWNFIELD` variable is already set in Step 0-B; reference it in the table note

## Acceptance Criteria
- [ ] Table present in `crystallize.md` between Step 0-C and Step 1
- [ ] All 8 rows populated with RUN/SKIP/CONDITIONAL + rationale
- [ ] Step 1A condition references `.viepilot/ui-direction/` existence check
- [ ] Step 1B condition references brownfield scanner stack cache output
- [ ] Step 1C and 1D explicitly marked SKIP with rationale
- [ ] Table uses standard markdown table format (consistent with existing workflow style)

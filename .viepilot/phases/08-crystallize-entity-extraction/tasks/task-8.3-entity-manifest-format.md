# Task 8.3: crystallize.md — Entity manifest output format

## Meta
- **Phase**: 08-crystallize-entity-extraction
- **Status**: done
- **Complexity**: S
- **Dependencies**: Tasks 8.1, 8.2
- **Git Tag**: `viepilot-vp-p8-t8.3`
  _(resolved at runtime: `TAG_PREFIX=$(node bin/vp-tools.cjs tag-prefix --raw)`)_

## Task Metadata

```yaml
type: "docs"
complexity: "S"
write_scope:
  - "workflows/crystallize.md"
recovery_budget: "S"
can_parallel_with: []
recovery_overrides:
  L1:
    block: false
  L2:
    block: false
  L3:
    block: false
    reason: ""
```

## Objective

Ensure the entity manifest table generated by Task 8.1 is also written into the SPEC.md output artifact during crystallize. This makes the entity analysis visible and reviewable in the project, not just a transient AI step.

Add to crystallize.md: instruction to append entity manifest table to SPEC.md under a new `## Domain Entity Manifest` section.

## Pre-execution documentation gate (doc-first; BUG-001)

- [x] Task contract fields filled
- [x] Paths listed
- [x] PHASE-STATE.md marks this task `in_progress` before commits

## Paths
```yaml
files_to_create: []
files_to_modify:
  - "workflows/crystallize.md"
```

## File-Level Plan
- `workflows/crystallize.md`: In the domain entity extraction step (from Task 8.1), add instruction: "After extracting entities, append `## Domain Entity Manifest` table to SPEC.md with columns: Entity, Type, needs_crud_api, Service Phase (or MISSING)". This persists the analysis for future reference and vp-auto context.

## Context Required
```yaml
files_to_read:
  - "workflows/crystallize.md"    # current state after 8.1 + 8.2
```

## Acceptance Criteria
- [x] crystallize.md instructs writing entity manifest table to SPEC.md
- [x] Table format: Entity | Type | needs_crud_api | Service Phase
- [x] "MISSING" sentinel value documented for entities without service phase

## Implementation Notes
- Added Step 6A.5 to crystallize.md: persist entity manifest to SPEC.md
- Table includes Entity, Type, needs_crud_api, Service Phase, Status columns
- MISSING sentinel value with warning icon documented
- Referenced by Step 11A dependency validation and vp-auto task context

## Verification
```yaml
automated:
  - command: "grep -n 'Domain Entity Manifest' workflows/crystallize.md"
    expected: "match found"
  - command: "grep -n 'SPEC.md' workflows/crystallize.md"
    expected: "reference to SPEC.md entity manifest write"
```

## State Update Checklist
- [x] Update PHASE-STATE.md after PASS
- [x] Update TRACKER.md
- [x] Update HANDOFF.json
- [x] Update ROADMAP.md if progress changed

## Files Changed
```
M	workflows/crystallize.md
```

## Rollback
```bash
TAG_PREFIX=$(node bin/vp-tools.cjs tag-prefix --raw)
git revert --no-commit $(git rev-list "${TAG_PREFIX}-p8-t8.3".."${TAG_PREFIX}-p8-t8.3-done")
```

## Post-Completion

> **AI fills this section after task PASS**

### Implementation Summary
- Added Step 6A.5: persist finalized entity manifest to SPEC.md as `## Domain Entity Manifest`
- Table with 5 columns: Entity, Type, needs_crud_api, Service Phase, Status
- MISSING sentinel with warning icon for entities without service phase
- Used by Step 11A dependency validation and vp-auto preflight

### Files Changed
| File | Action |
|------|--------|
| `workflows/crystallize.md` | modified |

### Checklist Verification
- [x] `## Meta → Status` set to `done`
- [x] All `## Acceptance Criteria` boxes ticked `[x]`
- [x] PHASE-STATE.md task row updated
- [x] HANDOFF.json `position.task` → 8.4
- [x] TRACKER.md updated

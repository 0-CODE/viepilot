# Task 37.4 — AI-GUIDE.md template: thêm row ui-direction

## Objective
Thêm row ui-direction vào Quick Lookup table trong `templates/project/AI-GUIDE.md` để AI biết đọc ui-direction khi cần design context, không phụ thuộc vào context_required trong task file.

## Paths
- `~/.cursor/viepilot/templates/project/AI-GUIDE.md`

## File-Level Plan

### `AI-GUIDE.md` template — Quick Lookup table

**Tìm table** có columns `Tôi cần... | Đọc file | Section`.

**Thêm row** (sau row về architecture hoặc tech stack):

```markdown
| Xem UI layout, design intent, page structure | `.viepilot/ui-direction/{session}/notes.md` | `## Pages inventory`, `## UX walkthrough log` |
| Xem prototype HTML/CSS từ brainstorm | `.viepilot/ui-direction/{session}/pages/{slug}.html` + `style.css` | full file |
```

**Note về {session}**: thêm footnote hoặc chú thích trong file template:
```
> ui-direction: thay `{session}` bằng folder gần nhất trong `.viepilot/ui-direction/` (sort by name, take last). Chỉ đọc khi task liên quan đến UI/frontend.
```

## Acceptance Criteria
- [ ] Quick Lookup table có ít nhất 1 row cho ui-direction notes.md
- [ ] Có note hướng dẫn resolve `{session}` placeholder

## Complexity: S

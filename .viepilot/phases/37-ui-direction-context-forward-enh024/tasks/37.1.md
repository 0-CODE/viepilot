# Task 37.1 — crystallize.md Step 1A: hardened trigger

## Objective
Đổi điều kiện của Step 1A từ soft heuristic `"If brainstorm indicates frontend/UI scope"` thành hard check: nếu `.viepilot/ui-direction/` tồn tại → Step 1A là **mandatory**.

## Paths
- `~/.cursor/viepilot/workflows/crystallize.md`

## File-Level Plan

### `crystallize.md` — Step 1A header

**Hiện tại** (dòng ~131-133):
```
## Step 1A: Consume UI Direction Artifacts (for UI/UX projects)

If brainstorm indicates frontend/UI scope, check for direction artifacts:

```bash
ls -la .viepilot/ui-direction/ 2>/dev/null
```
```

**Sau khi sửa** — thêm hard check trước shell command, đổi condition:
```
## Step 1A: Consume UI Direction Artifacts

**Hard check (mandatory):**
```bash
ls -la .viepilot/ui-direction/ 2>/dev/null
```

If `.viepilot/ui-direction/` exists → this step is **mandatory regardless of detected scope**.
If brainstorm indicates frontend/UI scope but directory is missing → warn and request user to run `/vp-brainstorm --ui` or explicitly continue with assumptions.
```

## Acceptance Criteria
- [ ] Step 1A không còn soft conditional `"If brainstorm indicates"`
- [ ] Hard check `ls .viepilot/ui-direction/` xuất hiện ở đầu step
- [ ] Hai nhánh rõ ràng: exists → mandatory; scope-detected but missing → warn

## Complexity: S

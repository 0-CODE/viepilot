# Task 37.2 — crystallize.md Step 10: inject context_required ui-direction vào task files

## Objective
Khi Step 1A xác nhận ui-direction artifacts tồn tại, Step 10 (Create Phase Directories) phải inject `context_required` trỏ vào ui-direction vào các task files liên quan đến UI. Đây là mắt xích quan trọng nhất để vp-auto có design context khi implement.

## Paths
- `~/.cursor/viepilot/workflows/crystallize.md`

## File-Level Plan

### `crystallize.md` — Step 10 (Create Phase Directories)

**Tìm section** tạo task files (dòng ~428-439 khoảng "Create: ... tasks/ directory with task files").

**Thêm subsection** ngay sau "Create: ... tasks/ directory with task files":

```markdown
### UI Direction context injection (ENH-024)

If Step 1A confirmed ui-direction artifacts exist (`.viepilot/ui-direction/{session-id}/`):

For each task in a phase whose goal/description involves **UI, frontend, layout, page, component, or design**:
- Add a `context_required` block to the task `.md` file:

```yaml
## Context Required
- `.viepilot/ui-direction/{session-id}/notes.md`  # design intent + Pages inventory
- `.viepilot/ui-direction/{session-id}/style.css`  # shared styling constraints
# Multi-page: also add the relevant page slug(s):
- `.viepilot/ui-direction/{session-id}/pages/{relevant-slug}.html`
```

Rule:
- Use the **latest** session-id found under `.viepilot/ui-direction/` (sort by folder name, take last).
- For multi-page projects: map task scope to relevant page slug(s) from `notes.md ## Pages inventory`; add all pages whose purpose overlaps with the task's implementation scope.
- If unsure which page is relevant, include `index.html` (hub) + the closest-match page.
- Non-UI tasks (pure backend, config, infra): skip injection; do not add ui-direction to context.
```

## Acceptance Criteria
- [ ] Step 10 có subsection "UI Direction context injection (ENH-024)"
- [ ] Subsection chứa `context_required` template block với `notes.md`, `style.css`, `pages/*.html`
- [ ] Có rule: latest session-id, map page slug theo Pages inventory
- [ ] Có rule: non-UI tasks skip injection

## Complexity: M

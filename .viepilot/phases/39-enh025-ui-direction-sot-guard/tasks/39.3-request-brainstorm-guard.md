# Task 39.3 — request.md: brainstorm_continuation guard

## Objective
Thêm guard vào `brainstorm_continuation` step trong `request.md` để ngăn mini-session trong vp-request tự write vào `.viepilot/ui-direction/`.

## File
`~/.cursor/viepilot/workflows/request.md`

## Implementation Notes
Tìm `<step name="brainstorm_continuation">` (khoảng dòng 381). Thêm block ngay sau step header:

```
**ui-direction guard (ENH-025):**
If the brainstorm topic involves changing existing UI design direction (layouts, color system, page structure, style guide):
- Do NOT modify `.viepilot/ui-direction/` files here.
- Redirect: "To update the design direction, please start a new `/vp-brainstorm --ui` session. ui-direction artifacts are the frozen design contract and can only be changed within a brainstorm session (new session-id)."
This mini-session saves to `docs/brainstorm/continuation-*.md` only.
```

## Acceptance Criteria
- [ ] `grep -n "READ-ONLY\|vp-brainstorm --ui\|frozen design contract" ~/.cursor/viepilot/workflows/request.md` trả về ≥1 dòng trong brainstorm_continuation block
- [ ] Guard không block non-UI brainstorm continuation flows

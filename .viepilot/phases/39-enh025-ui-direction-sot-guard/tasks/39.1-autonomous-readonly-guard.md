# Task 39.1 — autonomous.md: READ-ONLY guard

## Objective
Thêm 1 dòng guard vào ENH-024 block trong `autonomous.md` để LLM biết rõ `.viepilot/ui-direction/` là READ-ONLY.

## File
`~/.cursor/viepilot/workflows/autonomous.md`

## Implementation Notes
Tìm block ENH-024 (khoảng dòng 109-124). Sau dòng auto-load (`Read: .viepilot/ui-direction/{latest-session}/notes.md + style.css`), thêm:

```
⛔ READ-ONLY: do NOT create, edit, or delete any file under .viepilot/ui-direction/. This directory is the frozen design contract — only vp-brainstorm may write here. To update ui-direction, open a new /vp-brainstorm session (new session-id).
```

## Acceptance Criteria
- [ ] `grep -n "READ-ONLY" ~/.cursor/viepilot/workflows/autonomous.md` trả về ≥1 dòng trong ENH-024 block
- [ ] Text không break existing flow (chỉ thêm, không xóa/sửa logic khác)

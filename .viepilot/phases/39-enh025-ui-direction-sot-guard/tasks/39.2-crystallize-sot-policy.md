# Task 39.2 — crystallize.md: Source of truth policy

## Objective
Thêm "Source of truth policy" note vào đầu `consume_ui_direction` step trong `crystallize.md`.

## File
`~/.cursor/viepilot/workflows/crystallize.md`

## Implementation Notes
Tìm `<step name="consume_ui_direction">` (khoảng dòng 130). Ngay sau header/opening line của step, thêm block:

```
**Source of truth policy (ENH-025):**
`.viepilot/ui-direction/` is READ-ONLY in this workflow.
- Do NOT create, modify, or delete any file under `.viepilot/ui-direction/`.
- These artifacts are the frozen design contract set during vp-brainstorm.
- To change ui-direction: open a new `/vp-brainstorm` session (creates a new session-id). Never edit an existing session's artifacts.
```

## Acceptance Criteria
- [ ] `grep -n "Source of truth policy" ~/.cursor/viepilot/workflows/crystallize.md` trả về ≥1 dòng
- [ ] Block nằm trong `consume_ui_direction` step, trước nội dung existing

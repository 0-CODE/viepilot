# Task 91.3 — Contract tests + CHANGELOG + version 2.27.0

## Objective
Write ≥12 contract tests for `lib/skill-installer.cjs`, update CHANGELOG.md with [2.27.0], bump package.json to "2.27.0".

## Paths
- `tests/unit/vp-skill-installer.test.js`
- `CHANGELOG.md`
- `package.json`

## Implementation Notes

### Test coverage (≥12)

```
Channel detection
  1. detectChannel("npm:pkg") → "npm"
  2. detectChannel("@org/pkg") → "npm"  (bare npm name)
  3. detectChannel("github:org/repo") → "github"
  4. detectChannel("./local") → "local"
  5. detectChannel("/abs/path") → "local"

installSkill — local channel (easiest to test without network)
  6. installSkill("./src") with valid SKILL.md → ok=true
  7. installSkill("./src") copies SKILL.md to each active adapter dir
  8. installSkill("./src") writes skill-meta.json with source field
  9. installSkill("./src") calls scanSkills() — registry updated
  10. installSkill("./non-existent") → ok=false

uninstallSkill
  11. uninstallSkill(id) removes skill dir from installed adapter dirs
  12. uninstallSkill(id) calls scanSkills() after removal
  13. uninstallSkill("not-installed-id") → ok=false or ok=true with empty removedPaths

CLI integration
  14. --help includes "install-skill"
  15. --help includes "uninstall-skill"
  16. --help includes "update-skill"
```

Use temp home dirs (same pattern as vp-skill-registry.test.js) to avoid side effects.
Mock `child_process.execSync` for npm/github channels to avoid network calls.

### CHANGELOG entry
```markdown
## [2.27.0] - 2026-04-20

### Added (FEAT-020 Phase 2)
- `lib/skill-installer.cjs`: new `installSkill(source, home?)`, `uninstallSkill(id, home?)`, `updateSkill(id, home?)` — multi-channel installer (npm / GitHub / local); writes `skill-meta.json` post-install; auto-runs `scanSkills()`
- `bin/viepilot.cjs`: new `install-skill`, `uninstall-skill`, `update-skill` subcommands
```

## Acceptance Criteria
- [ ] `tests/unit/vp-skill-installer.test.js` exists with ≥12 tests
- [ ] All tests pass (`npm test`)
- [ ] `[2.27.0]` entry in CHANGELOG.md
- [ ] `package.json` version = "2.27.0"

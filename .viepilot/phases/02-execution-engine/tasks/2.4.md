# Task 2.4: Parallel context loading

## Meta
- **Phase**: 02-execution-engine
- **Status**: not_started
- **Complexity**: S
- **Dependencies**: 1.5 (AI-GUIDE static/dynamic boundary)
- **Git Tag**: vp2-p2-t2.4

## Objective

Refactor context loading instruction trong `workflows/autonomous.md` từ sequential ("read X, then read Y") thành parallel batch ("load all in one batch"). Add static/dynamic split và conditional load for logs/blockers.md.

## Paths
```yaml
files_to_modify:
  - workflows/autonomous.md
```

## File-Level Plan
- `workflows/autonomous.md` Step "load_context": Replace sequential read instructions với explicit parallel batch instruction. Add comment về static vs dynamic context. Add conditional: "If HANDOFF.json.recovery.recent_blocker == true: also read logs/blockers.md".

## New Load Instruction Format
```markdown
<step name="load_context">
## Context Load (batch — call all Read tools simultaneously in 1 turn)

Dynamic context (read every task):
- .viepilot/TRACKER.md
- .viepilot/HANDOFF.json
- .viepilot/phases/{current_phase}/PHASE-STATE.md
- .viepilot/phases/{current_phase}/tasks/{current_task}.md

Static context (cached after first read — do NOT re-read per task):
- .viepilot/AI-GUIDE.md
- .viepilot/SYSTEM-RULES.md

Conditional context (only when needed):
- .viepilot/logs/blockers.md → only if HANDOFF.json.recovery.recent_blocker == true
- .viepilot/PROJECT-CONTEXT.md → only if task involves architecture/scope decision
</step>
```

## Acceptance Criteria
- [ ] Load instruction uses "simultaneously" or "one batch" language
- [ ] 4 dynamic files listed
- [ ] Static context labeled với "do NOT re-read per task" note
- [ ] Conditional load for logs/blockers.md với trigger condition
- [ ] Old sequential "Read X, then read Y" language removed

## Do / Don't
### Do
- Simple language change — this is mostly an instruction rewrite, not logic change
- Test: count tool calls on first task — should be 1 turn for all 4 dynamic reads

### Don't
- Không thay đổi WHICH files are read — chỉ thay đổi HOW (parallel vs sequential)
- Không add new files to the read list (scope creep)

## Verification
```yaml
manual:
  - description: "Load instruction clearly states 'simultaneously' or equivalent"
    required: true
  - description: "Static/dynamic split labeled"
    required: true
```

## State Update Checklist
- [ ] Update PHASE-STATE.md task 2.4 → done
- [ ] Update TRACKER.md + HANDOFF.json
- [ ] Git commit: `perf(workflows): parallel context loading in autonomous.md`

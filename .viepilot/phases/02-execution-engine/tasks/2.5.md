# Task 2.5: Sub-task tracking table update in PHASE-STATE.md

## Meta
- **Phase**: 02-execution-engine
- **Status**: not_started
- **Complexity**: S
- **Dependencies**: 1.3 (PHASE-STATE execution_state block), 2.3 (HANDOFF continuous write)
- **Git Tag**: vp2-p2-t2.5

## Objective

Thêm instruction vào `workflows/autonomous.md` để update PHASE-STATE.md sub-task tracking table sau mỗi sub-task PASS hoặc FAIL.

## Paths
```yaml
files_to_modify:
  - workflows/autonomous.md
```

## File-Level Plan
- `workflows/autonomous.md`: Trong execution step, sau mỗi sub-task result → thêm instruction "Update PHASE-STATE.md: mark sub-task {id} as {status} with timestamp". Cũng update `execution_state.current` và `execution_state.status` sau mỗi transition.

## PHASE-STATE Update Points
```
On sub-task start:
  → execution_state.current = sub_task_id
  → execution_state.status = executing

On sub-task PASS:
  → sub-task table: status = pass, completed = timestamp
  → execution_state.recovery_budget.l1/l2/l3_used updated

On sub-task FAIL (before recovery):
  → execution_state.status = recovering_l1 (or l2/l3)

On task PASS:
  → task table: status = pass, completed = timestamp, git_tag = actual_tag
  → execution_state.status = pass
```

## Acceptance Criteria
- [ ] autonomous.md has instruction to update PHASE-STATE.md after each sub-task
- [ ] Sub-task table columns updated: status, completed timestamp
- [ ] execution_state.current updated to reflect current sub-task
- [ ] execution_state.status transitions correctly per state machine
- [ ] recovery_budget.l1/l2/l3_used incremented in execution_state

## Do / Don't
### Do
- Keep update instruction concise in workflow — "Update PHASE-STATE.md: sub-task X = pass"
- Tie into Task 2.2 state machine transitions

### Don't
- Không update PHASE-STATE.md on every minor step — only on sub-task state changes

## Verification
```yaml
manual:
  - description: "After task run: PHASE-STATE.md sub-task table has completed timestamps"
    required: true
  - description: "execution_state.status matches actual task state"
    required: true
```

## State Update Checklist
- [ ] Update PHASE-STATE.md task 2.5 → done
- [ ] Update TRACKER.md + HANDOFF.json
- [ ] Git commit: `feat(workflows): sub-task tracking table update in PHASE-STATE.md`

# Task 17.4 — Add non-interactive flags and install verification

## Objective
Support automation mode with flags and provide post-install verification summary.

## Paths
| Path | Action |
|------|--------|
| `bin/*installer*` | Modify |
| `tests/` | Add/Modify |

## Implementation notes
- Add flags such as `--target`, `--yes`, `--dry-run`.
- Print what was installed and what remains manual.
- Add tests for argument parsing and mode selection.

## Verification
```bash
npx viepilot install --target cursor-agent --yes
```

## State update checklist
- [x] Update PHASE-STATE to `done` when merged.

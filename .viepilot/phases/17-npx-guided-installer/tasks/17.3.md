# Task 17.3 — Implement profile handlers for Claude/Cursor modes

## Objective
Create profile-specific installation handlers for Claude Code, Cursor Agent, and Cursor IDE.

## Paths
| Path | Action |
|------|--------|
| `bin/*installer*` | Modify |
| `install.sh` | Modify (if needed) |
| `dev-install.sh` | Modify (if needed) |

## Implementation notes
- Separate shared setup from profile-specific steps.
- Keep script operations idempotent.
- Surface clear errors for unsupported environments.

## Verification
```bash
npx viepilot install
# choose each profile to validate behavior
```

## State update checklist
- [x] Update PHASE-STATE to `done` when merged.

# Task 17.1 — Add npm bin entrypoint for npx usage

## Objective
Enable `npx viepilot` command discovery via npm package bin configuration and entry script.

## Paths
| Path | Action |
|------|--------|
| `package.json` | Modify |
| `bin/viepilot.cjs` (or equivalent) | Add |

## Implementation notes
- Add `bin` mapping for `viepilot`.
- Ensure entry script routes `install` subcommand to guided installer flow.
- Keep backward compatibility with existing CLI helpers.

## Verification
```bash
npx viepilot --help
```

## State update checklist
- [x] Update PHASE-STATE to `done` when merged.

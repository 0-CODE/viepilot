# Task 25.2 — Crystallize: extract and validate horizon

## Objective
Extend crystallize analysis and roadmap generation so deferred capabilities are never dropped silently.

## Paths
- `workflows/crystallize.md` (Step 1 analyze brainstorm, Step 7 ROADMAP)

## File-Level Plan
- Step 1: checklist items for post-MVP epics, extensibility constraints, explicit “none — single-release product” when applicable.
- Step 7: always emit **executable MVP phases** plus **Post-MVP / horizon** block (epic-level OK); dependencies between MVP and future noted where relevant.
- If brainstorm has post-MVP content but output omits it → stop and ask user (validation).

## Best Practices
- Mirror terminology and section names from `workflows/brainstorm.md` (`## Product horizon`, tier tags).
- Prefer explicit user confirmation over inferring horizon from unstructured prose.
- Keep Step 1 outputs (horizon inventory + single-release flag) traceable into Step 7 so validation can compare.

## Verification
```bash
# Horizon steps and gates appear in crystallize workflow
rg -n "Product horizon|horizon inventory|Post-MVP|single-release" workflows/crystallize.md
```
**Expected:** Multiple matches under Step 1 and Step 7; Step 7 includes a mandatory Post-MVP / horizon block and a “do not silently omit” gate.

## Acceptance Criteria
- [x] Workflow text is unambiguous about mandatory horizon handling.
- [x] Gate documented for single-release vs multi-horizon products.

# Task 25.3 — Templates: ROADMAP + PROJECT-CONTEXT

## Objective
Ship template placeholders so generated projects include horizon and phased vision by default.

## Paths
- `templates/project/ROADMAP.md`
- `templates/project/PROJECT-CONTEXT.md` (glob if name differs)

## File-Level Plan
- ROADMAP: add `## Post-MVP / Product horizon` (or under milestone) with table or bullet epics + “Deferred from MVP” notes.
- PROJECT-CONTEXT: add `## Product vision & phased scope` (MVP boundary, post-MVP themes, anti-goals).

## Best Practices
- Reuse tier wording from `workflows/brainstorm.md` and `workflows/crystallize.md` (`(MVP)` / `(Post-MVP)` / `(Future)`).
- Prefer a few **meaningful** placeholders crystallize can fill as markdown blocks; avoid dozens of empty table cells.
- Keep XML-style sections in `PROJECT-CONTEXT.md` consistent with existing `<domain_knowledge>` pattern.

## Verification
```bash
grep -n "Post-MVP / Product horizon" templates/project/ROADMAP.md
grep -n "Product vision & phased scope" templates/project/PROJECT-CONTEXT.md
npm test
```
**Expected:** both section titles found; tests pass (no template-driven tests required).

## Acceptance Criteria
- [x] Templates render without broken placeholders when filled by crystallize.
- [x] Consistent terminology with workflow (MVP / Post-MVP / Future).

# Task 25.1 — Horizon contract in brainstorm workflow

## Objective
Make brainstorm output structurally capture **MVP vs Post-MVP vs Future** so crystallize can parse intent without losing long-range ideas.

## Paths
- `workflows/brainstorm.md`
- Optionally `docs/user/features/brainstorm.md` or existing brainstorm user doc

## File-Level Plan
- Add session structure: e.g. `## Product horizon`, `### MVP`, `### Post-MVP`, `### Future / exploratory` with bullet tagging convention `(MVP)` / `(Post-MVP)` / `(Future)`.
- Document “non-goals for MVP” vs “deferred capabilities” when user discusses both.

## Implementation Notes
- **`workflows/brainstorm.md`**
  - Insert **Product horizon** as a first-class topic (after core topics or as dedicated subsection under Brainstorm Mode): prompt assistant to capture scope tiers explicitly for `/vp-crystallize`.
  - Extend **§3 Load Context**: when continuing a session, require reading and preserving existing `## Product horizon` (merge edits; never drop the section silently).
  - Extend **§5 Save Session** markdown template: add full `## Product horizon` block with placeholders + tagging rules + `### Non-goals for MVP` + `### Deferred capabilities (from MVP)`.
  - Extend **success_criteria** to include horizon sections when applicable.
- **`docs/user/features/brainstorm.md`** (new): one-page contract for users (horizon tags, crystallize handoff).
- **`docs/README.md`**: add row under Features table linking to `brainstorm.md`.

## Best practices
- Keep workflow text bilingual where existing file mixes EN section titles with VI body (match current style).
- Tags `(MVP)` / `(Post-MVP)` / `(Future)` are suffix conventions on bullets, not separate files.

## Verification
- `rg -n "Product horizon" workflows/brainstorm.md docs/user/features/brainstorm.md` — expect ≥2 hits in workflow, ≥1 in user doc.
- `npm test` — all pass (no code logic change expected).

## Acceptance Criteria
- [x] Workflow instructs saving horizon sections into `docs/brainstorm/session-*.md`.
- [x] Continuing a session preserves horizon blocks.

## Completion
- **2026-04-01**: Implemented in `workflows/brainstorm.md` + `docs/user/features/brainstorm.md`; tags `viepilot-vp-p25-t1-start` / `viepilot-vp-p25-t1-done`.

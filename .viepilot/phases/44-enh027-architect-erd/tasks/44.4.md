# Task 44.4 — workflows/brainstorm.md: ERD triggers + notes.md schema

**Phase**: 44 | **Status**: todo

## What
Update Architect Design Mode section in `workflows/brainstorm.md`.

## Paths
- `workflows/brainstorm.md`

## Implementation Notes
1. Workspace layout: add `erd.html # ERD: entities, attributes, relationships (Mermaid erDiagram)`
2. ERD trigger keywords block: database, entity, table, schema, relation, relationship, foreign key, primary key, ERD, data model, normalization
3. Diagram types: add `erDiagram` alongside graph TD, sequenceDiagram, mindmap
4. notes.md YAML schema: add `## erd` section with entities[] and relationships[]

## Acceptance
- [ ] erd.html in workspace layout
- [ ] ERD trigger keywords listed
- [ ] erDiagram in diagram types
- [ ] ## erd YAML section in notes.md schema

# Task 100.2 — Crystallize Side: entity_mgmt Export + Template + SKILL.md

## Objective
Extend crystallize Step 1D to export `## entity_mgmt` YAML → `## Admin Entity Management`
table in PROJECT-CONTEXT.md. Add `entity-mgmt.html` to MANDATORY READ GATE list. Update
template and SKILL.md.

## Paths
- `workflows/crystallize.md`
- `templates/project/PROJECT-CONTEXT.md`
- `skills/vp-crystallize/SKILL.md`

## Implementation Notes

### A — workflows/crystallize.md: Step 1D new item

**Insert after item 9 (user-data.html / user_data — ENH-066), before current item 10
(feature-map.html cross-reference). Renumber 10 → 11, 11 → 12.**

```
10. **`entity-mgmt.html` / `notes.md ## entity_mgmt`** (if exists — ENH-068) → append to `.viepilot/PROJECT-CONTEXT.md`:
    ```markdown
    ## Admin Entity Management (from Architect Mode)
    | Entity | CRUD Ops | Soft Delete | Bulk Actions | Audit Trail | Scope |
    |--------|----------|-------------|--------------|-------------|-------|
    | —      | —        | —           | —            | —           | —     |
    ```
    Import/Export summary table:
    ```markdown
    ### Import / Export
    | Direction | Entities |
    |-----------|----------|
    | CSV Import | — |
    | CSV Export | — |
    ```
    Records `entity_mgmt_imported: {true/false}` and `entity_mgmt_entity_count: {count or "n/a"}`
    in working notes.
```

### B — workflows/crystallize.md: MANDATORY READ GATE list

Add item 15 after current item 14 (`user-data.html`):

```
15. `entity-mgmt.html` — admin entity CRUD matrix (if present — ENH-068)
```

### C — templates/project/PROJECT-CONTEXT.md

Add `## Admin Entity Management` section after `## User Data Management`:

```markdown
## Admin Entity Management

> Generated by crystallize from architect notes.md ## entity_mgmt (ENH-068).
> Update manually or re-run /vp-crystallize after updating brainstorm artifacts.

| Entity | CRUD Ops | Soft Delete | Bulk Actions | Audit Trail | Scope |
|--------|----------|-------------|--------------|-------------|-------|
| — | — | — | — | — | — |

### Import / Export
| Direction | Entities |
|-----------|----------|
| CSV Import | — |
| CSV Export | — |
```

### D — skills/vp-crystallize/SKILL.md

Add bullet after `**User Data Management Export (ENH-066):**`:

```
**Admin Entity Management Export (ENH-068):**
- Step 1D item 10: if `entity-mgmt.html` or `notes.md ## entity_mgmt` exists in architect
  workspace → append `## Admin Entity Management` table to `.viepilot/PROJECT-CONTEXT.md`
  (columns: Entity | CRUD Ops | Soft Delete | Bulk Actions | Audit Trail | Scope) +
  Import/Export sub-table. Records `entity_mgmt_imported` and `entity_mgmt_entity_count`
  in working notes.
```

## Acceptance Criteria
- [ ] Step 1D item 10 for entity_mgmt export present in crystallize.md
- [ ] Items 10→11, 11→12 correctly renumbered (feature-map cross-ref + working notes record)
- [ ] `entity-mgmt.html` as item 15 in MANDATORY READ GATE list
- [ ] `## Admin Entity Management` section + Import/Export sub-table in PROJECT-CONTEXT.md template
- [ ] `entity_mgmt_imported` and `entity_mgmt_entity_count` in working notes record
- [ ] SKILL.md updated with ENH-068 export bullet

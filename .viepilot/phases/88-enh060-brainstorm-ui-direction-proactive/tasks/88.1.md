# Task 88.1 — Update `workflows/brainstorm.md` — threshold + proactive banner

## Objective
Make UI Direction Mode discovery proactive and comparable to Architect Design Mode by:
1. Adding an **early-session check** that fires on first UI keyword in the initial message
2. Lowering the **surface threshold** from ≥5 to ≥2 accumulated signals
3. Adding a **proactive activation banner** (🎨) modeled after the Architect banner (🏗️)

## Paths
- `workflows/brainstorm.md`

## Implementation Notes

### Current state (lines ~423–445)
```
#### Threshold & accumulation rule
- Count unique keyword occurrences during the ongoing session.
- When ≥3 unique signal occurrences are reached: begin silent accumulation...
- Non-blocking: does not interrupt the main conversation...

#### Surface triggers (when to ask the user)
Display a confirmation dialogue when any of the following conditions occur:
- (a) Topic ends — user types /topic or says "next"
- (b) User types /save or /review
- (c) ≥5 signals accumulated in the buffer
```

### Changes to make

#### 1. Replace threshold & accumulation rule
Replace:
```
- When **≥3 unique signal occurrences** are reached: begin **silent accumulation**...
```
With:
```
- When **≥1 signal occurrence** is detected: begin **silent accumulation**...
```

#### 2. Replace surface threshold (c)
Replace:
```
- (c) **≥5 signals accumulated** in the buffer
```
With:
```
- (c) **≥2 unique signals accumulated** in the buffer
```

#### 3. Add early-session check block
Insert **before** the "#### Threshold & accumulation rule" section:

```markdown
#### Early-session detection (ENH-060)
At the very start of a brainstorm session, scan the user's **initial message** for UI/UX signal keywords. If **≥1 keyword** is found, show the proactive activation banner **immediately** (before topic selection):

```
🎨 I noticed your project involves UI/UX design.
Would you like to activate UI Direction Mode? I can generate an HTML prototype direction alongside our brainstorm.

1. Yes — activate UI Direction Mode (create .viepilot/ui-direction/{session-id}/)
2. Save ideas to notes only (background, no HTML yet)
3. No — continue brainstorm without UI Direction
```

This mirrors **Architect Design Mode** which proactively banners when system architecture heuristics fire.
```

#### 4. Add proactive banner to confirmation dialogue
In the "#### Confirmation dialogue template" section, update the emoji and wording from plain text to match the 🎨 banner style shown above, making it visually consistent with the Architect 🏗️ banner.

## Verification
```bash
grep -c "🎨" workflows/brainstorm.md    # ≥1
grep -c "≥2 unique signals" workflows/brainstorm.md   # ≥1
grep -c "Early-session detection" workflows/brainstorm.md  # ≥1
```

## Acceptance Criteria
- [ ] Early-session check block present (ENH-060)
- [ ] Accumulation threshold updated: ≥1 (was ≥3)
- [ ] Surface threshold updated: ≥2 (was ≥5)
- [ ] Proactive 🎨 banner added
- [ ] Existing confirmation dialogue still intact (updated wording only)
- [ ] All existing Architect Design Mode docs unchanged

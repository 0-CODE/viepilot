# Task 88.2 — Update `skills/vp-brainstorm/SKILL.md` — UI Direction auto-suggestion docs

## Objective
Update the UI Direction Mode documentation in SKILL.md to reflect:
1. UI Direction Mode now auto-suggests itself (like Architect Design Mode)
2. Lower accumulation threshold (≥1 signal, was ≥3)
3. Lower surface threshold (≥2 signals, was ≥5)
4. Early-session detection fires if initial message has ≥1 UI keyword

## Paths
- `skills/vp-brainstorm/SKILL.md`

## Implementation Notes

Find the UI Direction Mode section in SKILL.md (search for "UI Direction" or "ui-direction").

Update the description to state that UI Direction Mode auto-suggests itself — parallel wording to Architect Design Mode. Example:

Current (approximate):
```
**UI Direction Mode** (`--ui`): Generates an HTML prototype direction alongside brainstorm.
Activated via `--ui` flag or after ≥5 accumulated UI signal keywords.
```

Replace with:
```
**UI Direction Mode** (`--ui`): Generates an HTML prototype direction alongside brainstorm.
Auto-suggests itself when ≥1 UI/UX keyword is detected in the initial message (early-session detection, ENH-060), or when ≥2 unique signal keywords accumulate during the session. Shows a proactive 🎨 banner — mirrors Architect Design Mode's 🏗️ proactive activation.
```

Also update any threshold references from ≥3/≥5 to ≥1/≥2.

## Verification
```bash
grep -c "🎨" skills/vp-brainstorm/SKILL.md     # ≥1
grep -c "ENH-060" skills/vp-brainstorm/SKILL.md  # ≥1
grep -c "auto-suggest" skills/vp-brainstorm/SKILL.md  # ≥1
```

## Acceptance Criteria
- [ ] UI Direction Mode description updated to mention auto-suggestion
- [ ] Thresholds updated (≥1 accumulate, ≥2 surface)
- [ ] 🎨 banner reference present
- [ ] ENH-060 tagged

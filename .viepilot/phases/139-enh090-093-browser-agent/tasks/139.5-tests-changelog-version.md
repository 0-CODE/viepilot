# Task 139.5 — Contract Tests + CHANGELOG [3.6.0] + Version Bump

## Objective
Write contract tests (≥12) for Phase 139 deliverables: browser-intake-agent existence and
frontmatter, browser-audit-agent, lib/intake/adapters/browser.cjs exports, lib/audit/browser-runner.cjs
exports, lib/request/url-enricher.cjs URL routing, and skill/workflow documentation checks.
Prepend [3.6.0] to CHANGELOG.md and bump package.json version to 3.6.0.

## Paths
- `tests/unit/phase139-enh090-093-browser-agent.test.js` (new)
- `CHANGELOG.md` (modify — prepend [3.6.0])
- `package.json` (modify — version 3.5.0 → 3.6.0)

## File-Level Plan

### tests/unit/phase139-enh090-093-browser-agent.test.js
Test groups (target ≥12 tests):

```
describe('Phase 139 — ENH-090: browser-intake-agent')
  ✓ browser-intake-agent.md exists in agents/claude-code/
  ✓ browser-intake-agent.md has valid YAML frontmatter (name, model, tools, disallowedTools)
  ✓ name === 'browser-intake-agent', model contains 'claude-sonnet'
  ✓ disallowedTools includes Edit, Write, WebSearch, WebFetch

describe('Phase 139 — ENH-091: browser-audit-agent')
  ✓ browser-audit-agent.md exists in agents/claude-code/
  ✓ browser-audit-agent.md has valid YAML frontmatter
  ✓ name === 'browser-audit-agent'

describe('Phase 139 — ENH-090: lib/intake/adapters/browser.cjs')
  ✓ exports readBrowserUrl and detectUrlType
  ✓ detectUrlType('https://docs.google.com/spreadsheets/d/xxx') === 'google-sheets'
  ✓ detectUrlType('https://github.com/org/repo/issues/42') === 'github-issues'
  ✓ detectUrlType('https://example.com/unknown') === 'generic' or null

describe('Phase 139 — ENH-091: lib/audit/browser-runner.cjs')
  ✓ exports runBrowserAudit, resolveBaseUrl, DEFAULT_BASE_URL
  ✓ DEFAULT_BASE_URL === 'http://localhost:3000'

describe('Phase 139 — ENH-093: lib/request/url-enricher.cjs')
  ✓ detectIssueUrl('https://github.com/org/repo/issues/42') → { matched: true, source: 'github' }
  ✓ detectIssueUrl('https://company.atlassian.net/browse/PROJ-123') → { matched: true, source: 'jira' }
  ✓ detectIssueUrl('https://trello.com/c/abcdef12/card') → { matched: true, source: 'trello' }
  ✓ detectIssueUrl('https://example.com/not-an-issue') → { matched: false }

describe('Phase 139 — skill/workflow docs checks')
  ✓ skills/vp-intake/SKILL.md mentions 'browser' channel type
  ✓ skills/vp-audit/SKILL.md mentions '--visual' flag
  ✓ agents/claude-code/research-agent.md mentions 'browse_url'
  ✓ workflows/request.md mentions URL detection pre-step
```

### CHANGELOG.md
Prepend before current `[3.5.0]` entry:

```markdown
## [3.6.0] — 2026-05-23

### Added
- **ENH-090** `browser-intake-agent` — reads publicly shared URLs (Google Sheets, GitHub Issues, Jira, Trello, Notion) for vp-intake via `vercel-labs/agent-browser`
- **ENH-090** `lib/intake/adapters/browser.cjs` — URL router + type detector for browser channel
- **ENH-090** `skills/vp-intake/SKILL.md` — new `browser` channel type with `url` field
- **ENH-091** `browser-audit-agent` — visual + functional dev server audit (audit_routes, visual_check, accessibility_check)
- **ENH-091** `lib/audit/browser-runner.cjs` — browser audit dispatcher + report writer
- **ENH-091** `skills/vp-audit/SKILL.md` — `--visual` / `--browser <url>` flags
- **ENH-092** `research-agent` upgraded — `browse_url`, `compare_products`, `extract_pricing` ops for JS-rendered research
- **ENH-092** `workflows/brainstorm.md` — URL research sub-block in Step 3
- **ENH-093** `lib/request/url-enricher.cjs` — URL auto-enrichment for GitHub/Linear/Jira/Trello/Notion
- **ENH-093** `workflows/request.md` — URL detection pre-step for auto-fill
- **ENH-093** `skills/vp-request/SKILL.md` — URL auto-enrichment documentation
```

### package.json
Bump: `"version": "3.5.0"` → `"version": "3.6.0"`

## Acceptance Criteria
- [ ] `tests/unit/phase139-enh090-093-browser-agent.test.js` exists with ≥12 tests
- [ ] All ≥12 tests pass (`npm test -- --testPathPattern phase139`)
- [ ] `CHANGELOG.md` has `## [3.6.0]` entry at top with all 4 ENH items listed
- [ ] `package.json` version = `3.6.0`

## Best Practices
- YAML frontmatter parsing: split on `---`, take block between first two `---`, parse `key: value` lines
- Use `require()` to load cjs modules and assert exports exist (no need to invoke browser)
- Agent file tests are file-existence + grep only — no execution
- Test detectUrlType/detectIssueUrl with exact URL strings matching real-world patterns

## Verification
```bash
npm test -- --testPathPattern phase139
node -e "const p=require('./package.json'); console.assert(p.version==='3.6.0')"
grep "3.6.0" CHANGELOG.md
```

## Implementation Notes
_Fill before coding_

# Task 92.3 — Contract tests + CHANGELOG + version 2.28.0

## Objective
Write ≥10 contract tests for brainstorm skill integration (workflow contract), update CHANGELOG.md, bump version.

## Paths
- `tests/unit/brainstorm-skill-integration.test.js`
- `CHANGELOG.md`
- `package.json`

## Implementation Notes

### Test coverage (≥10)

These are **workflow contract tests** — they verify the documented behavior in `workflows/brainstorm.md`, not runtime execution (the workflow is a prompt-instruction document, not runnable code).

```
Workflow document contracts
  1. workflows/brainstorm.md contains "Skill Registry Integration"
  2. workflows/brainstorm.md contains "FEAT-020"
  3. workflows/brainstorm.md contains "loadRegistry"
  4. workflows/brainstorm.md contains "skills_used"
  5. workflows/brainstorm.md contains "Silent" or "silent" (silent apply rule)
  6. workflows/brainstorm.md contains capability-to-signal mapping table

SKILL.md contracts
  7. skills/vp-brainstorm/SKILL.md contains "FEAT-020"
  8. skills/vp-brainstorm/SKILL.md contains "skills_used"
  9. skills/vp-brainstorm/SKILL.md contains "skill-registry"

Registry loader contract
  10. loadRegistry() returns null (no crash) when registry absent
  11. loadRegistry() returns parsed object when registry exists (uses temp home)
```

### CHANGELOG entry
```markdown
## [2.28.0] - 2026-04-20

### Changed (FEAT-020 Phase 3)
- `workflows/brainstorm.md`: Added **Skill Registry Integration** step in UI Direction Mode — when active, loads `~/.viepilot/skill-registry.json`, matches skills by `capabilities` to UI signals, silently applies `best_practices` to HTML generation, records matched skills in `notes.md ## skills_used`
- `skills/vp-brainstorm/SKILL.md`: FEAT-020 skill integration docs — silent apply, crystallize lock reference
```

## Acceptance Criteria
- [ ] `tests/unit/brainstorm-skill-integration.test.js` with ≥10 tests
- [ ] All tests pass
- [ ] `[2.28.0]` in CHANGELOG.md
- [ ] `package.json` version = "2.28.0"

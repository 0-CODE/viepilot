# Task 86.2 — Contract test + CHANGELOG + version 2.22.1

## Objective
Add a contract test verifying the copilot row appears in `--list-targets` output,
ship CHANGELOG patch entry, and bump to 2.22.1.

## Paths
- `tests/unit/vp-cli-list-targets.test.js`
- `CHANGELOG.md`
- `package.json`
- `README.md`

## Contract Tests (≥3 tests)

File: `tests/unit/vp-cli-list-targets.test.js`

```js
'use strict';
const { execSync } = require('child_process');
const path = require('path');

const CLI = path.join(__dirname, '../../bin/viepilot.cjs');

describe('BUG-015: viepilot --list-targets includes all adapters', () => {
  let output;
  beforeAll(() => {
    output = execSync(`node ${CLI} --list-targets`, { encoding: 'utf8' });
  });

  test('--list-targets includes claude-code', () => {
    expect(output).toMatch(/claude-code/);
  });

  test('--list-targets includes codex', () => {
    expect(output).toMatch(/codex/);
  });

  test('--list-targets includes copilot', () => {
    expect(output).toMatch(/copilot/);
  });

  test('--list-targets includes antigravity', () => {
    expect(output).toMatch(/antigravity/);
  });

  test('install --target copilot --dry-run does not throw', () => {
    expect(() => {
      execSync(`node ${CLI} install --target copilot --dry-run --yes`, {
        encoding: 'utf8',
        stdio: 'pipe',
      });
    }).not.toThrow();
  });
});
```

## CHANGELOG Entry
```markdown
## [2.22.1] - 2026-04-18

### Fixed
- **BUG-015**: `copilot` adapter missing from `bin/viepilot.cjs` CLI — `--list-targets` now includes `copilot`; `install --target copilot` no longer fails with "Unknown target"
```

## Acceptance Criteria
- [ ] `tests/unit/vp-cli-list-targets.test.js` exists with ≥5 tests, all passing
- [ ] `CHANGELOG.md` has `[2.22.1]` entry with BUG-015 details
- [ ] `package.json` version = `"2.22.1"`
- [ ] `README.md` badge updated to 2.22.1
- [ ] `npm test` — all tests pass

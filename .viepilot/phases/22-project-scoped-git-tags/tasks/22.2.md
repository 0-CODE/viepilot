# Task 22.2 — Update tag creation in autonomous flow

## Objective
Switch checkpoint creation guidance from generic `vp-p...` tags to project-prefixed tags.

## Paths
- `workflows/autonomous.md`
- `skills/vp-auto/SKILL.md`
- `templates/phase/TASK.md`
- `templates/phase/SUMMARY.md`

## Implementation Notes
- Replace hardcoded tag examples with `{projectPrefix}-vp-p...` style.
- Add command snippet using `vp-tools tag-prefix --raw` to compute deterministic prefix.
- Keep legacy tag references only where needed for compatibility notes.

## Verification
Manual review of updated docs and consistency with CLI command outputs.

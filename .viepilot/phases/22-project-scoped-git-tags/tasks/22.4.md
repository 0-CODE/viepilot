# Task 22.4 — Add tests for tag naming and compatibility

## Objective
Protect new tag-prefix behavior and legacy compatibility with unit tests.

## Paths
- `tests/unit/validators.test.js`
- `lib/cli-shared.cjs`

## File-Level Plan
- Add tests for project slug normalization and tag prefix generation.
- Add CLI subprocess tests for `vp-tools tag-prefix` command.
- Keep existing validator tests stable by not changing unrelated expectations.

## Verification
```bash
npx jest tests/unit/validators.test.js --no-coverage
```

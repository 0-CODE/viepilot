# Task 22.3 — Update checkpoint list/lookup/recovery logic

## Objective
Ensure checkpoint tooling works with both new project-prefixed tags and legacy `vp-*` tags.

## Paths
- `bin/vp-tools.cjs`
- `workflows/rollback.md`
- `workflows/audit.md`

## File-Level Plan
- Update `checkpoints` command tag filter to include new and legacy formats.
- Add parsing guidance for rollback/audit docs to recognize both patterns.
- Keep commands backward-compatible without requiring tag migration.

## Verification
```bash
node bin/vp-tools.cjs checkpoints
```

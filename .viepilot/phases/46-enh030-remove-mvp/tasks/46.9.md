# Task 46.9 — skills: vp-brainstorm + vp-crystallize + vp-task SKILL.md updates

**Phase**: 46 | **Status**: todo

## Paths
- `skills/vp-brainstorm/SKILL.md`
- `skills/vp-crystallize/SKILL.md`
- `skills/vp-task/SKILL.md`

## Implementation Notes

### skills/vp-brainstorm/SKILL.md:
1. Replace bullet `**Product horizon (ENH-014):**` entirely with:
   ```
   - **Phase assignment (ENH-030):** trong mọi session, features/capabilities được gán trực tiếp vào Phase 1, Phase 2, Phase 3... — không dùng MVP/Post-MVP/Future tiers. Session file lưu trong `## Phases` section.
   ```
2. Rule 8 (line ~83): replace "cập nhật `## Product horizon`..." → "cập nhật `## Phases` trong session"
3. Rule 10 (line ~85): remove "Product horizon" from save format list
4. Checklist (line ~100): `"## Product horizon present..."` → `"## Phases present với Phase 1 có nội dung thật"`

### skills/vp-crystallize/SKILL.md:
1. Replace bullet `**Product horizon (ENH-014):**` (line ~102) entirely with:
   ```
   - **Phase assignment (ENH-030):** parse `## Phases` từ brainstorm sessions; build `phases_inventory`; run phase assignment gate (mọi feature phải có phase — full contract: `workflows/crystallize.md` Step 1)
   ```
2. Line ~103: `"Validate completeness (tech stack, features, schema/API clarity, **horizon gate**)"` → remove `horizon gate`, add `phase assignment gate`
3. Line ~161: `"Fill **<product_vision>**... MVP boundary, Post-MVP / Future themes, anti-goals"` → `"Fill **<product_vision>**... Project scope, Phase overview, anti-goals"`
4. Lines ~174–176: `"Use templates/project/ROADMAP.md — executable MVP phases..."` + `"Mandatory Post-MVP / Product horizon..."` → `"Use templates/project/ROADMAP.md — phases in order (Phase 1, Phase 2...); no Post-MVP block"`
5. Checklist (lines ~210–212): replace horizon gate items with phase gate items

### skills/vp-task/SKILL.md:
- Find and remove/replace any `horizon` or `MVP` reference (check the single occurrence found in scan)

## Acceptance
- [ ] vp-brainstorm SKILL.md: no Product horizon (ENH-014), has Phase assignment (ENH-030)
- [ ] vp-crystallize SKILL.md: no horizon gate, has phase assignment gate
- [ ] All 3 files: no `MVP`, `Post-MVP`, `horizon`, `deferred` remaining

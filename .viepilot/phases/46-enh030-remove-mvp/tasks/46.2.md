# Task 46.2 — workflows/crystallize.md: remove Horizon validation gate, add Phase assignment

**Phase**: 46 | **Status**: todo

## Paths
- `workflows/crystallize.md`

## Implementation Notes

### Remove (delete entirely):
1. **Step 1 item 6** (line ~107–113): entire sub-item `**Product horizon (mandatory)**` — parse `## Product horizon`, classify bullets, capture Non-goals/Deferred, single-release mode
2. **Horizon validation gate** (line ~115–118): entire gate block — "Horizon validation gate (before leaving Step 1)" including all 3 checklist items
3. **Step 1 consolidate note** (line ~113): "Consolidate across sessions into a single **horizon inventory**..."
4. **Step 7 Post-MVP block** (line ~485–493): entire paragraph about `## Post-MVP / Product horizon` mandatory section in ROADMAP.md, horizon inventory, single-release statement, stop-and-ask rules
5. **Step 7 item after MVP phases** (line ~486): `3. Define dependencies between MVP phases.` + `4. Post-MVP / product horizon (mandatory section...)` block
6. **feature-map cross-reference** (line ~300): "`**feature-map.html`** → cross-reference with brainstorm `## Product Horizon` (MVP tiers)..." → replace with phase consistency check
7. **AI-GUIDE context loading note** (line ~329): remove `ROADMAP.md horizon` reference
8. **Success criteria** (line ~640,644): remove horizon gate items, update

### Add / Replace with:
1. **Step 1 item 6** replacement — **Phase assignment**:
   ```
   6. **Phase assignment** — parse `## Phases` from brainstorm session(s):
      - Collect features/capabilities listed under each `### Phase N` heading.
      - If a feature has no phase assigned → ask user which phase before continuing.
      - Record `phases_inventory: { phase_1: [...], phase_2: [...], ... }` in working notes.
   ```
2. **Phase validation gate** (replaces Horizon validation gate):
   ```
   **Phase assignment gate (before leaving Step 1):**
   - [ ] Every brainstorm session checked for `## Phases` section OR features explicitly assigned during conversation.
   - [ ] All features have a phase number assigned — no unassigned features.
   - [ ] `phases_inventory` recorded in working notes with at least Phase 1 non-empty.
   ```
3. **Step 7 ROADMAP generation** — replace Post-MVP block with:
   ```
   3. Generate phases in order: Phase 1, Phase 2, Phase 3... from `phases_inventory`.
   4. Each phase: tasks, acceptance criteria, verification commands.
   5. No Post-MVP / horizon block needed — all work is already in phases.
   ```
4. **feature-map cross-reference** replacement: cross-reference feature-map.html Phase badges with `phases_inventory`; flag features in HTML not in inventory or vice versa.
5. **Success criteria** update: remove horizon gate items, add phase gate items

## Acceptance
- [ ] No `## Product horizon`, `horizon inventory`, `horizon validation gate`, `Post-MVP`, `(MVP)`, `(Future)`, `non-goals`, `deferred` in file
- [ ] Phase assignment step present in Step 1
- [ ] Phase assignment gate present
- [ ] ROADMAP generation uses phases_inventory (no Post-MVP block)

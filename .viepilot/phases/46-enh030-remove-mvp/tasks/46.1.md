# Task 46.1 — workflows/brainstorm.md: remove Product Horizon, add Phase assignment

**Phase**: 46 | **Status**: todo

## Paths
- `workflows/brainstorm.md`

## Implementation Notes

### Remove (delete entirely):
1. **ENH-014 rule** (line ~91–96): the entire bullet starting `**Product horizon (ENH-014):**` including sub-bullets about tier tags, non-goals, deferred capabilities, single-release note
2. **Rule 5** (line ~51): sentence mentioning `## Product horizon`, "merge vào section đó", tier tags
3. **`## Product horizon` session template block** (line ~536–558): entire section `## Product horizon` with MVP/Post-MVP/Future/Non-goals/Deferred subsections
4. **Dialogue cadence reference** (line ~452): "cập nhật `## Product horizon` trong bản nháp session... tier phù hợp"
5. **Scope lock gate** (line ~464): "Scope locked: `## Product horizon`..." replace with phase-based check
6. **Checklist item** (line ~690): "`## Product horizon` present với MVP / Post-MVP..." → replace

### Add / Replace with:
1. **Phase assignment rule** (replaces ENH-014 rule):
   ```
   **Phase assignment (ENH-030):** trong quá trình brainstorm, mỗi feature/capability được gán vào một **phase** cụ thể — Phase 1, Phase 2, Phase 3... Không dùng tier MVP/Post-MVP/Future. Nếu user chưa nêu phase, hỏi: "Feature này bạn muốn đưa vào Phase mấy?"
   ```
2. **`## Phases` session template block** (replaces `## Product horizon`):
   ```markdown
   ## Phases
   ### Phase 1
   - {Feature / capability}

   ### Phase 2
   - {Feature / capability}

   ### Phase 3 (và tiếp theo)
   - {Feature / capability}
   ```
3. **Scope lock gate** update: replace horizon check with "tất cả features đã được gán phase"
4. **Checklist** update: "`## Phases` present với ít nhất Phase 1 có nội dung thật"
5. **feature-map.html workspace description** (line ~204): `"Features với tags: layer, phase, MVP/Post-MVP/Future, status"` → `"Features với tags: layer, phase, priority, status"`

## Acceptance
- [ ] No `## Product horizon`, `(MVP)`, `(Post-MVP)`, `(Future)`, `non-goals for MVP`, `deferred capabilities` in file
- [ ] `## Phases` session template present
- [ ] Phase assignment rule present (ENH-030)
- [ ] feature-map.html workspace desc updated

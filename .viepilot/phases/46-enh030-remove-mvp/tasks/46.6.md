# Task 46.6 — templates/project/PROJECT-CONTEXT.md: remove MVP boundary, add Phase scope

**Phase**: 46 | **Status**: todo

## Paths
- `templates/project/PROJECT-CONTEXT.md`

## Implementation Notes

### Remove:
- Line referencing `ROADMAP.md → Post-MVP / Product horizon` and tier tags `(MVP)` / `(Post-MVP)` / `(Future)`
- Section `### MVP boundary (ship first)` + `{{MVP_BOUNDARY}}` placeholder
- Section `### Post-MVP themes` + `{{POST_MVP_VISION}}` placeholder

### Replace with:
```markdown
### Project scope

{{PROJECT_SCOPE}}

### Phase overview
| Phase | Goals | Key features |
|-------|-------|--------------|
| Phase 1 | {{PHASE_1_GOAL}} | {{PHASE_1_FEATURES}} |
| Phase 2 | {{PHASE_2_GOAL}} | {{PHASE_2_FEATURES}} |
```

Keep all other sections (product_vision tag, anti-goals if present) — just remove the MVP-specific framing.

## Acceptance
- [ ] No `MVP boundary`, `Post-MVP themes`, `(MVP)`, `(Post-MVP)`, `(Future)` in file
- [ ] `### Project scope` and `### Phase overview` table present

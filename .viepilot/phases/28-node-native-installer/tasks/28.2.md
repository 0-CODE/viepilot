# Task 28.2 — Thực thi plan cài (`applyInstallPlan`)

## Objective
Triển khai **áp dụng** từng bước trong `buildInstallPlan` lên filesystem: `mkdir`, `copy_file`, `copy_dir`, `symlink_dir`, `chmod`, `note` (cloc guidance), `path_shim` (Unix; bỏ qua Windows).

## Paths
- `lib/viepilot-install.cjs` — `applyInstallPlan`, helpers `copyDirRecursive`, `removePathForOverwrite`, `emitClocInstallGuidance`; mở rộng `buildInstallPlan(..., { overrideHomedir })` cho Jest
- `tests/unit/viepilot-install.test.js` — dry-run apply + install thật vào temp `HOME`
- `.viepilot/phases/28-node-native-installer/PHASE-STATE.md`, `TRACKER.md`, `ROADMAP.md`, `CHANGELOG.md` [Unreleased]

## File-Level Plan
- **`buildInstallPlan`**: thêm option **`overrideHomedir`** (chỉ test / advanced) để mọi path dưới `~/.cursor` trỏ tới thư mục tạm.
- **`copyDirRecursive`**: `fs.cpSync` + `force: true` nếu có; fallback đệ quy đọc/ghi file.
- **`applyInstallPlan(plan, { dryRun })`**: duyệt `plan.steps` theo thứ tự; dry-run chỉ ghi `logs` không ghi disk; thật thì thực hiện I/O; `path_shim` chỉ khi `process.platform !== 'win32'`, bắt `EACCES` và append log (không throw nếu muốn best-effort giống bash sudo hint — dùng log cảnh báo).
- **`symlink_dir`**: xóa `target` nếu tồn tại; Windows dùng `fs.symlink(..., 'dir')`, khác `dir` hoặc `junction` tùy platform.
- **`note` / cloc**: in/log cùng nội dung hướng dẫn như `install.sh` (không chặn cài); không `read` stdin trong task này (NPX luôn AUTO_YES).
- Export `applyInstallPlan` từ module.

## Verification
- `npm test` — pass
- `node --check lib/viepilot-install.cjs`

## Status
done (2026-04-01)

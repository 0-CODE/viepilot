# Task 28.3 — `bin/viepilot.cjs` install dùng Node (không `bash install.sh`)

## Objective
Lệnh `viepilot install` gọi `buildInstallPlan` + `applyInstallPlan` từ `lib/viepilot-install.cjs`; **gỡ** `spawnSync('bash', [install.sh])`. Giữ UX: chọn target(s), `--dry-run` in `[dry-run]`, summary theo từng target (cùng kết quả một lần cài).

## Paths
- `bin/viepilot.cjs` — import install lib, `runInstallWithNode` (inline hoặc tách), xóa `handlerForTarget` / `runInstaller` bash
- `tests/unit/guided-installer.test.js` — điều chỉnh assertion dry-run nếu cần (vẫn có `[dry-run]`)
- `CHANGELOG.md` [Unreleased]

## File-Level Plan
- Resolve `packageRoot` bằng `resolveViepilotPackageRoot(path.join(__dirname, '..'))` fallback `..`.
- Merge env: `VIEPILOT_AUTO_YES=1`, `VIEPILOT_INSTALL_PROFILE` = **target đầu** trong list (giống hành vi informational cũ); giữ `VIEPILOT_SYMLINK_SKILLS` / `VIEPILOT_ADD_PATH` từ `process.env`.
- `wantPathShim`: `process.env.VIEPILOT_ADD_PATH === '1'` (khớp install.sh khi AUTO_YES).
- **Một lần** `buildInstallPlan` + `applyInstallPlan` cho cả batch target (nội dung cài giống nhau); map kết quả sang mảng `results` cho summary từng dòng.
- `--dry-run`: `applyInstallPlan(..., { dryRun: true })` để log có tiền tố `[dry-run]`.
- Xóa import `spawnSync` nếu không còn dùng.
- (Nhỏ) `computeUninstallPaths`: `HOME` fallback `os.homedir()` cho Windows.

## Verification
- `npm test`
- `npm run lint:cli`
- `node bin/viepilot.cjs install --target cursor-agent --yes --dry-run` (stdout có `[dry-run]`)

## Status
done (2026-04-01)

# Task 28.4 — `install.sh` thin wrapper → Node (`viepilot.cjs install`)

## Objective
`./install.sh` không còn logic copy bash; **ủy quyền** `node bin/viepilot.cjs install` sau khi giữ (khi cần) prompt xác nhận, `cloc` best-effort, và PATH (`VIEPILOT_ADD_PATH`). Cập nhật **troubleshooting** — `npx` không còn gọi `bash install.sh`.

## Paths
- `install.sh` — rewrite wrapper
- `docs/troubleshooting.md` — bảng npx vs clone, Windows / bash
- `tests/unit/guided-installer.test.js` hoặc `viepilot-install.test.js` — smoke `bash install.sh` + `VIEPILOT_AUTO_YES=1` + `VIEPILOT_INSTALL_DRY_RUN=1` + `HOME=tmp`

## File-Level Plan
- Giữ banner, kiểm tra `README.md` + `skills/`, in đường dẫn, biến `VIEPILOT_SYMLINK_SKILLS` / `INSTALL_PROFILE` / `ADD_PATH_CHOICE` như cũ.
- Giữ `install_cloc_best_effort` + confirm `Continue?` khi không `VIEPILOT_AUTO_YES=1`.
- Prompt PATH: nếu interactive → `export VIEPILOT_ADD_PATH=1`; nếu `AUTO_YES` + `ADD_PATH_CHOICE=1` → export (Node `applyInstallPlan` làm `path_shim`).
- `export VIEPILOT_AUTO_YES=1` trước khi gọi Node (sau khi user đã confirm).
- Hỗ trợ `VIEPILOT_INSTALL_DRY_RUN=1` → thêm `--dry-run` cho test/CI.
- `node "$SCRIPT_DIR/bin/viepilot.cjs" install --target "$INSTALL_PROFILE" --yes` (+ optional `--dry-run`).

## Verification
- `npm test`
- `HOME=$(mktemp -d) VIEPILOT_AUTO_YES=1 VIEPILOT_INSTALL_DRY_RUN=1 bash install.sh` từ root repo (status 0, stdout có Node/dry-run)

## Status
done (2026-04-01)

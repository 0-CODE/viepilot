# Task 28.1 — Map `install.sh` → Node API; scaffold `lib/viepilot-install.cjs`

## Objective
Chuyển toàn bộ bước cài từ `install.sh` thành **cấu trúc kế hoạch** (JSON) có thể in dry-run; module load được từ repo; **chưa** thay `bin/viepilot.cjs` (task 28.3).

## Paths
- `lib/viepilot-install.cjs` (new) — `normalizeInstallEnv`, `validateViepilotPackageRoot`, `listSkillDirNames`, `buildInstallPlan`, `formatPlanLines`
- `tests/unit/viepilot-install.test.js` (new) — plan shape + layout validation trên root thật của repo
- `.viepilot/phases/28-node-native-installer/PHASE-STATE.md`
- `.viepilot/TRACKER.md`, `.viepilot/HANDOFF.json`, `.viepilot/ROADMAP.md` (progress 28.1)
- `CHANGELOG.md` [Unreleased] — ghi nhận scaffold

## File-Level Plan
- **`viepilot-install.cjs`**: Dùng `resolveViepilotPackageRoot` từ `viepilot-info.cjs` (tránh duplicate walk). `validateViepilotPackageRoot` giống guard bash: tồn tại `README.md` và thư mục `skills/`. `buildInstallPlan(root, env)` đọc tên thư mục con `skills/`, `workflows/`, v.v. và sinh danh sách bước tương ứng: `mkdir`, `copy_file`, `copy_dir` (hoặc `symlink_dir` khi `VIEPILOT_SYMLINK_SKILLS=1`), `chmod`, `note` (cloc hướng dẫn), `optional_path_symlink` (khi user chọn add PATH — biểu diễn bằng flag `wantPathShim` trong options vì Node batch không có `read`). Môi trường: `VIEPILOT_AUTO_YES`, `VIEPILOT_INSTALL_PROFILE`, `VIEPILOT_ADD_PATH`, `VIEPILOT_SYMLINK_SKILLS` như bash.
- **Test**: Gọi `buildInstallPlan` với `packageRoot` = repo root; assert có bước mkdir cho `~/.cursor/skills`, có bước copy `vp-tools.cjs` / `viepilot.cjs`, và `formatPlanLines` trả về string không rỗng.

## Best practices
- CommonJS, comment tiếng Anh; không mutate global; paths qua `path.join` + `os.homedir()` cho home cursor.

## Verification
- `npm test` — pass, gồm `tests/unit/viepilot-install.test.js`
- `node --check lib/viepilot-install.cjs`

## Status
done (2026-04-01)

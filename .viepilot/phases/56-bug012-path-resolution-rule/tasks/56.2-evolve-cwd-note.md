# Task 56.2 — Strengthen TASK PATH RULE in evolve.md with cwd-resolution note

## Goal
Add a one-sentence cwd-resolution clarification to the TASK PATH RULE section in `workflows/evolve.md`, so that task files generated by evolve carry an explicit reminder that paths resolve from `{cwd}`.

## Paths
```
workflows/evolve.md
```

## Implementation Notes

In the `### ⚠️ TASK PATH RULE (BUG-009)` section (around line 106), after the existing CORRECT/INCORRECT examples block, add:

```markdown
**Resolution rule (BUG-012):** Paths in `## Paths` are always resolved from `{cwd}` (the repo root where `package.json` lives) — never from `~/.claude/`, `~/.cursor/`, or any install directory. When both a codebase copy and an installed copy exist, `{cwd}` wins.
```

This is additive — no existing text is removed, just one clarifying paragraph appended.

## Acceptance Criteria
- [ ] `evolve.md` TASK PATH RULE section contains `BUG-012` marker with cwd-resolution rule
- [ ] Text references `{cwd}` as the resolution anchor
- [ ] Text mentions `~/.claude/` and `~/.cursor/` as forbidden destinations
- [ ] No existing BUG-009 text removed

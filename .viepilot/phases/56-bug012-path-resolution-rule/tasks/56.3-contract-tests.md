# Task 56.3 — Contract tests for BUG-012 PATH RESOLUTION RULE

## Goal
Add `tests/unit/vp-bug012-path-resolution-rule.test.js` with 3 contract tests verifying both files have the required rule text.

## Paths
```
tests/unit/vp-bug012-path-resolution-rule.test.js
```

## Tests to write

```js
describe('BUG-012 PATH RESOLUTION RULE — codebase vs install', () => {

  test('autonomous.md: has PATH RESOLUTION RULE (BUG-012) block after BUG-009 preflight')
  // Expect: /⛔.*PATH RESOLUTION RULE.*BUG-012/
  // Expect: block appears after the BUG-009 preflight text
  // Expect: block contains "cwd" or "{cwd}"
  // Expect: block contains "~/.claude/" or "install" as forbidden destination

  test('autonomous.md: PATH RESOLUTION RULE explicitly forbids ~/.claude/ and ~/.cursor/')
  // Expect: text contains "~/.claude/"
  // Expect: text contains "~/.cursor/" OR "install director"

  test('evolve.md: TASK PATH RULE has BUG-012 cwd-resolution clarification')
  // Expect: /BUG-012/ appears in evolve.md
  // Expect: "{cwd}" appears in the BUG-012 section

})
```

## Acceptance Criteria
- [ ] All 3 tests pass
- [ ] `npm test` green — no regression on BUG-009 tests or other existing tests
- [ ] Test file uses same `read(relPath)` helper pattern as other contract tests

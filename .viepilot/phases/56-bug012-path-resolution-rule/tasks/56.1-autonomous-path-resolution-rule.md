# Task 56.1 — Add PATH RESOLUTION RULE to autonomous.md

## Goal
Add an explicit `⛔ PATH RESOLUTION RULE (BUG-012)` block to `workflows/autonomous.md`, immediately after the existing BUG-009 preflight block. This rule tells the LLM that all repo-relative paths must be resolved from `{cwd}` (the working directory / repo root), never from install directories.

## Paths
```
workflows/autonomous.md
```

## Implementation Notes

Insert the following block **immediately after** the BUG-009 preflight section (after the line `It does NOT fire on paths inside code block content within the task (only the \`## Paths\` header block is validated).`):

```markdown
#### ⛔ PATH RESOLUTION RULE (BUG-012)

**All file reads and edits during task execution MUST be resolved from `{cwd}`** — the repository root where `package.json` lives (e.g. `/Users/.../my-project/`).

```
WHEN executing a task with:
  ## Paths
  workflows/brainstorm.md

→ Read and edit: {cwd}/workflows/brainstorm.md
→ NEVER:         ~/.claude/viepilot/workflows/brainstorm.md
→ NEVER:         ~/.cursor/viepilot/workflows/brainstorm.md
→ NEVER:         any path outside {cwd}
```

**Rule**: If a file exists at both `{cwd}/workflows/foo.md` (codebase) and `~/.claude/viepilot/workflows/foo.md` (production install), **ALWAYS use the `{cwd}` copy**.

The install directory (`~/.claude/`, `~/.cursor/`) is a **deployment artifact** — it is populated by `dev-install.sh`. Editing it directly bypasses version control and will be silently overwritten on the next install.
```

## Acceptance Criteria
- [ ] Block with `⛔ PATH RESOLUTION RULE (BUG-012)` text present in `autonomous.md`
- [ ] Block appears after BUG-009 preflight block
- [ ] Block contains "NEVER" references to `~/.claude/` and `~/.cursor/`
- [ ] Block explains `{cwd}` as repo root
- [ ] Block states that install dir is overwritten by `dev-install.sh`

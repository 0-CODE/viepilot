# Task 77.2 — Gap B: Polyrepo Hints Detection + Prompt + Schema

## Objective
After Signal Category 1 runs, add a **Polyrepo Detection** subsection to `workflows/crystallize.md`. Define 6 signal sources that indicate the scanned repo is part of a larger multi-repo system. Output `polyrepo_hints[]` and `related_repos[]` in the Scan Report schema. Define the interactive prompt rule and the gap-fill fallback.

## Paths
```
workflows/crystallize.md
```

## File-Level Plan

### Location in `workflows/crystallize.md`

Insert a new **Polyrepo Detection** subsection after the Git Submodule Detection block added in Task 77.1 (still within Signal Category 1, before the "Signal Category 2" heading).

### Content to add

**New subsection: "Polyrepo / Multi-Repo Detection"**

```markdown
**Polyrepo / Multi-Repo Detection** (run after submodule check):

Scan the following signals to determine if this repo is part of a larger multi-repo system:

| Signal Source | Pattern | Interpretation |
|--------------|---------|---------------|
| `docker-compose.yml` / `docker-compose*.yml` | `build: ../path` or `context: ../path` (value starts with `../`) | Sibling repo used as build context |
| `docker-compose.yml` | Multiple services, each pointing to a different external image with no local `build:` | External microservices — possible sibling repos |
| `package.json` | `"dependencies"` or `"devDependencies"` value matching `"file:../..."` | Local file: sibling repo dep |
| `.github/workflows/*.yml` / `.gitlab-ci.yml` / `ci/*.yml` | `git clone` steps, or `uses: org/other-repo/.github/workflows/` referencing a different repo | CI clones or calls a sibling repo |
| `README.md` / `CONTRIBUTING.md` | Lines containing `https://github.com/...` or `https://gitlab.com/...` whose repo path differs from the current repo's remote | Related repo link in docs |
| `Makefile` / `justfile` | Targets containing `cd ../` followed by build/test commands | Cross-repo build orchestration |

**Result:**
- For each match: record `{ source, hint, inferred_repo }` in `polyrepo_hints[]`
- Deduplicate by `inferred_repo` name
- If `polyrepo_hints` is empty: skip this section entirely (no empty array in clean single-repo reports)

**Interactive prompt (fire when `polyrepo_hints` non-empty):**
```
⚠️ Polyrepo signals detected:
  {list polyrepo_hints}

This repo appears to be part of a multi-repo system.
Would you like to list related repos? (optional — press Enter to skip)
Format: one URL per line, optionally with role: https://github.com/org/api-service [backend]
```
- User-supplied repos → stored in `related_repos[]` as `{ url, role }`
- If user skips → `related_repos: []`; system-level context fields (e.g. `deployment_topology`) set to ASSUMED tier

**Gap-fill rule:**
- `polyrepo_hints` non-empty AND `related_repos` empty → system-level fields = ASSUMED (not MISSING; single-repo scan is still valid)
- `related_repos` populated → system-level fields = DETECTED for user-supplied context
```

### Scan Report schema additions

In the Scan Report Schema section, add after `modules[]`:

```yaml
polyrepo_hints:                    # present only when signals detected
  - source: docker-compose.yml
    hint: "build: ../api-service"
    inferred_repo: api-service
  - source: package.json
    hint: '"utils": "file:../shared-utils"'
    inferred_repo: shared-utils
related_repos:                     # present only when user provided input
  - url: https://github.com/org/api-service
    role: backend
  - url: https://github.com/org/shared-utils
    role: shared-library
```

## Acceptance Criteria
- [ ] Polyrepo Detection subsection present in Signal Category 1
- [ ] All 6 signal sources listed in a table with source, pattern, interpretation columns
- [ ] `polyrepo_hints[]` field defined in schema with `source`, `hint`, `inferred_repo` sub-fields
- [ ] Rule: polyrepo_hints absent from report when empty (clean single-repo output)
- [ ] Interactive prompt text defined
- [ ] `related_repos[]` field defined in schema with `url`, `role` sub-fields
- [ ] Gap-fill rule documented: hints without related_repos → ASSUMED for system-level fields

## Verification
```bash
grep -n "polyrepo\|related_repos\|Polyrepo" workflows/crystallize.md
# Expected: multiple matches — detection table, schema, prompt, gap-fill rule

grep -n "file:\.\./" workflows/crystallize.md
# Expected: match in the package.json signal row
```

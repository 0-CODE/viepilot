# Task 77.4 — Tests + CHANGELOG 2.14.0 + package.json

## Objective
Write ≥8 tests covering Gaps A, B, and C. Update `CHANGELOG.md` with `[2.14.0]` section. Bump `package.json` to `2.14.0`.

## Paths
```
tests/unit/vp-enh047-multi-repo.test.js
CHANGELOG.md
package.json
```

## File-Level Plan

### `tests/unit/vp-enh047-multi-repo.test.js`

Follow the content-pattern test style used in all other `vp-*` unit tests (read file, match regex).

**Test groups and minimum cases:**

```
describe('workflows/crystallize.md — Gap A: Git Submodule Detection')
  test: .gitmodules detection step documented
  test: submodule fields: type: submodule, submodule_url, initialized in schema
  test: safety rule "never run git submodule update" present

describe('workflows/crystallize.md — Gap B: Polyrepo Detection')
  test: Polyrepo Detection subsection present
  test: docker-compose ../path signal listed
  test: file:../ (package.json) signal listed
  test: polyrepo_hints[] field in schema
  test: related_repos[] field in schema
  test: gap-fill rule: hints without related_repos → ASSUMED documented

describe('workflows/crystallize.md — Gap C: Per-Module Gap Detection')
  test: must_detect_status field documented
  test: gap_tier field on module entries documented
  test: root rollup rule (worst tier) documented
  test: pause-and-ask rule for MISSING module documented
  test: module_purpose and entry_point fields in schema

describe('skills/vp-crystallize/SKILL.md — Gap A+B+C summary')
  test: must_detect_status mentioned
  test: polyrepo_hints mentioned
```

Target: ≥15 tests total (8 minimum from ENH-047 AC, aiming higher for full coverage).

### `CHANGELOG.md`

Add above `[2.13.0]`:

```markdown
## [2.14.0] - 2026-04-13

### Added (ENH-047 — Phase 77: Brownfield Multi-Repo + Per-Module Gap Detection)

**Gap A — Git Submodule Detection:**
- `workflows/crystallize.md`: Signal Category 1 now detects `.gitmodules` → parses all
  `[submodule]` blocks (name, path, url); runs Signal Cat 1+2+4 on initialized paths;
  records uninitialized paths with `initialized: false` + MISSING primary_language
- Scan Report `modules[]`: new fields `type` (submodule/workspace/root),
  `submodule_url`, `initialized`
- Safety rule: scanner never runs `git submodule update` — read-only filesystem scan

**Gap B — Polyrepo / Multi-Repo Detection:**
- `workflows/crystallize.md`: new Polyrepo Detection subsection in Signal Category 1
  with 6 signal sources (docker-compose `../` paths, `file:../` deps, CI cross-repo
  clones, README external links, Makefile `cd ../` targets)
- Scan Report: new `polyrepo_hints[]` + `related_repos[]` fields
- Interactive prompt fires when polyrepo signals detected; user can supply related repo URLs
- Gap-fill rule: hints without `related_repos` → system-level context = ASSUMED tier

**Gap C — Per-Module Gap Detection:**
- `workflows/crystallize.md`: every `modules[]` entry now carries `gap_tier`
  (DETECTED/ASSUMED/MISSING), `must_detect_status{}` (evidence per MUST-DETECT field
  with `value`, `source`, `tier`), `open_questions[]`
- 4 per-module MUST-DETECT fields: `primary_language`, `framework`, `module_purpose`,
  `entry_point`
- Root gap tier = worst tier across all modules
- Module with `gap_tier: MISSING` blocks artifact generation with targeted user prompt
- `skills/vp-crystallize/SKILL.md`: Brownfield Mode section updated to cover Gaps A+B+C
```

### `package.json`
- `"version": "2.13.0"` → `"version": "2.14.0"`

## Acceptance Criteria
- [ ] `tests/unit/vp-enh047-multi-repo.test.js` exists
- [ ] ≥8 tests (aim ≥15) — all passing
- [ ] Tests cover: `.gitmodules` detection, submodule schema fields, safety rule, polyrepo table, `polyrepo_hints[]`, `related_repos[]`, gap-fill ASSUMED rule, `must_detect_status`, `gap_tier`, root rollup, pause-and-ask, SKILL.md mentions
- [ ] `CHANGELOG.md` has `[2.14.0]` section with Gaps A+B+C described
- [ ] `package.json` version = `"2.14.0"`
- [ ] Full test suite (`npx jest`) passes — no regressions

## Verification
```bash
npx jest vp-enh047
# Expected: all tests pass

npx jest
# Expected: all suites pass (no regressions)

grep '"version"' package.json
# Expected: "version": "2.14.0"

grep '\[2.14.0\]' CHANGELOG.md
# Expected: match found
```

# Task 4.6b: Stale reference audit — workflow cross-references

## Meta
- **Phase**: 04-verification-docs
- **Status**: not_started
- **Complexity**: S
- **Dependencies**: Phase 1 + 2 + 3 complete
- **Git Tag**: vp2-p4-t4.6b

## Objective

Audit tất cả `workflows/*.md` files: verify template paths still valid, .viepilot/ references correct for v2 structure.

## Paths
```yaml
files_to_modify:
  - workflows/*.md   # only if stale refs found
```

## Audit Checklist
```bash
# Template references in workflows:
grep -r "templates/" workflows/
# Verify each referenced template exists

# TRACKER.md structure references:
grep -r "Decision Log\|Version History\|Blockers" workflows/
# These should now reference logs/ directory, not inline TRACKER sections

# HANDOFF.json field references:
grep -r "HANDOFF.json" workflows/
# Verify field names match v2 schema

# PHASE-STATE.md section references:
grep -r "PHASE-STATE" workflows/
# Verify section names match v2 template
```

## Acceptance Criteria
- [ ] All template path references in workflows/ resolve
- [ ] Inline TRACKER.md Decision Log references updated to logs/decisions.md
- [ ] HANDOFF.json field references match v2 schema (position.sub_task, recovery.*, etc.)
- [ ] PHASE-STATE.md section references match v2 template (execution_state block)
- [ ] Zero stale references remaining

## State Update Checklist
- [ ] Update PHASE-STATE.md task 4.6b → done
- [ ] Update TRACKER.md + HANDOFF.json
- [ ] Git commit (if fixes needed): `fix(workflows): resolve stale cross-references`
- [ ] Git commit (if no fixes): `chore: stale reference audit 4.6b — workflows clean`

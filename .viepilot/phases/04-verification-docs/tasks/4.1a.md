# Task 4.1a: Integration test — small S/M project

## Meta
- **Phase**: 04-verification-docs
- **Status**: not_started
- **Complexity**: M
- **Dependencies**: Phase 1 + 2 + 3 all complete
- **Git Tag**: vp2-p4-t4.1a

## Objective

End-to-end integration test với fresh small project: brainstorm → crystallize → auto. Force lint error trên M task để verify silent L1 recovery fires.

## Paths
```yaml
files_to_create:
  - tests/integration/test-small-project/  # temp test dir (gitignored)
```

## Test Script
```
1. Create temp test project: mkdir /tmp/vp-test-small && git init
2. Run /vp-brainstorm → create minimal session (2-3 topics, 1 decision)
3. Run /vp-crystallize → generate .viepilot/ artifacts (S task + 1 M task)
4. Before /vp-auto: inject lint error into M task's target file
5. Run /vp-auto on M task
6. Verify: task PASS without user-visible failure
7. Check HANDOFF.log: l1_recovery event present
8. Check HANDOFF.json: recovery.l1_attempts = 1
```

## Acceptance Criteria
- [ ] Fresh project crystallizes correctly (all v2 artifacts generated)
- [ ] vp-auto runs S task → PASS without issues
- [ ] vp-auto runs M task → lint error → L1 recovery fires silently → PASS
- [ ] User did NOT see failure message during L1 recovery
- [ ] HANDOFF.log has: task_start, l1_recovery, task_pass events
- [ ] HANDOFF.json recovery.l1_attempts = 1 after task

## Do / Don't
### Do
- Use /tmp/ for test project — không contaminate viepilot repo
- Document test steps and actual results in this task file under Implementation Notes

### Don't
- Không commit test project files to viepilot repo
- Không use existing project for test — must be fresh to verify crystallize

## Verification
```yaml
manual:
  - description: "All 6 acceptance criteria checked and documented"
    required: true
```

## State Update Checklist
- [ ] Update PHASE-STATE.md task 4.1a → done
- [ ] Update TRACKER.md + HANDOFF.json
- [ ] Git commit: `test: integration test 4.1a — small project silent L1 recovery verified`

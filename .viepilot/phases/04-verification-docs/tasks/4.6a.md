# Task 4.6a: Stale reference audit — SKILL.md files

## Meta
- **Phase**: 04-verification-docs
- **Status**: not_started
- **Complexity**: S
- **Dependencies**: Phase 1 + 3 complete (template paths changed)
- **Git Tag**: vp2-p4-t4.6a

## Objective

Audit tất cả `skills/vp-*/SKILL.md` files: verify no stale paths to old template locations, verify workflow references still valid.

## Paths
```yaml
files_to_modify:
  - skills/vp-*/SKILL.md   # only if stale refs found
```

## Audit Checklist
```bash
# Find all workflow references in skills:
grep -r "@.*workflows/" skills/ 
# Verify each path exists

# Find any template path references:
grep -r "templates/" skills/
# Verify each path exists

# Find any .viepilot/ path references:
grep -r "\.viepilot/" skills/
# Verify paths still valid in v2 structure
```

## Acceptance Criteria
- [ ] All workflow references in skills/ resolve to existing files
- [ ] All template references in skills/ resolve to existing templates
- [ ] All .viepilot/ path references valid in v2 structure
- [ ] paths: frontmatter values valid (from Task 3.7)
- [ ] Zero stale references remaining

## State Update Checklist
- [ ] Update PHASE-STATE.md task 4.6a → done
- [ ] Update TRACKER.md + HANDOFF.json
- [ ] Git commit (if fixes needed): `fix(skills): resolve stale path references`
- [ ] Git commit (if no fixes): `chore: stale reference audit 4.6a — skills clean`

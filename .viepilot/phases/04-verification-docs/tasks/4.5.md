# Task 4.5: CHANGELOG.md v2.0.0 entry

## Meta
- **Phase**: 04-verification-docs
- **Status**: not_started
- **Complexity**: S
- **Dependencies**: All other Phase 4 tasks (write last)
- **Git Tag**: vp2-p4-t4.5

## Objective

Update `CHANGELOG.md`: promote `[Unreleased]` section to `[2.0.0] - {date}` với full feature list của v2.

## Paths
```yaml
files_to_modify:
  - CHANGELOG.md
```

## Changes Needed
Rename `[Unreleased]` → `[2.0.0] - 2026-04-{date}` và add comprehensive Added/Changed list:

```markdown
## [2.0.0] - 2026-04-XX

### Added
- 3-layer silent recovery engine (L1 lint / L2 test fix / L3 scope reduce)
- Recovery budget per task complexity (S/M/L/XL)
- recovery_overrides per-task (block L1/L2/L3 independently)
- Compliance domain auto-detection → L3 block (auth/payment/data/crypto)
- Continuous HANDOFF.json write after each sub-task
- HANDOFF.log append-only audit trail with phase-boundary rotation
- Typed state machine in vp-auto (8 named states)
- 3-tier validation pipeline (contract → write_scope lock → git gate)
- Scope drift detection post-task
- Control point state protocol (HANDOFF.json flag)
- vp-resume tiered restore (quick/standard/full)
- vp-status: control point banner + recovery stats
- vp-request NLP intake (description-first, 2-band confidence)
- vp-request: UI direction route detection
- vp-request: horizon-aware routing
- crystallize: auto-populate write_scope/type/recovery_budget
- crystallize: compliance domain detection → L3 block
- Parallel context loading in vp-auto
- Static/dynamic context boundary in AI-GUIDE.md
- paths: frontmatter for conditional skill activation
- TASK.md: type, write_scope, recovery_budget, can_parallel_with, recovery_overrides
- PHASE-STATE.md: execution_state block with typed transitions
- TRACKER.md: index ≤30 lines + logs/ directory

### Changed
- TRACKER.md template: blob → index + logs/ directory structure
- PHASE-STATE.md template: added execution_state YAML block
- TASK.md template: added Task Metadata section
- HANDOFF.json schema: v2 with position.sub_task, recovery.*, context.active_stacks, control_point.*
- vp-request: menu-first → description-first NLP intake
- vp-evolve: generates v2 TASK.md template

### Fixed
- Context loss on session reset (resolved by continuous HANDOFF write)
- Silent scope drift (now detected and escalated to control point)
- Over-eager control points for lint errors (now silent L1 recovery)
```

## Acceptance Criteria
- [ ] `[Unreleased]` promoted to `[2.0.0] - {date}`
- [ ] Added section has all major v2 features
- [ ] Changed section documents breaking changes
- [ ] New `[Unreleased]` section created (empty, for future work)
- [ ] Keep a Changelog format maintained

## State Update Checklist
- [ ] Update PHASE-STATE.md task 4.5 → done
- [ ] Update TRACKER.md + HANDOFF.json
- [ ] Git commit: `docs: CHANGELOG.md v2.0.0 release entry`

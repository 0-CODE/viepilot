# Task 4.1b: Integration test — large L/XL project

## Meta
- **Phase**: 04-verification-docs
- **Status**: not_started
- **Complexity**: M
- **Dependencies**: 4.1a complete (baseline verified)
- **Git Tag**: vp2-p4-t4.1b

## Objective

Multi-phase integration test với L/XL project: verify HANDOFF continuity across context resets và scope lock enforcement.

## Test Scenarios

**Scenario B1 — HANDOFF continuity**:
1. Create test project with 2 phases, 3+ tasks each
2. Run vp-auto through Phase 1 (all tasks PASS)
3. Simulate context reset (kill session, restart)
4. Run /vp-resume → verify position correct
5. Continue vp-auto Phase 2 → verify correct task picked up

**Scenario B2 — Scope lock enforcement**:
1. Create task with `write_scope: ["src/feature-a/"]`
2. Prompt AI to also modify `src/feature-b/` (out of scope)
3. Verify Tier 2 validation catches → control_point triggered
4. Verify HANDOFF.log has scope_drift event

## Acceptance Criteria
- [ ] B1: vp-resume after context reset → correct phase/task position
- [ ] B1: Phase 2 continues from correct task (not restart from beginning)
- [ ] B2: Write outside write_scope → control_point (not silent pass)
- [ ] B2: HANDOFF.log has scope_drift event
- [ ] B2: control_point.active = true in HANDOFF.json

## Verification
```yaml
manual:
  - description: "All B1 + B2 acceptance criteria verified and documented"
    required: true
```

## State Update Checklist
- [ ] Update PHASE-STATE.md task 4.1b → done
- [ ] Update TRACKER.md + HANDOFF.json
- [ ] Git commit: `test: integration test 4.1b — HANDOFF continuity + scope lock verified`

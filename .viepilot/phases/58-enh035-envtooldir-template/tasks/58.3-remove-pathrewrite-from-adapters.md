# Task 58.3 — Remove pathRewrite field from claude-code.cjs and cursor.cjs

## Goal
Remove the now-unused `pathRewrite` field from both adapter definitions. `executionContextBase` is the single source of truth.

## Paths
```
lib/adapters/claude-code.cjs
lib/adapters/cursor.cjs
```

## Implementation Notes

### claude-code.cjs — remove:
```js
pathRewrite: { from: '.cursor/viepilot', to: '.claude/viepilot' },
```

### cursor.cjs — remove:
```js
pathRewrite: null,     // no rewrite needed (source is cursor-first)
```

The comment `// no rewrite needed (source is cursor-first)` is also outdated (source is no longer cursor-first) — remove entirely.

No other changes to either file.

## Acceptance Criteria
- [ ] `claude-code.cjs` has no `pathRewrite` key
- [ ] `cursor.cjs` has no `pathRewrite` key or comment
- [ ] Both files still have `executionContextBase` with correct values
- [ ] `npm test` does not fail from these changes alone (tests updated in 58.4)

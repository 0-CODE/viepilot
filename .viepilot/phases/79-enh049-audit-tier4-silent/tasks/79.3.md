# Task 79.3 — CHANGELOG + Version 2.16.0

## Objective
Add `[2.16.0]` section to CHANGELOG documenting ENH-049, and bump `package.json` version to `2.16.0`.

## Paths
- `CHANGELOG.md`
- `package.json`

## File-Level Plan

### CHANGELOG.md
Prepend new section after `[Unreleased]` (or after `[2.15.0]` if no Unreleased):

```markdown
## [2.16.0] - 2026-04-17

### Changed
- **vp-audit** Tier 4 (Framework Integrity) now runs silently — output is suppressed
  when the check passes (✅) or is skipped (non-framework repo ℹ️). Tier 4 is only
  surfaced in the audit report when issues (⚠️) are found. (ENH-049)

### Files
- `workflows/audit.md` — Step 4 skip: removed `echo "→ Tier 4 skipped"` message;
  Step 4f report block wrapped in `TIER4_ISSUES > 0` guard; All Clear banner Tier 4
  line removed; Issues banner Tier 4 line kept as conditional
- `skills/vp-audit/SKILL.md` — documented silent-by-default Tier 4 behavior
- `tests/unit/vp-enh049-audit-tier4-silent.test.js` — ≥5 contract tests
```

### package.json
```json
"version": "2.16.0"
```

## Acceptance Criteria
- [ ] `CHANGELOG.md` has `## [2.16.0]` section with ENH-049 description
- [ ] `package.json` `"version"` = `"2.16.0"`
- [ ] `npm test` passes
- [ ] `git status` clean after commit

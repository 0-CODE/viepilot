# Task 79.2 — skills/vp-audit/SKILL.md + Contract Tests

## Objective
Update `skills/vp-audit/SKILL.md` to document the new silent Tier 4 behavior, then write ≥5 contract tests that verify the `workflows/audit.md` changes from task 79.1.

## Paths
- `skills/vp-audit/SKILL.md`
- `tests/unit/vp-enh049-audit-tier4-silent.test.js`

## File-Level Plan

### 1. skills/vp-audit/SKILL.md

Locate the `## Tier` or audit tiers description section. Add a note under Tier 4:

```markdown
**Tier 4: Framework Integrity** (viepilot framework repo only)
- Runs silently — output suppressed when passing or skipped.
- Only surfaced in report when issues (⚠️) are found.
- Non-framework repos: Tier 4 is skipped with no output.
```

### 2. tests/unit/vp-enh049-audit-tier4-silent.test.js

Create new test file with these contract tests (all `fs.readFileSync` pattern — no execution):

```javascript
const fs = require('fs');
const path = require('path');
const content = fs.readFileSync(path.join(__dirname, '../../workflows/audit.md'), 'utf8');

describe('ENH-049: vp-audit Tier 4 silent mode', () => {
  describe('Step 4 skip guard', () => {
    it('does not echo "Tier 4 skipped" message', () => {
      expect(content).not.toContain('echo "→ Tier 4 skipped');
    });
    it('still has the IS_VIEPILOT_FRAMEWORK guard block', () => {
      expect(content).toContain('IS_VIEPILOT_FRAMEWORK');
    });
  });

  describe('Step 4f conditional guard', () => {
    it('wraps Tier 4 report in TIER4_ISSUES conditional', () => {
      expect(content).toMatch(/TIER4_ISSUES.*>.*0/);
    });
    it('4f section still exists (not deleted)', () => {
      expect(content).toContain('4f');
    });
  });

  describe('Step 5 All Clear banner', () => {
    it('All Clear banner does not contain Tier 4 line', () => {
      // Extract All Clear section
      const allClearStart = content.indexOf('### All Clear');
      const issuesStart = content.indexOf('### Issues Found');
      const allClearSection = content.slice(allClearStart, issuesStart);
      expect(allClearSection).not.toContain('Tier 4');
    });
    it('All Clear banner does not contain "Skipped" for Tier 4', () => {
      const allClearStart = content.indexOf('### All Clear');
      const issuesStart = content.indexOf('### Issues Found');
      const allClearSection = content.slice(allClearStart, issuesStart);
      expect(allClearSection).not.toMatch(/Tier 4.*Skipped/);
    });
  });

  describe('Step 5 Issues banner', () => {
    it('Issues banner retains Tier 4 reference (conditional, for when issues exist)', () => {
      const issuesStart = content.indexOf('### Issues Found');
      const issuesSection = content.slice(issuesStart, issuesStart + 800);
      expect(issuesSection).toContain('Tier 4');
    });
  });
});
```

## Acceptance Criteria
- [ ] `skills/vp-audit/SKILL.md` has note about Tier 4 silent behavior
- [ ] `tests/unit/vp-enh049-audit-tier4-silent.test.js` created with ≥5 tests
- [ ] All tests pass: `npx jest tests/unit/vp-enh049-audit-tier4-silent.test.js`
- [ ] Full test suite passes: `npm test`

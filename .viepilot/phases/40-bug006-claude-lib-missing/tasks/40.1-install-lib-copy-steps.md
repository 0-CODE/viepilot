# Task 40.1 — viepilot-install.cjs: copy steps cho lib files còn thiếu (tất cả targets)

## Objective
Thêm 3 copy_file steps vào **cả 2 blocks** trong `buildInstallPlan()` để copy đủ lib files cho tất cả install targets.

## File
`lib/viepilot-install.cjs`

## Paths + File-Level Plan

### Block 1: Cursor targets (`viepilotDir` ~line 258-262)
Sau step copy `cli-shared.cjs` hiện tại, thêm:
```js
for (const f of ['viepilot-info.cjs', 'viepilot-update.cjs', 'viepilot-install.cjs']) {
  steps.push({ kind: 'copy_file', from: path.join(root, 'lib', f), to: path.join(viepilotDir, 'lib', f) });
}
```

### Block 2: Claude Code target (`claudeViepilotDir` ~line 190-194)
Sau step copy `cli-shared.cjs` hiện tại, thêm:
```js
for (const f of ['viepilot-info.cjs', 'viepilot-update.cjs', 'viepilot-install.cjs']) {
  steps.push({ kind: 'copy_file', from: path.join(root, 'lib', f), to: path.join(claudeViepilotDir, 'lib', f) });
}
```

## Acceptance Criteria
- [ ] Cursor plan có copy steps cho `viepilot-info.cjs`, `viepilot-update.cjs`, `viepilot-install.cjs` vào `~/.cursor/viepilot/lib/`
- [ ] Claude Code plan có copy steps cho 3 files trên vào `~/.claude/viepilot/lib/`

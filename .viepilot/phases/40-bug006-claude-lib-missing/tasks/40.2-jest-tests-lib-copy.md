# Task 40.2 — Jest tests: verify lib copy steps (tất cả targets)

## Objective
Thêm tests verify 3 copy steps mới tồn tại trong plan cho **cả cursor và claude-code targets**.

## File
`tests/unit/viepilot-install.test.js`

## Implementation Notes

```js
describe('BUG-006: all targets have complete lib files', () => {
  const targets = [
    { name: 'claude-code', dirKey: 'claudeViepilotDir', pathFn: (h) => `${h}/.claude/viepilot` },
    { name: 'cursor-ide',  dirKey: 'viepilotDir',       pathFn: (h) => `${h}/.cursor/viepilot` },
  ];

  test.each(targets)('$name: copy step for viepilot-info.cjs present', ({ name }) => {
    const plan = buildInstallPlan({ target: name, home: '/home/test', root: '/pkg' });
    const step = plan.steps.find(s => s.kind === 'copy_file' && s.to.endsWith('lib/viepilot-info.cjs'));
    expect(step).toBeDefined();
  });

  test.each(targets)('$name: copy step for viepilot-update.cjs present', ({ name }) => {
    const plan = buildInstallPlan({ target: name, home: '/home/test', root: '/pkg' });
    const step = plan.steps.find(s => s.kind === 'copy_file' && s.to.endsWith('lib/viepilot-update.cjs'));
    expect(step).toBeDefined();
  });

  test.each(targets)('$name: copy step for viepilot-install.cjs present', ({ name }) => {
    const plan = buildInstallPlan({ target: name, home: '/home/test', root: '/pkg' });
    const step = plan.steps.find(s => s.kind === 'copy_file' && s.to.endsWith('lib/viepilot-install.cjs'));
    expect(step).toBeDefined();
  });
});
```

## Acceptance Criteria
- [ ] `npm test` pass (314 → ~320 tests, 6 new: 3 files × 2 targets)

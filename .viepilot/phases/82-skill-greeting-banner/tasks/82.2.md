# Task 82.2 — Contract tests + CHANGELOG + version 2.19.0

## Objective
Add contract tests verifying every SKILL.md has a `<greeting>` block, write the
CHANGELOG entry, and bump the version to 2.19.0.

## Paths
- `tests/unit/vp-skill-greeting.test.js`
- `CHANGELOG.md`
- `package.json`
- `README.md`

## File-Level Plan

### `tests/unit/vp-skill-greeting.test.js`
New test file. Tests:
1. Each of the 17 skills has `<greeting>` in SKILL.md
2. Each `<greeting>` block contains `VIEPILOT ►`
3. Each `<greeting>` block contains `(fw`
4. `<greeting>` appears before `<objective>` in each file (banner first)
5. `</greeting>` closes the block (properly closed XML)

Pattern: `test.each` over the 17 skill names.

### `CHANGELOG.md`
Add `[2.19.0]` section above `[2.18.0]`:
```markdown
## [2.19.0] - 2026-04-18
### Added
- ENH-056: Skill invocation greeting banner — all 17 `vp-*` skills now output a
  version banner (`VIEPILOT ► {SKILL} v{ver} (fw {fw})`) as the first thing on
  invocation, confirming which version is running.
```

### `package.json`
`"version": "2.18.0"` → `"version": "2.19.0"`

### `README.md`
- Badge: `version-2.18.0` → `version-2.19.0`
- Tests badge: update count after new tests pass

## Acceptance Criteria
- [ ] `tests/unit/vp-skill-greeting.test.js` exists with ≥5 × 17 = 85 tests
- [ ] All new tests pass (+ existing 1021 = 1106 total)
- [ ] `package.json` version = "2.19.0"
- [ ] `CHANGELOG.md` has `[2.19.0]` section

## Verification
```bash
npm test
# Expected: all tests pass, including new greeting tests
node -p "require('./package.json').version"
# Expected: 2.19.0
```

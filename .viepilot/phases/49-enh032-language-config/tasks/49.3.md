# Task 49.3 — bin/vp-tools.cjs: config get/set/reset commands

**Phase**: 49 | **Status**: planned

## Paths
- `bin/vp-tools.cjs` (modify)

## Objective
Add `config` command with subcommands: `get <key>`, `set <key> <value>`, `reset`.

## File-Level Plan
### bin/vp-tools.cjs
- Add `config` entry in `commands` object
- Subcommand dispatch: args[0] = get|set|reset
- `config get language.communication` → prints value
- `config set language.communication vi` → writes config, confirms
- `config reset` → resets to defaults
- Add `config` to help listing

## Acceptance
- [ ] `vp-tools config get language.communication` prints current value
- [ ] `vp-tools config set language.communication vi` writes value
- [ ] `vp-tools config reset` resets to defaults
- [ ] help lists config command

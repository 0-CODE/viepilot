# Task 49.2 — lib/viepilot-install.cjs: Language prompts at install end

**Phase**: 49 | **Status**: planned

## Paths
- `lib/viepilot-install.cjs` (modify)

## Objective
After install completes, prompt user to configure communication and document languages.
With `--yes` / `VIEPILOT_AUTO_YES=1`, skip prompts and write defaults.

## File-Level Plan
### lib/viepilot-install.cjs
- Import `viepilot-config.cjs`
- Add step kind `language_config_prompt` at end of install plan steps
- In `applyInstallPlan`: handle `language_config_prompt` — call `writeConfig` with language values
- Prompt language is `en`/`vi` choice; default `en`

## Acceptance
- [ ] `language_config_prompt` step kind added to install plan
- [ ] --yes skips interactive prompt, uses defaults
- [ ] `~/.viepilot/config.json` written after install

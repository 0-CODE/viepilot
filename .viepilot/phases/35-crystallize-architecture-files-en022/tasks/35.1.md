# Task 35.1 — crystallize Step 4: `.viepilot/architecture/<diagram>.mermaid`

## Objective
Định nghĩa trong **`workflows/crystallize.md`** cách tạo thư mục `.viepilot/architecture/` và file `.mermaid` theo **6 loại** matrix (đồng tên ổn định với ENH-018).

## Paths
- `workflows/crystallize.md`
- `.viepilot/phases/35-crystallize-architecture-files-en022/PHASE-STATE.md`

## File-Level Plan
- Liệt kê tên file canonical: `system-overview.mermaid`, `data-flow.mermaid`, `event-flows.mermaid`, `module-dependencies.mermaid`, `deployment.mermaid`, `user-use-case.mermaid`.
- Quy tắc: `required` hoặc `optional` **có** khối Mermaid → **ghi file** (raw Mermaid, không fence markdown trong file).
- `N/A` hoặc không sinh diagram → **không** tạo file rỗng.
- Ghi rõ cách `ARCHITECTURE.md` trỏ tới file (relative path) và yêu cầu **cùng một nguồn sự thật** (copy đồng bộ hoặc mô tả rõ AI phải giữ hai nơi khớp — chọn một policy).

## Verification
- `rg "architecture/|\\.mermaid" workflows/crystallize.md`
- `npm test`

## Status
done (2026-04-02)

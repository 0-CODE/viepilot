# Task 35.4 — vp-audit + Jest contracts for ENH-022

## Objective
Mở rộng **`skills/vp-audit/SKILL.md`** (nếu có mục ARCHITECTURE) để nhắc kiểm tra matrix ↔ file `.mermaid`. Thêm **Jest** contract cho chuỗi bắt buộc trong `crystallize.md` + template (theo pattern `vp-en021-*` / `vp-feat001-*`).

## Paths
- `skills/vp-audit/SKILL.md`
- `tests/unit/vp-enh022-crystallize-architecture-files-contracts.test.js` (tên có thể chỉnh khi implement)
- `CHANGELOG.md` [Unreleased] khi ship
- `README.md` metrics nếu test count thay đổi (script sync)

## File-Level Plan
- Audit: một bullet “optional / recommended” — presence of `.viepilot/architecture/*.mermaid` when matrix marks diagram required.
- Test: assert files exist + substring checks cho `crystallize.md` và `templates/project/ARCHITECTURE.md`.

## Verification
- `npm test`

## Status
done (2026-04-02)

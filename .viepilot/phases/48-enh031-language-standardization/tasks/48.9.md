# Task 47.9 — Jest contract tests: verify language standardization

**Phase**: 48 | **Status**: todo

## Paths
- `tests/vp-enh031-language-standardization.test.js` (new file)

## Test Coverage

### Test 1: No Vietnamese prose in workflow files
For each workflow file (`workflows/*.md`):
- Read file content
- Extract lines NOT inside `cursor_skill_adapter` blocks
- Assert: no Vietnamese diacritic characters in those lines
- Allow list: code blocks (` ``` `), inline code (`` ` ``), and content between these markers

```javascript
const VIEPILOT_CHARS = /[àáảãạăắằẳẵặâấầẩẫậđèéẻẽẹêếềểễệìíỉĩịòóỏõọôốồổỗộơớờởỡợùúủũụưứừửữựỳýỷỹỵÀÁẢÃẠĂẮẰẲẴẶÂẤẦẨẪẬĐÈÉẺẼẸÊẾỀỂỄỆÌÍỈĨỊÒÓỎÕỌÔỐỒỔỖỘƠỚỜỞỠỢÙÚỦŨỤƯỨỪỬỮỰỲÝỶỸỴ]/;
```

### Test 2: No Vietnamese prose in SKILL.md files (outside invocation triggers)
For each `skills/vp-*/SKILL.md`:
- Extract content OUTSIDE `<cursor_skill_adapter>...</cursor_skill_adapter>` blocks
- Assert: no Vietnamese diacritic characters in extracted content

### Test 3: cursor_skill_adapter blocks intact
For each SKILL.md:
- Assert: `<cursor_skill_adapter>` block still exists
- Assert: block contains at least one non-empty line (not accidentally emptied)

### Test 4: AI-GUIDE.md template is English
- Read `templates/project/AI-GUIDE.md`
- Assert: no Vietnamese diacritic characters anywhere in file

### Test 5: Frontmatter descriptions are English
For each SKILL.md:
- Extract `description:` frontmatter field
- Assert: no Vietnamese diacritic characters in description value

## Acceptance
- [ ] All 5 test groups pass
- [ ] Tests added to Jest suite alongside existing contract tests
- [ ] `npm test` passes (all tests green)

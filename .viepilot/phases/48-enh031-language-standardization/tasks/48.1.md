# Task 47.1 — HIGH workflows: crystallize.md, autonomous.md, audit.md

**Phase**: 48 | **Status**: done

## Paths
- `workflows/crystallize.md`
- `workflows/autonomous.md`
- `workflows/audit.md`

## Language Policy
- **Convert to English:** all prose, section headers, bullet descriptions, inline comments, narrative text
- **Keep Vietnamese:** `cursor_skill_adapter` invocation keyword strings ONLY (e.g., `"crystallize"`, `"tinh chế"`)
- **Keep English:** all code blocks, bash, yaml, json, markdown templates

## crystallize.md — Key patterns to fix
- Mixed-language sentences like `"parse ## Phases từ brainstorm sessions"` → `"parse ## Phases from brainstorm sessions"`
- Vietnamese inline explanations like `"Đọc brainstorm output"` → `"Read brainstorm output"`
- Section descriptions using Vietnamese → translate to English
- Any `<purpose>`, `<step>`, `<note>` block content in Vietnamese → English

## autonomous.md — Key patterns to fix
- Section headers in Vietnamese with English body → convert headers to English
- Vietnamese inline comments scattered through English process blocks → translate
- Vietnamese bullet items in otherwise-English lists → translate
- Status/banner output strings in Vietnamese → translate (NOTE: these will later be replaced by config.language in ENH-032, but for now standardize to English)

## audit.md — Key patterns to fix
- `<purpose>` block: Vietnamese → English
- Section-level language switching (Vietnamese explanations, English bash) → all explanations to English
- Mixed descriptions like `"Hoạt động trên bất kỳ project nào"` → `"Works on any project"`

## Acceptance
- [x] No Vietnamese prose in crystallize.md outside of invocation triggers
- [x] No Vietnamese prose in autonomous.md outside of invocation triggers
- [x] No Vietnamese prose in audit.md outside of invocation triggers
- [x] All mixed-language sentences resolved (no sentence containing both language scripts)
- [x] `grep -c "[àáảãạ...]" workflows/crystallize.md` — only remaining line is the UI scope signal keyword list (line ~134): these are scan-target data patterns for detecting Vietnamese content in user brainstorm files (not prose). Removing them would break UI scope detection for Vietnamese projects. Documented exception.

## Implementation Notes
- **UI scope signal keywords** (crystallize.md line ~134): `màu`, `màu sắc`, `màn hình`, etc. are kept Vietnamese intentionally — they are pattern-match scan targets embedded in the hard gate logic that detects Vietnamese brainstorm files. They are not framework prose. This is a deliberate exception per ENH-031 intent (language standardization of narrative prose, not scan-target data).

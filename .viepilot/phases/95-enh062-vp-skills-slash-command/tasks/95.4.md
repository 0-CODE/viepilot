# Task 95.4 — Contract tests + CHANGELOG + version 2.31.0

## Objective
Write ≥12 contract tests covering vp-skills SKILL.md + get-registry CLI + BUG-016 workflow fixes. Update CHANGELOG [2.31.0], bump package.json.

## Paths
- `tests/unit/vp-skills-slash-command.test.js`
- `CHANGELOG.md`
- `package.json`

## Implementation Notes

### Test coverage (≥12)

```
skills/vp-skills/SKILL.md contracts
  1. skills/vp-skills/SKILL.md exists
  2. contains "vp-skills"
  3. contains "scan"
  4. contains "install"
  5. contains "get-registry" (installed path usage)
  6. contains "~/.claude/viepilot" (cross-project path)

CLI: get-registry
  7. --help includes "get-registry"
  8. get-registry (no args) outputs valid JSON or "null"
  9. get-registry --id non-existent-skill outputs "null"

BUG-016 fix contracts
  10. workflows/autonomous.md does NOT contain "Call loadRegistry()"
  11. workflows/autonomous.md contains "get-registry"
  12. workflows/brainstorm.md does NOT contain "Call loadRegistry()"
  13. workflows/brainstorm.md contains "get-registry"
```

### CHANGELOG entry
```markdown
## [2.31.0] - 2026-04-21

### Added (ENH-062)
- `skills/vp-skills/SKILL.md`: new `/vp-skills` slash command — agent-native global skill registry management from any project; commands: scan, list, install, uninstall, update, info; uses installed vp-tools path (`~/.claude/viepilot/bin/vp-tools.cjs`)
- `bin/vp-tools.cjs`: new `get-registry [--id <id>]` subcommand — outputs registry JSON to stdout; enables workflow shell integration

### Fixed (BUG-016)
- `workflows/autonomous.md`: replaced non-executable `Call loadRegistry()` with `node ~/.claude/viepilot/bin/vp-tools.cjs get-registry --id {id}` shell command
- `workflows/brainstorm.md`: replaced `Call loadRegistry()` with `node ~/.claude/viepilot/bin/vp-tools.cjs get-registry` shell command + cat fallback
```

## Acceptance Criteria
- [ ] `tests/unit/vp-skills-slash-command.test.js` with ≥12 tests
- [ ] All tests pass (`npm test`)
- [ ] `[2.31.0]` in CHANGELOG.md
- [ ] `package.json` version = "2.31.0"

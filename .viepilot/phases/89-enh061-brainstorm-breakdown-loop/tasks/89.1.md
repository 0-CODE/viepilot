# Task 89.1 — Add recommended breakdown ordering + coverage mapping step to `workflows/brainstorm.md`

## Objective
1. Add a **Recommended Breakdown Ordering** section that documents the 8-step idea→architect+UI flow
2. Add a **Feature → Coverage Mapping** step that triggers after scope is locked and populates a `## Coverage` matrix in notes.md

## Paths
- `workflows/brainstorm.md`

## Implementation Notes

### Part A: Recommended Breakdown Ordering section
Insert a new section **before** `### Architect Design Mode` (currently around line 201).

```markdown
### Recommended Breakdown Ordering (ENH-061)

For sessions that involve both system architecture and UI/UX, follow this sequence to ensure complete breakdown from idea to artifacts:

```
Step 1: Free idea collection
  → Explore the problem space; no structure required yet

Step 2: Scope lock + Phase assignment (existing)
  → Assign all features to Phase 1 / Phase 2 / Phase 3...
  → Fill ## Phases in session draft

Step 3: Feature → Coverage mapping (NEW)
  → For each Phase 1 feature: name the architect component AND UI screen
  → Output: ## Coverage matrix in notes.md

Step 4: Architect Design (existing)
  → Fill architect workspace per coverage matrix
  → Use /vp-brainstorm --architect or let heuristics fire

Step 5: Architect → UI sync (NEW — arch_to_ui_sync)
  → After architect edits: check which architectural decisions impact UI screens
  → Prompt user to update UI Direction accordingly

Step 6: UI Direction (existing)
  → Fill UI workspace per coverage matrix + arch feedback
  → Use /vp-brainstorm --ui or proactive 🎨 banner

Step 7: Completeness gate (NEW — expanded pre-save check)
  → Validate: every Phase 1 feature has ≥1 coverage (architect OR UI)
  → Warn on any feature with no coverage in both modes

Step 8: /save → /vp-crystallize (existing)
```

> **Note**: Steps 3–7 are optional for simple/early-stage sessions. The flow is recommended when a session produces both Architect Design and UI Direction artifacts.
```

### Part B: Feature → Coverage Mapping step
Insert a new subsection **after the Pre-Save Phase Assignment Validation** section and **before** the `/end` command documentation. Title: `### Feature → Coverage Mapping (ENH-061)`

```markdown
### Feature → Coverage Mapping (ENH-061)

**Trigger**: After `## Phases` is confirmed (scope locked) and before `/save`.

For each feature/capability listed under **Phase 1** in the session draft, ask:
- **Architect coverage**: which architect page handles this? (`architecture`, `data-flow`, `erd`, `apis`, `tech-stack`, `decisions`, `user-use-cases`, `sequence-diagram`, `deployment`, or `none yet`)
- **UI coverage**: which UI screen/page handles this? (page slug from `pages/*.html`, `index.html` for single-page, or `none yet`)

Output a `## Coverage` section in `notes.md`:

```markdown
## Coverage
| Feature | Architect page | UI screen |
|---------|---------------|-----------|
| User authentication | architecture, apis | login.html, register.html |
| Dashboard overview | data-flow | dashboard.html |
| Notification system | architecture | none yet |
```

**Warning rules**:
- If a Phase 1 feature has `none yet` in **both** columns: show warning:
  `⚠️ Feature "{name}" has no coverage in Architect or UI Direction. Add it to a workspace before /save.`
- If Architect workspace has pages but coverage matrix is empty: suggest running coverage mapping.

**This step is non-blocking** — the user can dismiss with "skip coverage" or "fill in later".
```

## Verification
```bash
grep -c "Recommended Breakdown Ordering" workflows/brainstorm.md   # ≥1
grep -c "Coverage Mapping" workflows/brainstorm.md                  # ≥1
grep -c "ENH-061" workflows/brainstorm.md                           # ≥1
grep -c "## Coverage" workflows/brainstorm.md                       # ≥1
```

## Acceptance Criteria
- [ ] Recommended Breakdown Ordering section present (8 steps)
- [ ] Feature → Coverage Mapping subsection present
- [ ] `## Coverage` matrix format documented
- [ ] Warning for features with no coverage in both columns
- [ ] Non-blocking (can be skipped)
- [ ] ENH-061 tagged

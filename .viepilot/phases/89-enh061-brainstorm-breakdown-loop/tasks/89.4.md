# Task 89.4 — Update `skills/vp-brainstorm/SKILL.md` — breakdown loop docs

## Objective
Add the ENH-061 breakdown loop capabilities to the SKILL.md capability list so users and the model know the feature exists.

## Paths
- `skills/vp-brainstorm/SKILL.md`

## Implementation Notes

In the capabilities list (the bullet list starting around line 53), add after the existing `Background UI extraction` line:

```markdown
- **Idea → Architecture breakdown loop (ENH-061):** after scope is locked, a structured **Feature → Coverage mapping** step maps each Phase 1 feature to both an Architect page and a UI screen — outputs `## Coverage` matrix in `notes.md`. **`arch_to_ui_sync`** reverse sync (symmetric to ENH-034): after architect workspace edits, surfaces UI implications (async flows, auth roles, error codes) and prompts UI Direction updates. `/sync-ui` command triggers this manually. A pre-save **completeness gate** (CHECK 3) warns if any Phase 1 feature has no coverage in either mode. The **Recommended Breakdown Ordering** (8-step flow) documents when to run each step.
```

## Verification
```bash
grep -c "ENH-061" skills/vp-brainstorm/SKILL.md       # ≥1
grep -c "arch_to_ui_sync" skills/vp-brainstorm/SKILL.md  # ≥1
grep -c "Coverage mapping" skills/vp-brainstorm/SKILL.md  # ≥1
```

## Acceptance Criteria
- [ ] Breakdown loop entry in capability list
- [ ] `arch_to_ui_sync` mentioned
- [ ] Coverage mapping mentioned
- [ ] ENH-061 tagged

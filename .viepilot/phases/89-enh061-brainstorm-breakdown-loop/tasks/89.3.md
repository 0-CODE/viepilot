# Task 89.3 — Expand pre-save completeness gate with coverage matrix check

## Objective
Extend the existing **Pre-Save Phase Assignment Validation** (ENH-052) to also check the **Feature → Coverage matrix**. If Phase 1 features exist but have no coverage in either architect or UI Direction workspace, warn the user before `/save`.

## Paths
- `workflows/brainstorm.md`

## Implementation Notes

Find the `### Pre-Save Phase Assignment Validation (ENH-052)` section. After the existing CHECK 1 / CHECK 2 block, add CHECK 3:

```markdown
**CHECK 3 (ENH-061)**: Does the session have a `## Coverage` matrix in notes.md?
- If YES: scan for features where both `Architect page` = `none yet` AND `UI screen` = `none yet`.
  - If any found: warn with list of uncovered features (non-blocking — user can proceed with acknowledgement).
- If NO and the session has BOTH Architect workspace AND UI Direction workspace active:
  - Suggest running coverage mapping before save:
    `⚠️  Both Architect and UI Direction are active — consider running Feature → Coverage mapping to ensure full breakdown.`
  - Non-blocking: user can proceed.
```

Also add `/sync-ui` to the commands list at the bottom of the file (alongside `/sync-arch`).

## Verification
```bash
grep -c "CHECK 3" workflows/brainstorm.md    # ≥1
grep -c "ENH-061" workflows/brainstorm.md    # ≥2 (task 89.1 + this)
```

## Acceptance Criteria
- [ ] CHECK 3 added to pre-save validation block
- [ ] Warns when Phase 1 features have no coverage in both columns
- [ ] Non-blocking (user can proceed with acknowledgement)
- [ ] `/sync-ui` added to commands list

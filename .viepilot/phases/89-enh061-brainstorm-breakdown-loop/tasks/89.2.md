# Task 89.2 — Add `arch_to_ui_sync` reverse sync step to `workflows/brainstorm.md`

## Objective
Add a **Architect → UI Direction reverse sync** step (`arch_to_ui_sync`) — symmetric to the existing `architect_delta_sync` (ENH-034, UI→Architect). When architect workspace pages are edited, scan for decisions that constrain or expand UI behavior and prompt the user to update UI Direction accordingly.

## Paths
- `workflows/brainstorm.md`

## Implementation Notes

Insert a new section **after** `### UI Direction — UX walkthrough & upgrade (FEAT-010)` and **before** the existing `## 6B. Architect Delta Sync (ENH-034)` section.

Title: `## 6C. Architect → UI Direction Sync (ENH-061)`

```markdown
## 6C. Architect → UI Direction Sync (ENH-061)

Bridges the gap from **Architect Design** → **UI Direction** (reverse of architect_delta_sync which goes UI → Architect).

When the architect workspace is updated (any page edit during the session), scan the changed content for decisions that carry **UI implications**:

### Architectural decisions that trigger UI updates

| Architect signal | UI implication |
|-----------------|----------------|
| Async/event-driven flow | Loading states, optimistic updates, error handling screens |
| API pagination | Pagination component, empty state, scroll-to-load |
| Auth roles / permissions | Conditional UI rendering, role-specific screens |
| Rate limiting / quota | Warning banners, upgrade prompts, disabled states |
| Real-time → polling fallback | Refresh indicators, stale data warnings |
| File size / type constraints | Upload validation, error messages, progress indicators |
| Third-party OAuth | Redirect flow, consent screen, token expiry handling |
| Error codes from APIs | Error state screens, retry affordances |

### Trigger conditions
`arch_to_ui_sync` runs when:
- User edits any architect workspace page during an active brainstorm session
- User types `/sync-ui` (manual trigger, parallel to `/sync-arch`)
- After `/review-arch` if changes were applied

### Output format
```
🎨 Architect → UI sync detected implications:

From `data-flow.html` (async notification queue):
  → UI may need: notification bell with unread count, real-time badge updates, empty state for no notifications
  → Affected screens: [dashboard.html, notifications.html] or "not yet modeled"

Update UI Direction to reflect these decisions?
1. Yes — open UI Direction and add these screens/states
2. Note it in notes.md ## arch_to_ui_sync for later
3. Skip — already handled
```

### Manual command
`/sync-ui` — manually trigger arch_to_ui_sync from current architect session state.

### Notes.md record
Append to `notes.md`:
```yaml
## arch_to_ui_sync
- architect_page: data-flow.html
  decision: async notification queue
  ui_implication: unread count badge, empty state, real-time update
  status: noted | addressed | skipped
  date: {YYYY-MM-DD}
```
```

## Verification
```bash
grep -c "arch_to_ui_sync" workflows/brainstorm.md   # ≥2 (section + command)
grep -c "6C\." workflows/brainstorm.md               # ≥1
grep -c "/sync-ui" workflows/brainstorm.md           # ≥1
```

## Acceptance Criteria
- [ ] `## 6C. Architect → UI Direction Sync` section present
- [ ] Trigger conditions documented
- [ ] Output format / banner documented
- [ ] `/sync-ui` command documented
- [ ] `## arch_to_ui_sync` notes.md schema documented
- [ ] `arch_to_ui_sync` appears in `/sync-ui` command list at bottom of file

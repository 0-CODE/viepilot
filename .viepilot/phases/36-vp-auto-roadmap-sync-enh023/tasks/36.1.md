# Task 36.1 — Thêm ROADMAP.md vào Step 3 PASS handler

## Objective
Sửa `~/.cursor/viepilot/workflows/autonomous.md`: thêm `ROADMAP.md` vào danh sách update trong Step 3 Handle Result PASS block, ngay sau dòng `Update CHANGELOG.md if feature/fix`.

## Paths
- `~/.cursor/viepilot/workflows/autonomous.md`

## File-Level Plan

### `autonomous.md` — Step 3 Handle Result PASS (~dòng 227-232)

**Hiện tại:**
```
**PASS:**
- Create git tag: `{projectPrefix}-vp-p{phase}-t{task}-done`
- Update PHASE-STATE.md immediately: task → done, append files changed by this task to Files Changed table (individual files, no glob patterns)
- Update TRACKER.md immediately
- Update HANDOFF.json immediately
- Update CHANGELOG.md if feature/fix
- Move to next task
```

**Sau khi sửa** — thêm 1 dòng trước `Move to next task`:
```
**PASS:**
- Create git tag: `{projectPrefix}-vp-p{phase}-t{task}-done`
- Update PHASE-STATE.md immediately: task → done, append files changed by this task to Files Changed table (individual files, no glob patterns)
- Update TRACKER.md immediately
- Update HANDOFF.json immediately
- Update CHANGELOG.md if feature/fix
- Update ROADMAP.md: sync phase progress % và task count nếu phase status/progress thay đổi
- Move to next task
```

## Acceptance Criteria
- [ ] Dòng `Update ROADMAP.md: sync phase progress % và task count nếu phase status/progress thay đổi` xuất hiện trong Step 3 PASS block
- [ ] Dòng này nằm sau `Update CHANGELOG.md` và trước `Move to next task`
- [ ] Step 4 `update_state` không bị thay đổi (vẫn là rule chung)
- [ ] Step 5a ROADMAP sync vẫn còn nguyên

## Verification
```bash
grep "Update ROADMAP.md" ~/.cursor/viepilot/workflows/autonomous.md
# Expected: ít nhất 2 occurrences (Step 3 mới + Step 4 cũ)

grep -c "ROADMAP" ~/.cursor/viepilot/workflows/autonomous.md
# Expected: >= 4 lines
```

## Complexity: S

# Task 47.4 — Jest contract tests: vp-bug009-path-guard.test.js

**Phase**: 47 | **Status**: todo

## Paths
- `tests/unit/vp-bug009-path-guard.test.js` (new file)

## Test Coverage

### Test group 1: evolve.md contains the TASK PATH RULE guard
```javascript
describe('workflows/evolve.md — TASK PATH RULE guard', () => {
  it('contains BUG-009 guard label')
  it('contains CORRECT examples (repo-relative paths)')
  it('contains INCORRECT examples (~/.claude/ paths)')
  it('guard appears before task file generation template')
})
```

### Test group 2: autonomous.md contains preflight path validation
```javascript
describe('workflows/autonomous.md — preflight path validation', () => {
  it('contains BUG-009 preflight check')
  it('check mentions ~/ prefix as invalid')
  it('check mentions absolute path / prefix as invalid')
  it('error message template references offending path and task file location')
})
```

### Test group 3: SKILL.md documentation
```javascript
describe('SKILL.md path convention docs', () => {
  it('vp-evolve SKILL.md contains BUG-009 path convention note')
  it('vp-auto SKILL.md contains BUG-009 path validation note')
})
```

### Test group 4: Existing phase task files use repo-relative paths
```javascript
describe('Phase 47–49 task files — repo-relative paths only', () => {
  // Read all task files in phases/47-*, phases/48-*, phases/49-*
  // Check ## Paths blocks
  it('no task file ## Paths block contains ~/.claude/ prefix')
  it('no task file ## Paths block contains absolute / prefix')
})
```

## Acceptance
- [ ] All 4 test groups pass
- [ ] `npm test` green (all existing tests still pass)
- [ ] Test group 4 catches the original BUG-009 scenario (would have failed on old Phase 47/48 files)

# Task 47.1 — workflows/evolve.md: add TASK PATH RULE guard

**Phase**: 47 | **Status**: todo

## Paths
- `workflows/evolve.md`

## Changes

Locate the step in `evolve.md` that generates task `.md` files (the step that writes `## Paths`, task descriptions, acceptance criteria into phase task files). Add the following guard block **immediately before** any task file generation instruction:

```
⚠️ TASK PATH RULE (BUG-009):
When writing the `## Paths` block in any task .md file, ALWAYS use
paths relative to the repository root (where package.json / Makefile
is located).

CORRECT:
  workflows/crystallize.md
  skills/vp-audit/SKILL.md
  templates/project/AI-GUIDE.md
  lib/viepilot-config.cjs
  bin/vp-tools.cjs
  tests/unit/my-test.test.js

INCORRECT (never use these forms in ## Paths):
  ~/.claude/viepilot/workflows/crystallize.md
  ~/.claude/skills/vp-audit/SKILL.md
  /Users/someone/.claude/...
  /absolute/path/to/anything

Exception: paths INSIDE code block content (e.g., inside a bash
snippet describing what the code reads/writes at runtime) may use
absolute paths (e.g., `~/.claude/viepilot/config.json` inside a
bash example). Only the `## Paths` header block must be repo-relative.

If you are unsure of the repo-relative path, run:
  ls workflows/   → for workflow files
  ls skills/      → for skill files
  ls lib/         → for library files
  ls bin/         → for CLI files
  ls templates/   → for template files
```

## Placement
Add this guard as a named note or callout box right before the task file generation template in the evolve workflow. It should be visible every time a new task file is being drafted.

## Acceptance
- [ ] TASK PATH RULE guard present in `workflows/evolve.md`
- [ ] Guard appears before/at the task generation step (not buried elsewhere)
- [ ] Guard lists CORRECT and INCORRECT examples
- [ ] Guard notes the exception for code blocks inside task content

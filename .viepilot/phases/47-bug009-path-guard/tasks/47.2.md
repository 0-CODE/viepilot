# Task 47.2 — workflows/autonomous.md: preflight path validation

**Phase**: 47 | **Status**: todo

## Paths
- `workflows/autonomous.md`

## Changes

Locate the preflight / task-start step in `autonomous.md` (the section that reads a task file before executing it). Add a path validation check:

```
PREFLIGHT CHECK — Task Paths validation (BUG-009):

Before executing any task, read the `## Paths` block and validate:

FOR EACH path listed in ## Paths:
  IF path starts with `~/` OR starts with `/` (absolute path):
    → STOP. Do NOT execute this task.
    → Output error:

    ⛔ TASK PATH ERROR (BUG-009)
    Task {N}.{X} contains an absolute or external path:
      "{offending_path}"

    Expected: a repo-relative path (e.g., workflows/foo.md, skills/vp-bar/SKILL.md)

    Fix the task file before continuing:
      .viepilot/phases/{N}-{slug}/tasks/{N}.{X}.md

    Replace:
      {offending_path}
    With the repo-relative equivalent.

  ELSE:
    → Continue with task execution normally.
```

## Placement
Insert this check at the beginning of the per-task execution loop, right after the task file is read and before any file edits are made.

## Acceptance
- [ ] Path validation check present in `workflows/autonomous.md`
- [ ] Check fires on `~/` prefix → outputs ⛔ error and aborts task
- [ ] Check fires on `/` absolute prefix → outputs ⛔ error and aborts task
- [ ] Check passes silently for repo-relative paths (no output)
- [ ] Error message names the offending path and the task file location
- [ ] Other tasks in the phase continue after user fixes the bad task file

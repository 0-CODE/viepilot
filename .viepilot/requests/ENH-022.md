# 🔧 ENHANCEMENT: vp-crystallize — lưu diagram kiến trúc vào `.viepilot/architecture/*.mermaid`

## Meta
- **ID**: ENH-022
- **Type**: Enhancement
- **Status**: done (shipped **1.9.5**)
- **Created**: 2026-04-02
- **Breaking**: no

## Summary
Trong luồng **vp-crystallize**, các diagram kiến trúc (theo ma trận applicability trong Step 4 / ENH-018) cần được **vẽ (sinh nội dung Mermaid)** và **lưu** thành file tách biệt tại `.viepilot/architecture/<diagram>.mermaid` trong dự án, không chỉ nhúng trong `ARCHITECTURE.md`.

## Current Feature
- **vp-crystallize** + `workflows/crystallize.md` Step 4 sinh `ARCHITECTURE.md` với khối Mermaid nội tuyến (hoặc N/A) theo matrix `system-overview`, `data-flow`, `event-flows`, `module-dependencies`, `deployment`, `user-use-case` (Phase 29 / ENH-018).

## Current Behavior
- Nội dung Mermaid chủ yếu nằm trong `ARCHITECTURE.md`; chưa có chuẩn thư mục `.viepilot/architecture/` với file `.mermaid` theo tên diagram.

## Desired Improvement
1. Tạo thư mục `.viepilot/architecture/` khi crystallize sinh diagram **required** hoặc **optional** (có nội dung thực).
2. Đặt tên file ổn định, ví dụ: `system-overview.mermaid`, `data-flow.mermaid`, `event-flows.mermaid`, `module-dependencies.mermaid`, `deployment.mermaid`, `user-use-case.mermaid` (khớp loại trong matrix).
3. Mỗi file chứa **chỉ** nguồn Mermaid (hoặc comment tiêu đề tối thiểu nếu workflow quy ước).
4. `ARCHITECTURE.md` **tham chiếu** file (link đường dẫn tương đối + có thể giữ snippet `mermaid` đồng bộ hoặc `include` theo template đã chọn — cần quyết định trong implement để tránh drift).
5. Cập nhật **skill `vp-crystallize`**, **template project** (nếu có), và **workflow `crystallize.md`** cho hành vi mới; **vp-audit** (nếu có rule matrix ↔ file) kiểm tra nhất quán.

## Value Proposition
- Dễ diff/review diagram trong PR; tooling (Mermaid CLI, IDE preview) mở trực tiếp file; tái sử dụng diagram ngoài markdown dài.

## Acceptance Criteria
- [x] `crystallize.md` Step 4 mô tả rõ: khi nào ghi `.viepilot/architecture/<diagram>.mermaid` và mapping tên ↔ loại matrix.
- [x] Template / generator crystallize tạo thư mục + file `.mermaid` tương ứng các diagram được sinh (không tạo file rỗng thừa cho mục N/A).
- [x] `ARCHITECTURE.md` (template) hướng dẫn đọc/ghi diagram qua `.viepilot/architecture/` và tránh mâu thuẫn với matrix.
- [x] Skill `vp-crystallize` success criteria / process nhắc artifact `.viepilot/architecture/*.mermaid`.
- [x] Audit + Jest contracts (`vp-enh022-*`).

## Related
- **Requests**: ENH-018 (ARCHITECTURE Mermaid + matrix)
- **Phase**: 29-crystallize-architecture-mermaid (có thể mở phase follow-up hoặc gộp vào milestone tiếp theo)
- **Skills/workflows**: `vp-crystallize`, `workflows/crystallize.md`, `templates/project/`
- **Dependencies**: Không phá vỡ dự án chỉ có Mermaid trong `ARCHITECTURE.md` — migration hoặc dual-write một phiên bản nếu cần.

## Implementation Notes
_To be filled when triaged (evolve + auto)._

## Resolution
**1.9.5** — `workflows/crystallize.md` ENH-022 subsection; `templates/project/ARCHITECTURE.md`; `skills/vp-crystallize` 0.5.2; `skills/vp-audit` 0.3.2; `docs/skills-reference.md`; `tests/unit/vp-enh022-crystallize-architecture-files-contracts.test.js`.

# ENH: Optional symlink install for Cursor skills (dev / power users)

## Meta
- **ID**: ENH-015
- **Type**: Enhancement
- **Status**: done
- **Priority**: medium
- **Created**: 2026-04-01
- **Reporter**: User
- **Assignee**: AI

## Summary
Cho phép cài skills vào `~/.cursor/skills/` bằng **symlink** trỏ về repo (sửa skill trong clone là Cursor thấy ngay), thay vì chỉ copy — đặc biệt hữu ích cho **`vp-info`** / **`vp-update`** và mọi skill `vp-*` khi dev ViePilot.

## Details
- Mặc định giữ **copy-first** (FEAT-005) để tránh lỗi discovery.
- Thêm biến môi trường **`VIEPILOT_SYMLINK_SKILLS=1`**: `dev-install.sh` và `install.sh` dùng `ln -sfn` với đường dẫn tuyệt đối tới từng `skills/vp-*`.
- Document trong comment đầu script + CHANGELOG [Unreleased].

## Acceptance Criteria
- [x] `VIEPILOT_SYMLINK_SKILLS=1` + `dev-install.sh` symlink toàn bộ `skills/vp-*`.
- [x] `install.sh` hỗ trợ cùng cờ (optional consistency).
- [x] Request + TRACKER backlog row.

## Related
- **Depends on**: FEAT-005 (copy-first default remains)
- **Files**: `dev-install.sh`, `install.sh`

## Resolution
- **2026-04-01** — Implemented `VIEPILOT_SYMLINK_SKILLS=1` in `dev-install.sh` and `install.sh`; symlink uses absolute paths via `realpath` when available, else `cd && pwd`.
